# Kummer surface: p-adic density of rational points

$p$-adic density of rational points on the Kummer surface $X : y^2 = f(x)f(t)$,
where $f = x^3 + Ax + B$ has no repeated root and $X = \mathrm{Kum}(E \times E)$
for $E : v^2 = f(u)$.

Tested with PARI/GP 2.18. Run everything **from this directory** (the scripts
`read()` each other by relative path), and always close stdin, otherwise `gp`
drops into its interactive prompt after the script:

```sh
gp -q -s 4000000000 driver.gp < /dev/null
```

Note: do **not** put `default(parisize, N)` inside a script — in `gp` that
aborts the rest of the file. Use the `-s` flag as above.

## Files

| file | contents |
|---|---|
| `kummer2.gp` | **core.** `densegroup(Em, pts, p)` — is the subgroup generated by `pts` dense in `Em(Q_p)`? Also `Mval`, `sqclass`, `twistdata`. Odd `p`. |
| `driver.gp` | the search. `sweep` + `procyclic` + `hybrid` are the fast path (see **Search strategy** below); `build`/`report` are the slow pure-descent reference path, `hunt(A,B,p,target,DMAX)` a targeted single-class search. |
| `p2.gp` | the `p = 2` variant: `densegroup2`, `M2val`, `sqclass2` (8 classes), `report2`. |
| `cover2.gp` | independent verification for odd `p`: `coverage(A,B,p,k,ds,NB)` checks that every genuine reduction mod `p^k` of a point of `X(Z_p)` with `y` a unit is hit by an honest rational point. |
| `families.gp` | enumerates all quadratic-twist families with `E[3]` decomposable and tests the denominator-of-`j` criterion (document §5.2.3): `families`, `testfamily`, `sweepfamilies`. |
| `verify-dual.sage` | independent Sage check of the one computational input to the §5.1.5 theorem (uses Sage's own `.dual()`); run under Docker, see below. That input is now *proved* -- see below and document §5.1.5. |
| `sadic.gp` | `S`-adic density (document §2.2): `Mstar`, `coprimeS`, `densefactorwise`, `denseS`, `reportS`; the level-2 product test `inE2p`, `Border`, `denseprod`, `reportSprod`, and the per-tuple search `tuplepart`, `tuplename`, `reportSprodk`, `reportSprodtuples`; and the rank dichotomy `torsdim`, `torsdimloc`, `gexactS` (exact) against `torsdimUB`, `gtop`, `triage` (bounds). |
| `residues.typ` | **the residue map on the Brauer group, and what "unramified" means** (a question of Rene's; companion to `csa-brauer.typ` and `corestriction.typ`). For a discrete valuation `v` on `K` the map `d_v : Br(K) -> H^1(kappa(v), Q/Z)`, given three ways: as the INERTIA edge map of Hochschild-Serre (`H^1(I,mu_n) = Hom(mu_n,mu_n) = Z/n` with trivial residue action -- the twist in the tame inertia cancels the twist in the coefficients); as the GYSIN boundary of the etale localisation sequence, which is where the answer to *why codimension one* comes from (absolute purity gives `H^(q+1)_Z(X,mu_n) = H^(q-1)(Z,Z/n)` only for `c=1`; in codimension `c` the boundary lands in `H^(3-2c)`, zero for `c>=2`); and as the TAME SYMBOL `d_v(a,b) = (-1)^(v(a)v(b)) a^v(b) b^-v(a) mod n`, which is what one computes with. Kernel = classes extending to an AZUMAYA algebra over `O_v` (good reduction) = classes split by `K_v^nr`; injectivity of `Br(O_v) -> Br(K)` is Auslander-Goldman, exactness is purity. **Two degenerations worth knowing**: over a local field `Br(F_q) = 0`, so `d_v` IS `inv_v` and "unramified" collapses to "split"; over a number field likewise at every finite place, so the residue only carries new information when `kappa(v)` is big -- i.e. in the geometric setting. Geometrically one residue per codimension-one point (normality suffices to define it, regularity for exactness), Bloch-Ogus/Gersten `0 -> Br(X) -> Br(k(X)) -> (+)_x H^1(kappa(x),Q/Z)`, the ramification divisor, and the observation that **`n`-divisible divisors kill every tame symbol** -- which is why descent functions automatically give unramified algebras (`kummer-survey.typ`) and the two facts are one fact. Faddeev's sequence with its `sum cor` reciprocity, and the two transfer compatibilities `d_w . res = e(w/v) res . d_v` and `d_v . cor = sum_{w|v} cor . d_w` -- the second is "cor preserves unramified", used by `ec-density-bm.typ` §5. **Section 10 answers the place-vs-divisor question honestly**: the two are the same construction, and for `Spec O_K` the index sets literally coincide (minus the archimedean places, which have invariants but no residues), but over a function field the family of prime divisors depends on the MODEL -- blowing up makes new ones -- so `Br(X)` is model-dependent while `Br_nr(K/k) = intersection over all divisorial valuations` is not; they agree for `X` smooth proper, which is exactly why `Br(X)` is a birational invariant and the Artin-Mumford rationality obstruction. **Section 12 is six applications**: (a) Tsen and `cd(K(t)) <= cd(K)+1` -- the residue sequence as a devissage, and `Br(k(t)) = 0` for `k` algebraically closed falls straight out of Faddeev since every residue field is `k`; (b) Faddeev BACKWARDS as a construction machine -- `A = prod_i (c_i, t - alpha_i)` realises prescribed residues `c_j` at `alpha_j` and has residue `prod c_i` at infinity, so on `A^1` any prescription works and on `P^1` exactly those with square product, which is why an affine curve has a big Brauer group and why integral BM has content; (c) residues as the COMPUTABLE invariant -- distinguishing two symbol algebras, and the finite test for "is this class constant?", a question otherwise about every specialisation at once; (d) `Br(Z_p) = 0` (itself a one-line residue argument), hence good reduction kills the local invariant, hence the Brauer-Manin sum is finite -- the fact the density notes use silently; (e) **Iskovskikh's surface `y^2+z^2 = (3-x^2)(x^2-2)` worked end to end**: `A = (-1, 3-x^2)` is unramified because on `X` the product `(3-x^2)(x^2-2)` is a norm from `Q(i)`, so `A = (-1,x^2-2)` too and the two expressions cannot ramify along a common divisor (`x^2=3` and `x^2=2` are disjoint) -- then `inv_v = 0` at every place except `p=2`, where local solubility forces `v_2(x) >= 2` and hence `3-x^2 = 3 mod 4`, so `sum_v inv_v = 1/2` always and `X(A_Q)^Br` is empty while `X(A_Q)` is not; (f) `Br_nr` as a rationality obstruction, Artin-Mumford's conic bundle and Bogomolov's multiplier for Noether's problem. Wild case (`p = char kappa(v)`) flagged, not developed -- see `wild-symbols.typ`. Worked micro-examples: `(a,t)` over `k(t)` ramified exactly on `(0)+(inf)` with residue `a` at both, and `(t,t-1)` with residue `-1` at `0` and `inf` and nothing at `t=1` (Steinberg in disguise). 14 references, with §-level pointers: Gille-Szamuely Ch. 6 first, then Colliot-Thelene-Skorobogatov Ch. 3 and Colliot-Thelene's Santa Barbara notes for `Br_nr`. Eight checks in `residues.gp`. |
| `cubic-residues.typ` | **when is 3 a cube mod p** (a question of Rene's) -- short note, and the criterion `kummer-example-j0.typ` 6.6.2 actually runs on. For `p=3` and `p=2 mod 3`: always, cubing being a bijection. For `p = 1 mod 3`: write `4p = L^2 + 27M^2` (possible, and unique once `L = 1 mod 3`, `M > 0`), then **3 is a cube iff `3 | M`**. **No congruence can decide it**, because the condition says `p` splits completely in `Q(zeta_3, 3^(1/3))`, which is `S_3` over `Q` hence non-abelian, and congruence-defined sets of primes are exactly those coming from ABELIAN extensions. Witnesses: 7 and 61 are congruent mod 27 and disagree; 31 and 193 mod 81; 13 and 499 mod 243; 139 and 1597 mod 729. The pair 7, 61 is the one to keep -- they have the SAME `L`, `(1,1)` against `(1,3)`, so only `M` separates them, which is exactly what a congruence throws away. Equivalent form version: `p = x^2 + xy + 61y^2`, the principal form of discriminant `-243`, via Cox's theory of `x^2+ny^2` -- `Q(zeta_3,3^(1/3))` is the ring class field of the order of conductor 9 in `Q(sqrt(-3))`, `h(-243) = 3`. **A trap worth knowing**: Gauss's criterion for 2 is `p = a^2+27b^2`, so one expects `p = x^2+243y^2` for 3, but that is SUFFICIENT NOT NECESSARY -- it is the principal form of disc `-972` (conductor 18, `h = 9`), catching only 271 of the 833 primes below 50000. Seen as one case of a pattern: `q = 2,3,5,7` is a cube iff `2|M`, `3|M`, `5|LM`, `7|LM` respectively. Density `1/6` of all primes by Chebotarev (so infinitely many), but **infinitude alone is far cheaper** -- a Euclid-style argument on prime divisors of `f(m)` plus the fact that all residue degrees agree in a Galois extension, with no analysis at all. Five checks in `cubic-residues.gp`. |
| `cubic-residues.gp` | **the five checks of `cubic-residues.typ`**, all counts zero. (1) `4p = L^2+27M^2` has exactly one solution with `M>0` for all 4784 primes `p = 1 mod 3` below `10^5` -- never none, never two. (2) the four criteria `2|M`, `3|M`, `5|LM`, `7|LM` on the 2556 such primes below 50000, no mismatch for any of 2, 3, 5, 7. (3) `p = x^2+xy+61y^2 <=> 3` a cube, no mismatch; and `p = x^2+243y^2` implies it with no counterexample but catches only 271 of 833. Class numbers `h(-243)=3`, `h(-972)=9`, `h(-108)=3`. (4) the colliding pairs at moduli 9, 27, 81, 243, 729 that rule out congruence criteria. (5) the density to `10^7`: 0.15663, 0.15566, 0.16496, 0.16617, 0.16656 against `1/6 = 0.16667`. Also prints the `(L,M)` table for `p < 130`. |
| `integral-points.typ` | **finding integral and S-integral points on elliptic curves** (a question of Rene's) -- short expository note with the p-adic logarithm as its spine. Two facts collide: the canonical height is a QUADRATIC form, `h^(sum n_i P_i) = Q(n)`, and `h(x(P)) = h^(P) + O(1)`, so an integral point with coefficients of size `N` has `|x| ~ e^(cN^2)`; and "enormous" means "close to O", which the logarithm of the group law turns into a LINEAR FORM IN LOGARITHMS being small, which Baker's theory forbids. Siegel (1929) stated with both proofs and the reason each is ineffective (Roth; unit equations). **The point of the note is that this happens in every topology at once**: over R the logarithm is `psi(P) = int_x^oo dt/2y ~ x^(-1/2)` and "small" means small in absolute value (David's lower bound); over `Q_p` it is the formal-group logarithm and "small" means DIVISIBLE BY A LARGE POWER OF `p`. The bridge is the dictionary `v_p(log_E P) = v_p(z(P)) = -v_p(x(P))/2` for `p` odd -- three lines from `kn - v_p(k) > n` -- so that "`x` has `p^2m` in its denominator" and "`log_p P = 0 mod p^m`" are THE SAME STATEMENT. **S-integral points are where this is not optional**: `h^(P) = log+|x|_oo + sum_{p in S} (-v_p x)+ log p` has `|S|+1` terms, so one dominates -- either `x` is archimedean-huge (use `psi`) or some `p in S` carries the size in the DENOMINATOR, where the archimedean argument sees nothing. Also: where denominators come from (`n P_0` first acquires `p` exactly when `n` hits the ORDER of `P_0` mod `p`, then `v_p(den) = 2(1 + v_p(n/n_0))`), the S-integral tables, and why LLL (de Weger) is what actually finishes -- `v_p` of the linear form `>= m` is a sublattice of index `p^(m-1)`, thinning candidates by a factor `p` per power. Running example is the `37a1` model `y^2 = x^3-16x+16` of `analytic-local.typ` 8 with `P_0 = (0,4)`: twelve integral points, all `n P_0` with `|n| <= 6`. Seven checks in `integral-points.gp`. |
| `integral-points.gp` | **the seven checks of `integral-points.typ`**, all counts zero. (1) `h^(nP_0) = n^2 h^(P_0)` to 30 digits, `n <= 30`. (2) the twelve integral points with `|x| <= 10^6`, each identified as `n P_0`, max `|n| = 6`. (3) **the dictionary** `v_p(log_E P) = -v_p(x(P))/2` on 214 instances -- five curves, every odd good-reduction prime dividing a denominator, multiples to 12 -- no exception. (4) first `n` with `p | den` equals the order of `P_0` in `E~(F_p)` on eleven primes (note `p=23`: order 11, NOT the group order 22), and the growth law `v_p(den) = 2(1+v_p(n/n_0))` for every `n <= 60` divisible by `n_0`. (5) the S-integral tables: `S={}` gives `n <= 6`, and 3, 5, 7, 2 admit `n = 7, 8, 9, 10` respectively -- exactly their `n_0`. (6) `|psi(P)| = x^(-1/2)(1+O(1/x))` to five figures. (7) rank 2 on `389a1`: `v_5(n_1 l_1 + n_2 l_2) >= m` cuts `Z^2` to a sublattice of index `5^(m-1)` -- counts 124, 24, 6, 2 against predictions 124.8, 25.0, 5.0, 1.0. |
| `residues.gp` | **the five checks of `residues.typ`**, all counts zero, and each one re-run against deliberately corrupted symbols (sign dropped, exponents transposed) to confirm it is not passing vacuously. (1) Over `Q_p`, `p` odd: the residue `(-1)^(ab) u^b w^(-a)` is a square in `F_p` exactly when `(x,y)_p = 1` -- 43200 pairs over twelve primes, which is document §6 ("over a local field the residue IS the invariant") made concrete, the sign `(-1)^(ab)` accounting exactly for the `(-1/p)^(ab)` in the classical Hilbert-symbol formula. (2) Steinberg: every residue of `(f, 1-f)` over `Q(t)` is trivial in `kappa(P)^*/squares`, at every closed point AND at infinity -- the cancellation at infinity is where the sign earns its keep. (3) Faddeev reciprocity `prod_P N_{kappa(P)/Q}(d_P A) in (Q^*)^2` on 120 classes `(f,g)` of degree up to 3. (4) The corestriction compatibility `d_P . cor = sum_{w|P} cor . d_w`, tested on the degree-2 cover `u = t^2` of the t-line (ramified at 0 and oo): with `A = (t-a, g(u))` the projection formula gives `cor(A) = (a^2-u, g)`, and the two sides agree at 330 rational points and at infinity -- this is the identity behind "cor preserves unramified" used by `ec-density-bm.typ` §5. (5) `(f^2, g)` is unramified everywhere, the mechanism by which 2-divisible divisors make descent algebras Azumaya. (6) **Iskovskikh**: for each of twelve places, the `x = a/b` (`|a|,b <= 40`, which realises every 2-adic valuation from -5 to 5 and every unit class mod 32) admitting a local point are sampled and `inv_v(-1,3-x^2)` recorded -- the achievable set is a SINGLE value at every place, `1/2` at `p=2` and `0` elsewhere, with local points everywhere, so the sum is `1/2` for every adelic point: obstruction confirmed. The sampling also reproduces both non-obvious steps of the hand proof (`v_2(x) >= 2`, unit part of `3-x^2` congruent to 3 mod 4). Evidence not proof -- the proof is document §12.5. (7) Faddeev backwards: 648 residues correct over 216 triples of targets, and unramified-at-infinity iff the product of targets is a square, both directions. (8) unramified <=> constant: of 64 classes `(f,c)` over `Q(t)` twisted by `(-1,-1)`, the residue test calls 12 unramified and those are exactly the 12 whose specialisations at `t0 = -12..12` all have the same ramification set in `Br(Q)`; the other 52 all vary. Checks (1)-(5) were each re-run against corrupted symbols and each then fails; (7) and (8) are self-discriminating, reporting both a positive and a negative population. Also prints the residue tables of `(3,t)` and `(t,t-1)`. |
| `sha-zhao.typ` | **Zhao's method, and why it does not reach a single prime** (third note on Ш at Fermat primes). Explains Zhao's induction on the number of prime divisors, quotes Nomoto's theorem for exactly our family, and shows the bound is vacuous here because `p` splits in `Z[i]` (`r(D) = 2`). Eliminates three further candidate invariants at 2. Concludes that the Fermat pattern is extremal in the one direction the machinery does not control: all known bounds grow with the number of prime factors, and ours is pinned at the minimum while the valuation grows. |
| `sha-tunnell.typ` | **looking for a Tunnell-style formula** for `y^2 = x^3+px` (sequel to `sha-fermat.typ`). Background on the Shimura correspondence, Waldspurger's theorem and Tunnell's congruent-number theorem -- why central values of twisted `L`-functions are SQUARES of half-integral-weight coefficients, and why that meets the Cassels pairing's demand that `#Sha` be a square. Explains why Waldspurger does not literally apply here (the family is a QUARTIC twist family, governed by the Hecke character `psi-bar chi_d` with `chi_d` quartic), then pins the parity of `m` to Gauss's quartic residue criterion and refutes the octic continuation. |
| `sha-bsdsum.typ` | **the exact evaluation** (fourth note on Ш at Fermat primes). Computes the BSD finite sum exactly and reduces the whole pattern to ONE identity: for `p = 2^(2^k)+1`, the quartic-character sum over the `p`-torsion of `y^2 = x^3-x` equals `2 sqrt2 * 4^(k-2) * p^(3/4)`. Explains why the sum is intrinsically a SQUARE (so `#Sha = (2m)^2` and the Cassels pairing agree because they are the same fact), and identifies Stickelberger's theorem -- whose answer is a base-`p` digit sum -- as the tool for the remaining 2-adic step. |
| `sha-fermat.typ` | **why Sha of `y^2 = x^3 + px` is `(Z/2^(k-1))^2` at Fermat primes** (a question of Rene's). Separates the pattern into the SHAPE, which is a theorem needing no BSD -- for any prime `p = 1 mod 8` with `L(1) =/= 0`, Coates-Wiles plus Rubin plus Silverman X.6 force `dim Sha[2] = 2`, and Cassels' alternating pairing then forces `Sha[2^oo] = (Z/2^e)^2` -- and the SIZE, which reduces to the single statement `L(E_p,1)/Omega = 2^(2k-3)`. |
| `selmer-involution.typ` | **PROOF that the 2-Selmer ranks of `y^2=x^3+a` and `y^2=x^3-1/a` agree for odd `a`** (MathOverflow 227987, 2015). The curves are non-isogenous but `theta -> -1/theta` is an isomorphism of the étale algebras, so they share their mod-2 Galois module and both Selmer groups are cut out of the SAME `H^1(Q,M)` -- which is why the ranks are correlated at all. Lemma C: the two descent maps agree on 2-torsion, since `theta_i theta_j = theta_i^2 zeta` and `zeta` has odd order. The local conditions then coincide at every place: `H^1(R,M) = 0`; `H^1_ur` at good places; at `v = 3` and at odd `v | a` by valuation parity (`f'` even, root differences odd, `N(1-zeta) = 3`); and at `v = 2` by a finite EXHAUSTIVE check over the four classes of `a mod 8`. |
| `selmer-local-conditions.typ` | **expository: what local Selmer conditions are FOR** (companion to `selmer-involution.typ`). Seven applications with their flagship theorems -- descent is finite/computable (Bhargava-Shankar, mean `#Sel_2 = 3`); the Greenberg-Wiles comparison formula and its four faces (Cassels' isogeny formula, the parity congruence, the change-one-place duality sequence, class-group reflection); bounding Sha by moving one local condition (Kolyvagin, Rubin, Kato, Skinner-Urban); parity as a product of local terms (p-parity theorem, Dokchitser regulator constants, Kramer); Selmer STATISTICS as random maximal isotropic subspaces (Poonen-Rains, Klagsbrun-Mazur-Rubin's Markov model -- which is the frame the even-`a` +-1 table belongs in, and A. Smith's density-zero congruent numbers); local conditions as DEFINITIONS (Bloch-Kato, `R = T`, level-raising); and Diophantine payoffs (Mazur-Rubin's Hilbert's tenth problem). Section 9 places the involution theorem: it is a 2-Selmer COMPANION result in the sense of Mazur-Rubin (arXiv:1203.0620), their Theorem 3.1 does NOT reach `y^2 = x^3+a` (`j = 0` is potentially good everywhere, so hypothesis (iv) fails at 2 and (i) would need `E[4]`), and the gap is at exactly the place where our proof does all its work. Two consequences worth having: the theorem says `E_a` and `E_{-1/a}` are 2-Selmer companions along every quadratic character UNRAMIFIED AT 2 (since `E_a^(d) = E_{ad^3}`), and Yu (arXiv:1610.01195) proves the converse -- bounded difference across the twist family FORCES `E[2] ~ A[2]`, so Theorem A was the only possible explanation. |
| `descent-s3.typ` | **2-descent when `E[2]` is generic** (`Gal(Q(E[2])/Q) = S_3`). Sets up `E[2]` as the sum-zero submodule of the permutation module on the roots, so `H^1(Q,E[2]) = ker(N : K*/(K*)^2 -> Q*/(Q*)^2)` for the cubic field `K = Q[x]/f` (Shapiro), and the descent map is `x - theta`. Full descent as a four-step algorithm: `Sel_2 <= K(S,2) ∩ ker N`; `dim K(S,2) = (r1+r2+#S_f) + dim Cl_S(K)[2]`; the local conditions, whose dimensions are read off Lemma B' of `selmer-involution.typ` purely from how `f` splits over `Q_v`; and `dim Sel_2 = rank + dim Sha[2]` since `E(Q)[2] = 0`. TWO readings of "partial 2-descent", with opposite answers: as ISOGENY descent it does not exist over `Q` at all (no Galois-stable line in an irreducible `E[2]`), and the minimal field where it does is the CUBIC field `K` itself -- not the quadratic resolvent, over which the group is still `C_3`, irreducible on `F_2^2`; the feed-back to `Q` is `rank E(K) = rank E(Q) + m_std` from `Ind_{<tau>}^{S_3} 1 = 1 + std`, so the partial descent answers a weaker question at comparable cost. As DISCARDING local conditions it is cheap and is what one does first -- the norm condition alone removed 3 of 6 dimensions in every example. Notes that the whole `y^2 = x^3+a` family is generic, since `disc = -27a^2` is never a square. |
| `descent-s3.gp` | **the worked 2-descents of `descent-s3.typ`**. For `37a1`, `571a1`, `389a1` (all `S_3`, trivial torsion, no rational isogeny) reports: the cubic `f` and its Galois type; `K`'s discriminant, signature, class group and units; `S`, the primes of `K` above it, and `dim K(S,2)`; the norm map to `Q(S,2)` as an explicit `F_2` matrix, with its kernel -- the bound BEFORE any local condition; the local dims `dim L_v`; and `ellrank`'s answer. Local conditions cut 3 to 1 (`37a1`), 3 to 2 (`389a1`), 3 to 2 (`571a1`, where all of `Sel_2` is `Sha[2] = (Z/2)^2` and the descent cannot see the rank 0). Also does `37a1` by hand: the generator `(0,0)` has `delta(P) = -theta`, of square norm 16 and not a square in `K`, so `Sel_2 = <-theta>`. NB when `Cl_S[2] != 0` the S-unit generators do not span `K(S,2)`, so the bound is reported as an inequality. Output: `results/descent-s3.txt`. |
| `corestriction.typ` | **what the corestriction map is, and the one computation the descent notes use it for**. Written because `cor` now appears in six notes (`descent-s3`, `kummer-example-j0`, `ec-density-bm`, `integral-bm`, `csa-brauer`, `kummer-survey`) with no single place explaining it. The construction in two steps: for `H <= G` of finite index, `Ind_H^G N = Map_H(G,N)` with `(g0.phi)(g) = phi(g g0)`, Shapiro `sh = (ev_1)_* . res : H^i(G, Ind N) -> H^i(H,N)`, then the TRACE `pi(phi) = sum_{Hg} g^{-1} phi(g)`, and `cor := pi_* . sh^{-1}`. Why it is a norm: in degree 0 a `G`-invariant element of `Ind N` is the constant function at some `x` in `N^H`, so `cor(x) = sum_{Hg} g^{-1} x` -- literally `N_{K/k}` on `H^0(K, k-bar^*) = K^*`. Degrees 1 and 2 follow because `cor` commutes with connecting maps: on `H^1(K,mu_n) = K^*/(K^*)^n` it is again the norm (the fact `integral-bm.typ` uses to check a residue descends), and on `H^2 = Br` it is the map with `inv_k . cor = inv_K`. Four properties, three of them formal: **(P1)** `cor . res = [G:H]`; **(P2)** projection formula `cor(x u res y) = cor(x) u y`; **(P3)** Shapiro is multiplicative for the coordinatewise product on `Ind`, proved on cochains in four lines (the only subtlety is that `phi(h) = h.phi(1)` absorbs the cup-product twist); **(P4)** `inv . cor = inv`, which is local class field theory and the one genuine input. Warns what `cor` is NOT: it does not shrink an algebra -- the corestriction of a quaternion algebra over a cubic field is a degree-8 algebra, Brauer-equivalent to a quaternion algebra over Q but only via its ramification set, which is why the descent pairing in the irreducible case has no constant-slots form. **Section 4 is the payoff**: with `A` etale over `k`, `Omega = Hom_k(A,k^s)` and `M = F_2[Omega]`, the permutation form `<e_theta,e_theta'> = delta` IS `sigma(xy)` for the coordinatewise product and the trace `sigma` -- i.e. the trace form of `A` -- and those are exactly the induced product and the trace `pi`. So the cup-product pairing on `H^1(k,M) = A*/(A*)^2` is `sigma_*(mult_*(u)) = cor_{A/k}(alpha,beta)_A`, of invariant `sum_w inv_{A_w}(alpha_w,beta_w)`: **the local Tate pairing is a product of Hilbert symbols over the places of `A`, and the sum over `w` is nothing but the sum over cosets in the definition of the trace**. Two corollaries: both slots rational gives `(a,b)_v^{dim A}` (that is (P1) made visible), and one slot rational gives `(N_{A/k} alpha, b)_k` by (P2) -- the only case where the corestriction disappears. References: Brown III.9-10, NSW I.4-5 and VII, Serre `Local Fields` VII/XI and `Galois Cohomology` I.2.4, Gille-Szamuely 3.9/4.2, Poonen-Stoll for the descent application. |
| `corestriction.gp` | **the checks for `corestriction.typ`**, on `A = Q[y]/(y^3-16y+16)` (the cubic field of `37a1`) and `B = Q x Q(i)`. All counts zero. (1) **(P1)**: `prod_{w|v} (a,b)_{A_w} = (a,b)_v^3` for rational `a,b`, 900 symbols -- and since 3 is odd and the target 2-torsion, this also says `cor . res = id` on `Br(Q)[2]` for an odd-degree extension. (2) **(P2)**: `<alpha,b>_v = (N_{A/Q} alpha, b)_v` for rational `b`, 450 symbols over `A` and 640 over `B`. (3) **reciprocity**: `prod_v prod_{w|v} (alpha_w,beta_w)_w = 1` on all 100 pairs of test classes -- Hilbert reciprocity for `A` regrouped by the places of Q, which is the check that the local invariants add up the way (P4) says. (4) **degree 1 is the norm, RECOVERED not assumed**: for each test class solve for the square class `c` with `(c,b)_v = prod_w (alpha_w,b)_w` for all test `b` and all `v`; the solution is unique in every case and equals `N_{A/Q}(alpha)` mod squares, so the norm is forced by the pairing rather than put in by hand. Output: `results/corestriction.txt`. |
| `analytic-local.typ` | **PROOF that regular functions are `v`-adically analytic**: for `X/K` a variety, `P` a SMOOTH `K`-rational point, `x_1..x_n` local coordinates and `f` in the stalk `O_{X,P}`, the formal expansion of `f` converges on a `v`-adic neighbourhood of `P` in `X(K_v)` and computes `f` there. Uniform in the place -- archimedean and non-archimedean are not distinguished, since only completeness of `K_v` is used, so the proof works verbatim over any complete valued field. The engine is ONE fixed-point lemma (an implicit function theorem stated for an arbitrary commutative Banach `K_v`-algebra, proved by a Banach contraction with explicit constants), applied TWICE: in the Banach algebra of convergent power series, where it produces the expansion and its convergence, and in `K_v` itself, where it produces the neighbourhood; functoriality of the solution under norm-decreasing homomorphisms glues formal to analytic. Also: formal uniqueness (so the convergent solution automatically has coefficients in `K`); the local presentation of a smooth point (`U = V(g_1..g_r)` near `P` with invertible Jacobian block, by a height-zero-prime argument); change of local coordinates via the inverse function theorem. Complements: the expansion map is an injective local homomorphism, so an identity checkable on a `p`-adic neighbourhood is global; `X^sm(K_v)` is a `K_v`-analytic manifold; the INTEGRAL refinement for almost all `v` (coefficients in `O_v`, the whole residue disc, by the same contraction run `(x)`-adically); and what fails at a singular point. Worked example: the formal group of an elliptic curve -- `w = z^3 + azw^2 + bw^3` is literally the lemma's hypothesis -- which is the statement `selmer-involution.typ` assumes when it writes `E(K) = Z_p^[K:Q_p] x T`. |
| `analytic-local.gp` | **the worked example of `analytic-local.typ` §8**. For `y^2 = x^3-16x+16` (37a1) at `P = O` with `z = -x/y`, `w = -1/y`: checks the exact relation `w = z^3 + azw^2 + bw^3` on a rational point; prints the expansion `w(z) = z^3 - 16z^7 + 16z^9 + 512z^11 - ...`, integral as Proposition 7.2 predicts; and verifies CONVERGENCE at `v = 5` on the point `8*P0` of the formal group (`v_5(z) = 1`), where truncating at order `N` gives error valuation 14, 23, 33, 43 for `N` = 10, 20, 30, 40 -- linear growth in `N`, which is what convergence at a point of valuation 1 predicts. Output: `results/analytic-local.txt`. |
| `hensel-different.typ` | **an INTRINSIC form of the multivariate Hensel lemma** (a question of Rene's). The coordinate-free invariant behind `e = v(det J(a))` is the KAEHLER DIFFERENT `d_{X/O} = Fitt_0(Omega_{X/O})`, the zeroth Fitting ideal of the sheaf of relative differentials -- an ideal sheaf on `X`, generated by `det(dF_i/dx_j)` in any square presentation (Stacks 0BVZ), cutting out the locus where `X -> Spec O` is NOT UNRAMIFIED (Stacks 0BVY), and equal to the classical different for quasi-finite syntomic morphisms. Along a section it evaluates to `(pi^e)` with `e = length_O(a* Omega_{X/O})`, so Hensel reads: *an approximate point lifts as soon as it solves the equations to precision better than the SQUARE of the different*, and the lift is pinned to precision `d`. Why `Omega` must be the answer: lifting points is a square-zero deformation problem, obstructed by the cotangent complex and with lifts a torsor under `Hom(x*Omega, I)`; etale means `Omega = 0` means unique lifting, which IS classical Hensel, and `2e+1` is the fixed point of the Newton recursion `N -> 2N - e`. Three readings of `e`: total order of contact between the branch through `a` and the other branches (`e = sum_{beta != a} v(a-beta)`); the different exponent of a monogenic extension; and -- the payoff -- **why square classes need `a` mod 8 at `v = 2` and only mod `p` at odd `v`**, since for `x^2 - u` the different is `(2x) = (2)` and `e = v(2)`, so `2e+1` is 3 at 2 and 1 elsewhere. Cubes are easy at 2 for the same reason (`d = (3x^2)`, `e = v(3) = 0`). Beyond the square case: `Fitt_d(Omega)` in relative dimension `d`, Elkik's `H_{B/A}` (minors weighted by ideal quotients `(F_alpha : I)`) for non-lci, and Greenberg when `e = infinity`. Ends with a coordinate/intrinsic dictionary. |
| `hensel-different.gp` | **the four concrete checks of `hensel-different.typ`**. (1) Square classes: for `x^2-u` the different is `(2)`, so the threshold `2e+1` is 1 at odd `p` and 3 at `p=2`; sweeping units confirms "square mod `p^{2e+1}`" decides "square in `Z_p`" with 0 disagreements. (2) SHARPNESS: one step short, at `p^{2e} = 4`, there are 4 disagreements, the first at `u = 5` -- a square mod 4, not a square in `Z_2`. (3) The different: `N(f'(theta))` equals the norm of the different of `O_K` for `x^2-2`, `x^2+1`, `x^3-2`, and FAILS for `x^2-5` (20 vs 5) because `Z[sqrt5]` is not maximal -- the instructive case, since `Fitt_0(Omega)` is an invariant of the algebra presented, not of its normalisation. (4) Presentation independence: adding a redundant variable and the equation eliminating it changes the Jacobian matrix but not its determinant. Output: `results/hensel-different.txt`. |
| `csa-brauer.typ` | **central simple algebras and the Brauer group in number theory** -- an EXPLICIT expository tour (cohomological readings set off in grey boxes and skippable). Quaternion algebras `(a,b)` and cyclic algebras `(L/K,sigma,b)`, with the one engine result: `(L/K,sigma,b)` splits iff `b` is a norm, so `K*/N(L*) = Br(L/K)` -- a norm question becomes an algebra question. Local: every CSA is split by an UNRAMIFIED extension, `inv_v(K_n/K_v, Frob, pi^r) = r/n`, so the invariant is a valuation over a degree and `Br(K_v) = Q/Z`; local class field theory falls out by counting invariants. Global: the fundamental sequence `0 -> Br(K) -> (+)_v Br(K_v) -> Q/Z -> 0`, whose two halves are Albert-Brauer-Hasse-Noether (split locally everywhere => split) and reciprocity (`sum inv_v = 0`). Shows QUADRATIC RECIPROCITY is the degree-2 case: the three local formulas `(p,q)_p = (q/p)`, `(p,q)_q = (p/q)`, `(p,q)_2 = (-1)^{eps(p)eps(q)}` make the product formula into Gauss, with both supplements. Applications: Legendre/Hasse-Minkowski for conics; the Hasse norm theorem for cyclic extensions (one-line proof) and its FAILURE for `Q(sqrt13,sqrt17)`, where every decomposition group has order <= 2 so every square is a local norm, yet 25 is not a global norm -- with a full PROOF (Prop 5.1): writing `k = Q(sqrt13)`, `L = k(sqrt17)` and `y = N_{L/k}(x)`, the inertness of 5 in `k` plus `N(fundamental unit) = -1` force `y = +-5` times a square, and then `(17,+-5)_w = (5/17) = -1` at the places above 17 (which SPLIT in `k`) kills both -- using only the easy direction of local-global. The contrast `(2/17) = +1` is exactly why 4 IS a norm. Section 5.2 also writes out the NORM FORM `N = (a^2+13b^2-17c^2-221d^2)^2 - 52(ab-17cd)^2` (three equivalent shapes, one per quadratic subfield, the sign patterns being the character table of `(Z/2)^2`), so the counterexample becomes an explicit quartic form representing 25 everywhere locally and nowhere rationally; and then exhibits an AZUMAYA ALGEBRA registering that failure: `V : N(x) = 25` is a torsor under the norm-one torus, so by Sansuc the obstruction must be Brauer-Manin, and it is carried by the single quaternion class `A = (17, A-5)` pulled back from the conic `A^2-13B^2 = 25` (unramified since `div(A-5) = 2*(5,0)`). At places NON-split in `Q(sqrt13)` its invariant is independent of the local point (17 becomes a square in `Q_v(sqrt13)`), at SPLIT places local solubility forces invariant 0, so the sum is `inv_5(17,-5) = 1/2` -- the obstruction is complete and sits entirely at `v = 5`, while Prop 5.1's contradiction appeared at 17; classification of division algebras by local invariants and index = exponent = lcm of denominators; Grunwald-Wang and Wang's counterexample; and the Brauer-Manin obstruction, which is the SAME product formula applied fibrewise. **Section 6 is on CROSSED PRODUCTS**: by Skolem-Noether plus the double centraliser theorem, any CSA with a Galois maximal subfield ALREADY is one, `A = (+)_sigma L u_sigma` with `u_sigma u_tau = c(sigma,tau) u_{sigma tau}`; associativity of that multiplication table IS the 2-cocycle identity (computed both bracketings explicitly), rescaling `u_sigma` is changing `c` by a coboundary, `c = 1` gives `End_K(L) = M_n(K)`, and so `H^2(G,L^*) = Br(L/K)` -- the cohomological description of section 1 DERIVED from a multiplication table, and cohomologically the connecting map of `1 -> L^* -> GL_n -> PGL_n -> 1` (Galois descent, injective by Hilbert 90). The cyclic factor set `c(sigma^i,sigma^j) = b^{[i+j>=n]}` recovers section 1.3 and PROVES Prop 1.2; Hamilton is `(C/R, conj, -1)`. Buys: exponent | degree via `cor . res = n`. Limits: degree 2, 3 always cyclic (Wedderburn), degree 4 always a crossed product (Albert) but not always cyclic, degree p >= 5 open, and NON-crossed products exist in degree divisible by `p^3` (Amitsur 1972) -- but over a number field ABHN in strong form says every CSA is CYCLIC, so the frame collapses to its cyclic case and the explicit approach of the earlier sections is not a simplification but complete. **Section 7 gives two fully worked examples over Q.** Degree 2: Hamilton's `(-1,-1)`, with its multiplication table, presented as the crossed product `(Q(i)/Q, sigma, -1)` with `u_sigma = j`; ramified at 2 and oo. Since a pure quaternion has `v^2 = -(x^2+y^2+z^2)`, `Q(sqrt(-m))` embeds iff `m` is a sum of three rational squares -- so the Brauer criterion (2 and oo non-split, i.e. `m != 7 mod 8`) and the THREE-SQUARE THEOREM are the same statement; six explicit generators `i`, `i+j`, `i+j+k`, `j+2k`, `i+j+2k`, `j+3k` give six non-isomorphic quadratic fields, and `Q(sqrt(-7))` is the first exclusion. The Hurwitz order and Lagrange's four-square theorem noted alongside. Degree 3: `A = (Q(zeta_7)^+/Q, sigma, 2)` with `f = x^3+x^2-2x-1`, the EXPLICIT automorphism `sigma(alpha) = alpha^2-2`, basis `1,a,a^2,u,au,a^2u,u^2,au^2,a^2u^2` and rules `u^3 = 2`, `u*alpha = (alpha^2-2)u`; ramified at 2 and 7 with invariants 1/3 and 2/3. Bonus: `(L/Q,sigma,b)` is a division algebra iff `b` is NOT a cube mod 7, and the two invariants vanish together because `p` inert in `L` and `p` a cube mod 7 are the same condition. Its cubic subfields are the cubic fields where 2 and 7 are both non-split: `Q(zeta_7)^+`, `Q(zeta_9)^+`, `Q(cbrt2)`, `Q(cbrt14)` all embed (distinct discriminants, two cyclic and two NOT -- so one division algebra holds both Galois and non-Galois maximal subfields), while `Q(cbrt5)` does not since 2 splits there. Generators visible inside `A`: `alpha`, `u`, and `(2-alpha)u` whose cube is `N(2-alpha)*2 = 14`. **Section 8 answers two questions about the subfields of the degree-3 algebra `A`**, both from the one criterion: since `inv_2 = 1/3` and `inv_7 = 2/3` both have order 3, a cubic `L` splits `A` iff every completion above 2 and above 7 has local degree 3, i.e. iff **2 and 7 are each NON-SPLIT in `L`** (one prime above each); and `deg A = 3` is prime, so a subfield is `Q` or a cubic maximal subfield. (i) WHICH `Q(cos(2pi/m))` OCCUR: `[Q(zeta_m)^+ : Q] = phi(m)/2`, so a cubic needs `phi(m) = 6`, which has exactly four solutions `m = 7, 9, 14, 18` (no more, since `phi(m) >= sqrt(m/2)` bounds `m <= 72`) -- and `Q(zeta_14) = Q(zeta_7)`, `Q(zeta_18) = Q(zeta_9)`, so **the two fields already named exhaust them**; both do embed (2 and 7 inert in each). Degenerately `Q(cos(2pi/m)) = Q` for `m in {1,2,3,4,6}`. (ii) NON-REAL SUBFIELDS: **yes, and two are already in section 7's own list** -- `Q(cbrt 2)` (disc -108) and `Q(cbrt 14)` (disc -5292) each have signature (1,1). The reason nothing forbids them: **`Br(R)` has exponent 2 while `A` has order 3, so `inv_oo(A) = 0` NECESSARILY** -- an algebra of odd degree cannot ramify at a real place -- and the criterion at infinity reads `[L_w : R] * 0 = 0`, vacuous. Only 2 and 7 constrain. The contrast with Hamilton's quaternions in section 7.1 is exact and is the likely source of the opposite intuition: there `inv_oo = 1/2` is non-zero, every quadratic subfield must be non-split at infinity, and that is exactly why they are all IMAGINARY (the three-square theorem). Empirically the non-real subfields are the more common: over a box of 2877 distinct cubic fields, 421 embed -- 187 totally real, 234 with a complex place -- and the smallest maximal subfield of `A` of either kind is `Q[x]/(x^3+x-1)` of discriminant **-31**, non-real and smaller than the totally real record holder `Q(zeta_7)^+` of discriminant 49. Ends with an explicit/cohomological dictionary and a map of which survey computation is which. |
| `csa-brauer.gp` | **eight computations for `csa-brauer.typ`**. (1) Hilbert reciprocity `prod_v (a,b)_v = 1` on a sample. (2) Quadratic reciprocity read off from it: checks `(p,q)_p = (q/p)`, `(p,q)_q = (p/q)`, `(p,q)_2 = (-1)^{eps eps}` and Gauss's law on 8 pairs, 0 discrepancies. (3) Conics `ax^2+by^2=z^2`: "splits everywhere" agrees with "rational point found" on all six test cases, including `(-1,-1)` (Hamilton, no point) and `(2,7)` (point `(1,1,3)`). (4) The Hasse norm principle for `Q(sqrt13,sqrt17)`: verifies `kronecker(17,13) = kronecker(13,17) = 1` and that the disc is odd, so every decomposition group has order <= 2 and every square is a local norm; then `bnfisnorm` shows 25 is NOT a global norm while 4, 9, -25 are. (5) Grunwald-Wang: `x^8-16` factors into four quadratics, so 16 is an 8th power in `Q_p` iff one of 2,-2,-1 is a square; the three Legendre symbols multiply to +1 so this holds for every odd `p`, fails at 2, and 16 is not an 8th power in `Q`. (6) Division algebras built to order with PARI's `alginit` from prescribed local invariants: index = lcm of denominators every time, including degree 6 with invariants 1/2,1/2 giving index 2 (`M_3(D)`). (7) Crossed products: the cyclic factor set satisfies the cocycle identity (1295 triples for `n = 2..8`, 0 failures); `(Q(sqrt5)/Q,sigma,b)` has index 1 exactly when `b` is a norm (10 values, 0 mismatches); `b` and `-b` give the same algebra since `N(2+sqrt5) = -1` is a coboundary; and the class of `b = 3` squares to that of `9 = N(3)`, so it has exponent 2. (8) The two worked examples: for Hamilton, "m is a sum of three squares" agrees with "2 non-split in Q(sqrt(-m))" on every squarefree `m <= 31` (0 disagreements, first exclusion `m = 7`), with an explicit pure-quaternion witness printed for each; for `A = (Q(zeta_7)^+/Q, sigma, 2)`, PARI's `alginit` confirms degree 3, index 3 and invariants 1/3 at 2 and 2/3 at 7, the `b`-sweep confirms "split iff `b` is a cube mod 7" (0 disagreements), and the decomposition of 2 and 7 is tabulated for five cubic fields. (9) The Azumaya class `(17, A-5)`: `(17,-5)` ramifies exactly at {5,17} with 5 inert and 17 split in `Q(sqrt13)`; 93 values of `m` against every non-split place up to 40 give 2139 symbol comparisons with 0 deviations from `(17,-5)`, confirming the invariant is point-independent there; the ramification set moves with `m` ({5,17}, {3,5}, {5,29}) but always contains 5. Output: `results/csa-brauer.txt`. |
| `integral-bm.typ` | **a Brauer-Manin obstruction to INTEGRAL points**, worked out end to end on the negative Pell conic `X : x^2 - 34y^2 = -1` over `Z` -- the companion to `csa-brauer.typ` §5.2, but for `X(Z)` rather than `X(Q)`. The point of the example is that it is about integrality and NOTHING else: `X(Q)` is Zariski dense (`(3/5,1/5)` is a point, so `X = P^1_Q` minus one closed point of degree 2 with residue field `Q(sqrt34)`), and `X(Z_v) != 0` at every place, yet `X(Z) = 0` because the fundamental unit `35+6sqrt34` has norm `+1`. The class is a single quaternion algebra `A = (2, x+4)`. Why `x+4`: for a constant `a` the tame symbol is `d_Q(a,f) = a^{v_Q(f)}`, so `(2, x+c)` is unramified on `X` iff `2` is a square in the residue field `Q(sqrt(34(c^2+1)))` of the zeros of `x+c`, i.e. iff `17(c^2+1)` is a perfect square -- and `17*(4^2+1) = 289 = 17^2`, giving residue field exactly `Q(sqrt2)`. At the deleted point at infinity the residue is `2` in `K*/K*^2`, non-trivial, so `A` is non-constant. The evaluation is three lines: `inv_oo = 0` always (a symbol `(a,b)_oo` needs BOTH entries negative and `2 > 0`); `inv_p = 0` at odd `p` (`(2,x+4)_p = (2/p)^{v_p(x+4)}`, and `p | x+4` forces `2y^2 = 1 mod p`, so `2` is a square there anyway); `inv_2 = 1/2` ALWAYS, because `(x^2+1)/2 = 17y^2 = 1 mod 8` forces `x = +-1 mod 8`, hence `x+4 = +-3 mod 8`. Sum `= 1/2 != 0`: the obstruction, sitting entirely at the prime 2. **The instructive half is section 5**, how the rational points escape: reciprocity forces `sum_v inv_v = 0`, so every rational point must fail to be `p`-integral at some odd `p = +-3 mod 8` -- verified on all points of denominator `<= 45` (each has bad set `{2,p}` with `p in {3,5,11,29,37}`, all `= +-3 mod 8`, all dividing the denominator) and along the whole orbit of `(3/5,1/5)` under the fundamental unit, where the denominator 5 never goes away. So the class LITERALLY MEASURES DENOMINATORS, which is why it can obstruct integral points while the rational Hasse principle holds. Section 6 identifies what it is: the residue is the genus character of `K = Q(sqrt34)` cutting out the genus field `Q(sqrt2,sqrt17)`, `h(K) = 2` -- this Brauer-Manin obstruction IS the classical genus-theory obstruction to negative Pell, and by Colliot-Thelene-Xu (strong approximation for homogeneous spaces of connected groups) it is the ONLY obstruction, so the computation is the reason and not just a symptom. Section 7 runs the recipe over `d = 2q`, `q = 1 mod 8` prime, and gets 7 for 7. **A closing section on RELATIVE DIMENSION ZERO** asks whether any of this survives when the generic fibre is a finite set of points, and answers it with a dichotomy. If `X = Spec B` with `B` a FINITE `Z`-algebra then `X(Z_p) = X(Q_p)` for every `p` (every element of `B` is integral over `Z_p`, which is integrally closed -- equivalently the valuative criterion, a finite morphism being proper), so `X(A_Z) = X(A_Q)` and no obstruction called integral can be new: a class sees only denominators, and a finite `Z`-scheme has none. Non-properness is therefore the hypothesis, not a convenience. The rational theory in dimension 0 is the theory of finite `G_Q`-sets: local points almost everywhere says every cyclic subgroup of `Gal(L/Q)` is conjugate into some stabiliser, Jordan's theorem (a proper subgroup never covers a group by conjugates) settles the CONNECTED case, and the first failure is in degree 5 -- TATE'S EXAMPLE, recorded in Harari-Voloch (MPCPS 2010, Remark 3.1), the roots of `x^3-x-1` together with those of `x^2+23`. That example is killed here by ONE class: `2` is inert in the cubic field and splits in `Q(sqrt-23)`, so taking `B = (0, B_2)` with `B_2` the quaternion algebra over `Q(sqrt-23)` ramified at exactly the two primes above 2 makes `sum_v inv_v = 1/2` at every adelic point -- the whole obstruction at one prime, as on the conic; and both branches being monic, it is an integral obstruction with no integral content. What is left is the NON-MONIC case, where integrality selects among the degree-one places above `v`, and there the smallest possible example is genuine: `X = Spec Z[x]/((2x-1)(3x-1))` has `X(Z) = 0`, `X(Q) = {1/2, 1/3}` and `X(Z_v) != 0` everywhere, and the NON-DIAGONAL class `((-1,-1), (-1,-3))` in `Br(Q) + Br(Q) = Br(X)` -- ramified at `{2,oo}` and `{3,oo}` -- gives `sum_v inv_v = 1/2` on all of `X(A_Z)` while the rational point `x = 1/2` gives 0, so `X(A_Z)^Br = 0` but `X(A_Q)^Br != 0`. The forced places 2 and 3 contribute nothing; the invariant is carried by the real place, where nothing is forced. |
| `integral-bm.gp` | **the ten checks of `integral-bm.typ`**. (1) `quadunit(136) = 35+6w` has norm `+1`, and no `y <= 2*10^5` solves negative Pell. (2) `X(Z_v) != 0` everywhere: `33 = 1 mod 8` gives the 2-adic point `(sqrt33, 1)`, the gradient `(2x,-68y)` never vanishes on `X` so `X` is smooth over `Z[1/2]` and Hensel lifts, and no odd `p < 2000` fails mod `p`. (3) The residue computation: `34*(4^2+1) = 578 = 2*17^2` has squarefree part 2, so the zeros of `x+4` have residue field `Q(sqrt2)` (residue trivial), while `2` is not a square in `Q(sqrt34)` (residue at infinity non-trivial). (4)-(6) The three invariants, with (5) EXHAUSTIVE over `Z/2^10`: 2048 solutions, `x mod 8 in {1,7}` for all of them, `hilbert(2,x+4,2) = -1` for all of them; and (6) comparing "`p | x+4` is realisable" against "`2` is a QR mod `p`" for every odd `p < 5000` -- they agree at every prime, 0 exceptions. (8) The rational-point table (denominator `<= 60`), each row's bad set of even size (Hilbert reciprocity) and equal to `{2,p}`; plus the check that the two representatives `(2,x+4)` and `(2,x-4)` agree at every place, as they must since `(2,17)` splits and `2y^2-1 = -N(1+y sqrt2)`. (9) `bnfinit(x^2-34)` gives class group `Z/2`. (10) The family scan: for `d = 2q` with `q = 1 mod 8` prime up to 200, find `c` with `q(c^2+1)` square, then compute `inv_2` over `Z/2^10` -- constant `1/2` for `d = 34, 146, 178, 194` (negative Pell insoluble) and constant `0` for `d = 82, 226, 274` (soluble). Seven for seven, and `d = 34` is the smallest. Output: `results/integral-bm.txt`. (11) The relative-dimension-zero checks: `polrootspadic` shows every `Q_p`-root of the monic `x^3-x-1` has valuation `>= 0` while `2x-1` has the `Q_2`-root `1/2` of valuation `-1`; Tate's example is locally soluble at every `p < 50000` and at 23 and `oo`, `2` is inert in `Q[x]/(x^3-x-1)` and split in `Q(sqrt-23)`, and `alginit` builds the quaternion algebra over `Q(sqrt-23)` ramified at exactly the two primes above 2; and for `(2x-1)(3x-1)` the achievable invariant sums are computed by exhausting the branch choices place by place -- `{1/2}` over `X(A_Z)` (obstruction) against `{0, 1/2}` over `X(A_Q)` (none). |
| `ec-padic-closure.typ` | **elliptic curves E/Q and primes p for which E(Q) is NOT dense in E(Q_p)** -- six worked examples, one at each layer of `E(Q_p) > E_0 > E_1 > E_2`. Framing: for `p >= 3`, `E(Q_p) = Z_p x (finite)`, so the closure `Gamma` of `E(Q)` is a closed subgroup, and it is OPEN exactly when the rank is `>= 1` (if the rank is 0 then `E(Q)` is finite and the image is nowhere dense at every prime, for trivial reasons -- hence every example here has rank exactly 1 and trivial torsion). Density is therefore the vanishing of a computable finite index, and for rank 1 with generator `P` it is `(N_p/m) p^{v-1}`, where `N_p = |E(Q_p)/E_1|`, `m` = order of `Pbar` there, and `v = v_p(c(mP))` with `x(Q) = a/c^2` -- so the formal-group depth is read straight off the DENOMINATOR of a rational point. **The correction the document is built around**: for good `p`, surjectivity of `E(Q) -> E(F_p)` is NECESSARY for density but NOT SUFFICIENT, because it controls only the top two layers and says nothing about `E_1/E_2 = F_p`; the right criterion is surjectivity of `E(Q) -> E(Q_p)/E_2(Q_p)`, i.e. **mod p^2, not mod p** (proved in §1.2: if `Gamma ∩ E_1 ⊆ E_2` the image in `E/E_2` has order `<= N_p < p N_p`). The witness is the curve of conductor 89, `y^2+xy+y = x^3+x^2-x`, `P = (0,0)`: at `p = 11`, `E(F_11) = Z/14` and `Pbar` generates it -- the reduction map IS onto -- yet `14P` has `c = 35695 = 5 * 11^2 * 59`, so `14P` lies in `E_2(Q_11)` and the index is 11; the same curve does it again at `p = 13` (`c = 2366 = 2*7*13^2`, index 13). Good-reduction examples of the ordinary kind: conductor 37 (`y^2+y = x^3-x`, `P = (0,0)`) at `p = 23`, where `E(F_23) = Z/22` but `Pbar` has order 11, index 2; and at `p = 67`, where `E(F_67) = Z/30 x Z/2` is NON-CYCLIC, so a rank-1 trivial-torsion curve cannot possibly surject -- a structural obstruction, occurring for 113 of the 667 good `p < 5000` (16.9%). Bad-reduction examples, one per layer: conductor 43 at `p = 43` (non-split multiplicative `I_1`, so `E_0/E_1` is the norm-one torus of `F_{43^2}/F_43`, cyclic of order `p+1 = 44`, and `Pbar` generates only the index-2 subgroup); conductor 77 at `p = 11` (`Pbar` generates ALL of `E(Q_11)/E_1` of order 12, but `12P` has `c = 968 = 2^3 * 11^2`, index 11 -- the bad twin of the conductor-89 example); Section 3.4 CORRECTS an earlier claim in this file that the component mechanism is rare: that rested on a listing truncated to 28 rows and on sweeping only odd `p`. A census of 156450 curves in reduced minimal form (`a1,a3 in {0,1}`, `a2 in {-1,0,1}`, `|a4|<=40`, `|a6|<=80`, `p<=200` including `p=2`) gives **5249 confirmed (E,p) with certified rank >= 1 and PROPER image in `Phi(F_p)`**, against 60022 pairs with `c_p>=2` (denominator includes rank-0 curves, so the true rate is above the 8.7% ratio) -- the mechanism is ordinary, not exceptional. 3060 of the 5249 sit at `p = 2`, which the earlier sweeps skipped; `p = 2` is legitimate here since the component argument needs only that `E_0(Q_p)` is open AND closed, so unlike the formal-group layer it carries no `p = 2` caveat. Smallest conductors found: 438 (`y^2+xy = x^3+x^2-65x-231`, `p=2`, `I_2`, `c=2`, image trivial) and 446 (`y^2+xy+y = x^3+x^2-39x-35`, `p=2`, SPLIT `I_14`, so `Phi(F_2) = Z/14`, rank 1, trivial torsion, generator `P = (-5,10)`): `kP` avoids the singular point `(1,0)` exactly for `k = 7, 14`, so the image has order 7 and **E(Q) lands in the even part of Z/14 and misses all seven odd components of the Neron model**. Also worked: conductor 11289, `y^2+xy = x^3+x^2-9` at `p = 3`, the COMPONENT layer, with `c_3 = 2` and the generator `(2,1)` reducing to a non-singular point while the singular point is `(0,0)`, so all of `E(Q)` sits in `E_0(Q_3)` and the closure misses the non-identity component of the Neron model outright (total index 6 = 2 x 3, an odd-p instance of the same thing). Section 5: for conductor 37, `E(Q) -> E(F_p)` fails to be onto for 357 of 667 good `p < 5000` (53.5%) -- non-density is the TYPICAL behaviour, not a curiosity; the complementary 46.5% is an observed value of the elliptic Artin primitive-root density (Lang-Trotter), not a theorem. Caveat recorded: `p = 2` is excluded throughout since `E_1(Q_2)` need not be `Z_2`. |
| `ec-padic-closure.gp` | **the eleven checks of `ec-padic-closure.typ`**. Utilities: `cden(Q) = sqrtint(denominator(x(Q)))`, which equals `p^{v_p(z)}` up to prime-to-p factors so membership in `E_n(Q_p)` is just `p^n | c`; `Nquot(E,p) = p+1-a_p` (good) or `c_p (p - a_p)` (bad, covering split mult/non-split mult/additive uniformly); `singpt(E,p)` finding the singular point of a bad reduction by brute force over `F_p^2` from the three partials; and `closure(E,P,p)` returning `[N_p, m, v, index]`. (2)-(3) conductor 37 at `p = 23` and `p = 67`, with the index tabulated for every good `p < 150` and the non-cyclic `p < 400` listed. (4) conductor 89 at `p = 11` and `13`, printing `14P` and `12P` in full with their factored denominators, plus a table of all good `p < 150` showing which primes are non-dense and whether the reduction was onto. (5)-(7) the three bad-reduction curves, each printing the Kodaira code, `c_p`, `a_p`, the singular point, whether `P in E_0`, and the full index chain. (8) The clean criterion verified on all nine (curve, p) pairs: `|E(Q_p)/E_2|` against the order of the image, agreeing with the index computation every time. (9) The sweep over 667 good `p < 5000` for conductor 37. (10) The rank-0 degenerate case on the conductor-11 curve. (11) The component-layer census, with conductors 438 and 446 worked in full. A note recorded from debugging this file: GP's lax comparison makes `[0,0] == 0` return 1, which silently broke a "no singular point" sentinel and produced a batch of false `P in E_0` hits -- use `#S == 0` instead. Output: `results/ec-padic-closure.txt`. |
| `ec-density-bm.typ` | **is the non-density of `ec-padic-closure.typ` a Brauer-Manin obstruction? Yes -- and BM and "Tate pairing + reciprocity" are the SAME argument.** On an elliptic curve `Br_1(E)/Br_0(E) = H^1(Q, Pic E-bar) = H^1(Q,E)`, and the invariant of the class attached to `beta` at a local point IS the local Tate duality pairing `<Q_v, beta_v>_v : E(Q_v) x H^1(Q_v,E) -> Q/Z`; the BM condition `sum_v inv_v = 0` is literally Poitou-Tate/Cassels global duality. Manin's theorem settles it in principle: if `Sha(E)` is finite then `E(A_Q)^Br = closure of E(Q)`, so EVERY failure of density is a BM obstruction and nothing else is -- and both curves treated here have analytic rank 1 (`ellanalyticrank`), so Gross-Zagier + Kolyvagin make that unconditional for them. The content is exhibiting the class, and a 2-isogeny makes it explicit: on `E : y^2 = x(x^2+ax+b)` with `T = (0,0)`, the descent map `Q -> x(Q)` into `Q_v*/(Q_v*)^2` pairs with the dual class `d` by the HILBERT SYMBOL, `<Q,d>_v = (1/2)(d, x(Q))_v`, carried by the quaternion algebra `A_d = (d,x)` -- which is unramified on `E` because `div(x) = 2(T) - 2(O)` has even multiplicities (same trick as the `(2,x+4)` class of `integral-bm.typ`). Two places come free from the equation: if `a^2-4b < 0` then `x^2+ax+b > 0` so `x >= 0` at every real point and `inv_oo = 0` for EVERY `d`; and at odd `v` of good reduction every point has `v_v(x)` even (good reduction forces `v` not dividing `b`), so the local image is all unit classes and the symbol of two units is trivial -- leaving only `v = 2`, the odd bad primes, and `v | d` to check. **Case 1 (good reduction)**: `y^2 = x^3-2x^2+2x`, conductor 128, rank 1, `E(Q) = <(1,1)> + <(0,0)>`, `p = 7`, `d = 7`. `inv_v A = 0` at every `v != 7` (real place by positivity, odd `v` by unit classes, `v=2` checked on 61 representatives of `S_2`), so Hilbert reciprocity forces `inv_7 = 0` at every rational point; but `x = 3` gives `x(x^2-2x+2) = 15`, a square in `Q_7`, with `(7,3)_7 = -1`. Since `Q -> (7,x(Q))_7` is a surjective homomorphism to `{+-1}`, its kernel has index exactly 2 -- matching the filtration index, so the class explains the WHOLE failure. **Case 2 (bad reduction)**: `y^2 = x^3-4x^2+9x`, conductor 960, `p = 3`, type `I_4` non-split with `c_3 = 2`, `d = 3`; the singular point mod 3 IS the 2-torsion point `(0,0)`, so the component-group image is full and the failure sits in the torus layer; `x = 2` gives 10, a square in `Q_3`, with `(3,2)_3 = -1`. Nothing in the argument cares whether `p` is good or bad -- the Hilbert symbol does not see Kodaira types. **Section 4, the limit**: a class of order 2 cuts index at most 2, so the explicit quaternion story reaches exactly the index-2 failures (conductor 37 at `p=23`, conductor 43 at `p=43`) and NOT conductor 37 at `p=59` (index 4) or conductor 89 at `p=11` (index 11, the formal layer `E_1/E_2`, where the reduction map is surjective). Manin still covers those, but with classes of order 4 and 11 that are not quaternion algebras. The moral: `<Q,d>_v = (1/2)(d,x(Q))_v` is exactly the local pairing of 2-descent, so the classes one can write down by hand are precisely those 2-descent already sees; deeper layers need deeper descent. **Section 5 kills the natural misreading** that an index-2 failure requires a 2-isogeny over Q (equivalently a rational 2-torsion point, i.e. a rational root of the cubic). What is needed is a class of order 2 in `Br(E)`, i.e. in `H^1(Q,E[2])`, and FULL 2-descent exists for every curve; the isogeny only puts the descent map into `Q*/(Q*)^2` so that the class is a single quaternion algebra with CONSTANT entries. With `E[2]` irreducible one works over the cubic etale algebra `L = Q[t]/(f)`: descent map `Q -> X(Q)-theta` into `L*/(L*)^2`, classes `H^1(Q,E[2]) = ker(N : L*/sq -> Q*/sq)`, pairing `<Q,d>_v = (1/2) prod_{w|v} (X(Q)-theta, d)_w` over places of `L`, and the Brauer class the CORESTRICTION `cor_{L(E)/Q(E)}(d, X-theta)` -- still unramified (`div(X-theta) = 2(T_theta)-2(O)` is even and corestriction preserves that), still order 2, just not a quaternion algebra over `Q(E)` with constant slots. The counterexample is the notes' own first curve: conductor 37 in short form `Y^2 = X^3-16X+16` has `t^3-16t+16` irreducible (so `E[2]` irreducible, `S_3`) and `ellisomat` isogeny class size 1 -- NO rational isogeny of any degree -- yet index 2 at `p = 23`. The class: `L = Q(theta)`, `theta^3 = 16theta-16` (totally real, disc 148, h = 1), `d = theta^2/4 - 2theta - 11` with `N(d) = 529 = 23^2` (square norm is exactly the `H^1(Q,E[2])` condition) and `(d)` the single degree-2 prime above 23, so `d` is a unit away from 23. Pairing is +1 at `oo, 2, 37` on every local point and +1 at all other `v` since `v_w(X(Q)-theta)` is even there; reciprocity over `L` forces +1 at 23 on rational points (verified on `nP`, `n <= 8`, `P = (0,4)`); but `X = -1` gives `31 = 8 mod 23`, a residue, so `E(Q_23)` has a point there with pairing -1.. **Section 6** answers why index 4 and index 11 are harder, given that every curve has n-descent: the framework is uniform in `n` and Manin guarantees the classes, so it is purely a matter of explicitness, in three stages. (i) THE ORDER IS FORCED: both local quotients are CYCLIC -- `E(Q_59)/Gamma = Z/4` (conductor 37: `E(F_59) = Z/52` cyclic, `ord(Pbar) = 13`, `13P` in `E_1 \ E_2`) and `E(Q_11)/Gamma = E_1/E_2 = Z/11` (conductor 89) -- and a family of order-2 classes cuts a quotient of EXPONENT 2, so it can never give `Z/4`. But 2-descent does get PART of it: `Pbar` has odd order 13 so lies in `2E(F_59)`, and a quaternion class cuts a factor 2 of the 4; the last factor 2 needs 4-descent. (ii) THE DESCENT ALGEBRA GROWS: for `n = 2` it is the CUBIC `L = Q[t]/(f)` (which is why §5.1 fits on a page), whereas for `n = 11` on conductor 89 the 11-division polynomial has degree 60 and is IRREDUCIBLE over Q, with `[Q(E[11]):Q]` bounded by `|GL_2(F_11)| = 13200`; plus a degree-`n` cyclic algebra needs `mu_n`, which is in Q only for `n = 2`. (iii) WILDNESS AT THE DETECTION PLACE, the real wall: the `n`-th norm residue symbol at `v` is tame iff `v` does not divide `n`, so index 4 at `p = 59` is TAME where the class must be non-trivial (wild only at `v = 2`, where one needs mere vanishing and the wildness is the classical mod-8 kind), while index 11 at `p = 11` has `n = p` -- wild exactly where the work is. **And that is structural**: `E_1(Q_p) = Z_p` is pro-`p`, so every finite quotient of the formal group is a `p`-group and cutting `E_1/E_2 = Z/p` ALWAYS needs an order-`p` class at the place `p`, whereas `E_0/E_1` has order `p+1-a_p` (or `p -+ 1`), prime to `p` away from anomalous primes. So the filtration of `ec-padic-closure.typ` is also a TAME/WILD filtration -- the top layers tame and reachable, the formal-group layer wild. Tooling: `nfhilbert` is literally "does `X^2-aY^2-bZ^2 = 0` have a solution", quadratic only, as is Sage's `hilbert_symbol`; `descent-s3.typ` already hit this exact wall needing a CUBIC norm-residue symbol at the wild place `v = 3`, and index 11 is the same gap one prime up.. **Section 7** answers whether BM is then just the n-descents repackaged: for an elliptic curve essentially YES, and the identification is EXACT -- `Br(E-bar) = 0` by Tsen (a curve over an algebraically closed field has no Brauer group), so `Br(E) = Br_1(E)` and `Br(E)/Br_0(E) = H^1(Q,E) = union_n H^1(Q,E)[n]` with `H^1(Q,E)[n] = coker(E(Q)/n -> H^1(Q,E[n]))` exactly the output of n-descent; nothing is left over and no obstruction escapes descent. (For fixed `(E,p)` one `n` suffices, the exponent of the finite group `E(Q_p)/Gamma`; across all `p` you need `n -> oo` along `p`-powers, since the formal group is `Z_p`.) Three qualifications keep it from being ONLY a repackaging. (1) It is a DUALITY, not a restatement: descent cuts down `H^1(Q,E[n])` by the local conditions `W_v` and bounds the GROUP, BM cuts down `prod_v E(Q_v)` by classes and bounds the CLOSURE. The sharpening this yields: `A_beta` vanishes at every `v != p` iff `beta_v` is in `W_v^perp = W_v` there and detects at `p` iff `beta_p` is not in `W_p`, so **the classes that obstruct density at `p` are exactly `Sel_n^(p)/Sel_n`**, the Selmer group relaxed at `p` modulo the honest one -- and the `d` of §5.1 is a generator of that quotient for `n = 2`, `p = 23` on conductor 37 (`dim Sel_2 = 1` from rank 1, no rational 2-torsion, `Sha[2] = 0`; `dim Sel_2^(23) >= 2`). Whether such a class exists is a Greenberg-Wiles computation, not a formality -- cf. `selmer-local-conditions.typ`. (2) That the descents see EVERYTHING is a THEOREM: `closure(E(Q))` inside the BM set is formal (reciprocity + continuity), but the reverse inclusion is Manin's theorem and needs Sha finite, via Poitou-Tate and Cassels-Tate; unconditional here only because these curves have analytic rank 1. (3) The equivalence is SPECIAL to `E`: off an abelian variety there is no group law, no isogeny, no n-descent, but still a Brauer group and still `sum_v inv_v = 0` -- the `(2,x+4)` class on the negative Pell conic of `integral-bm.typ` is not an n-descent class of anything. The packaging buys PORTABILITY; on `E` it buys nothing new. |
| `ec-density-bm.gp` | **the seven checks of `ec-density-bm.typ`**. `issqQp(z,p)` (p-adic square test by valuation parity plus Legendre/mod-8 on the unit part); `Svreps(a,b,v,...)` collecting rational representatives of the local descent image `S_v = x(E(Q_v))` in `Q_v*/(Q_v*)^2` by scanning `x = +-n v^e` and testing whether `x(x^2+ax+b)` is a `v`-adic square (rationals are dense and the condition is open, so a wide enough scan finds every class); `Nquot`, `idxE1` and `singpt` reused from `ec-padic-closure.gp` to cross-check the filtration index. (2) and (3) run the full argument on conductors 128 (`p=7`, `d=7`) and 960 (`p=3`, `d=3`): rank and analytic rank, generators, local data at `p`, the filtration index, the four place-by-place vanishing statements, a table of `inv_oo`/`inv_2`/`inv_p` and the reciprocity product on `nG` and `nG+T` for `n <= 8` (all invariants 0, all products 1), and a search for the smallest positive integer `x` giving a point of `E(Q_p)` with invariant 1/2. (4) tabulates the total closure index `(N_p/m) p^{v-1}` for four (curve, p) pairs from `ec-padic-closure.typ` against the order of Brauer class needed. (5) The no-isogeny counterexample on conductor 37 at `p = 23`, using `bnfinit` on the cubic field and `nfhilbert` at each prime above `v` (real places done by hand from the three real embeddings). Two notes from debugging: an inner `f(x) = ...` closure inside a GP function body made the function return the closure instead of its last value (inline the polynomial instead), and GP rejects braces nested inside a `{...}` block, so helper functions must sit at top level. (6) The local quotient structures (both cyclic), the per-`n` visibility table at `p = 59`, the irreducible degree-60 11-division polynomial, and the tame/wild table. (7) The Selmer dimensions behind the `Sel_n^(p)/Sel_n` statement. Output: `results/ec-density-bm.txt`. |
| `wild-symbols.typ` | **why WILD norm residue symbols are hard to evaluate, and tame ones are not** -- written after the index-11 wall of `ec-density-bm.typ` §6. For a local field `K` containing `mu_n`, the `n`-th symbol `( , )_n` is TAME when `v` does not divide `n` and WILD when it does, and four things break at once. **Break 1, units stop being invisible**: for `v` not dividing `n`, `U^(1)` is a pro-`p` group and `n` is prime to `p`, so `U^(1)` is uniquely `n`-divisible, sits inside `(K*)^n` and dies -- verified on 556656 pairs of units at all odd `p < 200`, every symbol trivial. At `v = 2` the four unit classes pair non-trivially (`(3,3)_2 = -1`: two principal units), and the pairing has DEPTH: the symbol is non-trivial on `U^(i) x U^(j)` exactly when `i+j <= pe/(p-1) = 2`, i.e. only for `(1,1)`. **Break 2, precision**: at odd `p`, changing `a` or `b` by `p` never changes the symbol (checked all odd `p < 100`, 0 violations) -- the tame symbol reads its arguments mod `p`. At `v = 2` it does not: 1 and 5 agree mod 4 but `(1,2)_2 = +1` and `(5,2)_2 = -1`, so mod 4 fails and mod 8 works. The general law is `U^(m) ⊆ (K*)^n exactly when m > pe/(p-1)`, `e = v_K(p)`. The document's centrepiece is the odd-`p` verification nobody has memorised: **CUBES in `K = Q_3(zeta_3)`** (`O = Z[omega]`, `pi = omega-1`, `pi^2 = -3omega`, `e = 2`, so the prediction is `m >= 4`), brute-forced over `O/pi^12 = O/3^6` (531441 elements, past the Hensel bound `2e+1 = 5`): `U^(m)` consists of cubes for `m = 4,5,6` and not for `m = 0,1,2,3`, and at the last failing level `m = 3` exactly one third of the 19683 units are cubes -- one layer of filtration left. **This is the same constant as `hensel-different.typ`**: that document derives Hensel's `2e+1` from the Kaehler different (3 at `v=2`, 1 at odd `v`); here the same `e` gives the SHARP bound `pe/(p-1)`, which agrees at 3 for `Q_2` but gives 4 where Hensel gives 5 for cubes at 3. Wild ramification is a statement about the different, and precision is what it costs. **Break 3, the group explodes**: `|K*/(K*)^n| = n^2/|n|_K` -- 4 classes over odd `Q_p` versus 8 over `Q_2`; and for `n = p` over `Q_p(zeta_p)` (`e = p-1`) it is `p^(p+1)` against the tame `p^2`, so for `p = 11` a formula must be a function on 3138428376721 classes rather than 121. **Break 4, acquiring `mu_n` makes it worse**: the symbol is not defined until `mu_n` is in `K`, and for odd `p` that forces `Q_p(zeta_p)/Q_p`, TOTALLY RAMIFIED of degree `p-1`, pushing `e` from 1 to `p-1` and the depth to `p`. For `n = 2` this costs nothing (`mu_2 = {+-1}` is already in `Q_2`), which is exactly why the quadratic symbol at 2 is the ONE wild symbol with a closed form and the only one in PARI or Sage. Section 6 sketches what the real formulas look like -- Artin-Hasse (1928) for `Q_p(zeta_{p^n})` in the shape `zeta^{Tr(... log u)/p^n}`, a `p`-adic logarithm and a trace rather than a residue; the general explicit reciprocity laws of Iwasawa, Wiles, Coleman, Vostokov and Brueckner, which are power-series formulas (Coleman series, residues of differentials); and Lubin-Tate, which makes the local Artin map explicit via formal groups, so "computable in principle" is fair but it is a formal-group computation, not a finite-field one. The honest summary of "hard": no shortcut through the residue field, hence `p`-adic analysis to depth `pe/(p-1)`, in a field you had to ramify by `p-1` to enter, on a class group of size `p^(p+1)` -- every one of those four numbers being 1 or trivial in the tame case.. **Section 7 is LUBIN-TATE, and it explains where `pe/(p-1)` comes from.** Lubin-Tate theory builds the maximal totally ramified abelian extension from a formal group with `[pi](x) = pi x + ... + x^q` and makes the local Artin map explicit (uniformiser acts trivially on `K(F_pi[pi^n])` and by Frobenius on `K^ur`; a unit `u` acts by `[u^{-1}]`), so the symbol IS a formal-group computation. For `K = Q_p`, `pi = p` the Lubin-Tate group is the multiplicative one and `K_pi = Q_p(mu_{p^oo})`, so Lubin-Tate recovers the cyclotomic picture and Artin-Hasse rather than replacing it; Wiles' explicit reciprocity law is the Lubin-Tate generalisation and Coleman series are the tool. The payoff is a one-line derivation of the depth: on `Ghat_m`, `[p](x) = (1+x)^p - 1`, so with `j = v(x)` the two extreme terms give `v(px) = e+j` against `v(x^p) = pj`, and the crossover is at `j = e/(p-1)`. **Below it `[p]` behaves like FROBENIUS** (valuation multiplied by `p`), **above it like MULTIPLICATION BY `p`** (valuation shifted by `e`) -- so `[p]` carries `m^j` onto `m^(j+e)` once `j > e/(p-1)`, whence `U^(m) ⊆ (K*)^p` for `m > pe/(p-1)`. Tabulated, this reproduces every depth measured by brute force in §3: `m >= 3` for `Q_2`, `m >= 4` for `Q_3(zeta_3)`, and predicts `m >= 6` for `Q_5(zeta_5)`, `m >= 12` for `Q_11(zeta_11)`. The crossover is exactly where `log_F` converges and becomes an isomorphism, which is why the symbol turns analytic there and why every explicit reciprocity law is a formula in logarithms. In the tame case there is no crossover: `n` is prime to `p`, `[n]` is invertible on the formal group, the filtration is `n`-divisible -- **tame means the Lubin-Tate group is invisible**. |
| `wild-symbols.gp` | **the eight checks of `wild-symbols.typ`**. (1) 556656 unit pairs at odd `p < 200`, zero non-trivial symbols, plus the 4x4 table of unit classes at `v = 2`. (2) Precision: mod-`p` invariance at odd `p` (0 violations) against the explicit mod-4 failure `(1,2)_2 = +1` vs `(5,2)_2 = -1` and mod-8 sufficiency over 10^4 odd pairs. (3) The depth of the unit filtration in three settings: squares in `Q_p` for odd `p` (answer `m = 1`, since `U^(1)` is already `n`-divisible -- the `pe/(p-1)` formula does not apply in the tame case), squares in `Q_2` (`m >= 3`), and cubes in `Q_3(zeta_3)` by brute force over `O/3^6` with the arithmetic done by hand as pairs `(a,b)` for `a+b*omega`, `omega^2 = -1-omega`, and `v_pi(a+b*omega) = v_3(a^2-ab+b^2)` (`m >= 4`). (4) The `n^2/|n|_K` table, tame against wild, up to `p = 13`. (5) The pairing filtration at 2, confirming non-triviality exactly on `i+j <= 2`. (6) The tame closed formula `(a,b)_p = (-1/p)^{ab}(u/p)^b(w/p)^a` checked against PARI's `hilbert` on 195792 cases, 0 disagreements. (7) The cost of acquiring `mu_p`, tabulated. (8) The Lubin-Tate valuation table for `Ghat_m` at `Q_2`, `Q_3(zeta_3)`, `Q_5(zeta_5)`, `Q_11(zeta_11)`, reproducing every measured depth from `v(px) = e+j` against `v(x^p) = pj`. Output: `results/wild-symbols.txt`. |
| `density-bridge.typ` | **how the single-curve documents sit against the twist-family work** -- the connection between `ec-padic-closure.typ` / `ec-density-bm.typ` / `wild-symbols.typ` and `kummer-padic-density.typ` + the survey. Opens with a DUPLICATION notice: `ec-padic-closure.typ` re-derives §2.1 of `kummer-padic-density.typ` -- the criterion `closure(Gamma) = E_d(Q_p)` iff `Gamma` onto `E(Q_p)/E_1` (index `M = c_p * #Etilde^ns(F_p)`) AND `Gamma ∩ E_1` not inside `E_2`, the denominator test for the second clause, and the "non-cyclic `Etilde(F_p)` forces rank >= 2" obstruction are all already there; what is new is narrow (the conductor-89 witness with SURJECTIVE reduction and index 11, the component-group census, the mod-`p^2` phrasing). Note also that the second clause is exactly the one the naive "reduction is surjective" phrasing drops -- so that earlier correction was a correction to the phrasing, not to this repo, which had it right. Reproduces the CM Tamagawa row: `d = 6` (class `[u.3]`) gives `y^2 = x^3-432`, type `IV*`, `c_3 = 3`, `M = 9`; `d = 3` gives `c_3 = 1`, `M = 3`. **The substantive difference (§2, the COLLAPSE)**: in `ec-density-bm.typ` the obstruction pairs a POINT against a CLASS, `inv_v A_beta(Q) = <Q_v, beta_v>_v`; on the Kummer surface a point is an unordered PAIR of points on one twist, so reciprocity only has `<delta_v P, delta_v Q>_v` with both entries in the local Kummer image `W_v` -- and `W_v` is MAXIMAL ISOTROPIC (`W_v^perp = W_v`, the very fact used in `ec-density-bm.typ` §7), so the pairing vanishes identically at every place and reciprocity gives `0 = 0`. **The single-curve Brauer-Manin argument is VACUOUS on X**, not merely weaker. Verified as a dimension count (`dim W_v` is exactly half `dim H^1(Q_v,E[2])` at `v = 3,5,7,11,23,37` for conductor 37). What rescues it is §5.1.5's TWISTED pairing `beta_v(P,Q) = <delta_v P, phi delta_v Q>_v` with `phi` a non-scalar `G`-endomorphism of `E[n]`, which needs **`E[n]` DECOMPOSABLE** -- otherwise `End_G(E[n]) = F_n`, `phi` is scalar and `beta` collapses back. Striking consequence: the curve of `ec-density-bm.typ` §5, chosen precisely for having NO rational isogeny at all, would be useless in the family setting -- the two settings want opposite things from `E[n]`. **§3, what `wild-symbols.typ` actually contributes**: not a new tool (`level3.gp` already built the wild cubic symbol) but the SHARPNESS of its lemma -- brute force over `O/pi^12 = O/3^6` confirms `U^(4) ⊆ (K*)^3` for `K = Q_3(zeta_3)` and shows 4 cannot be lowered to 3 (at `m = 3` exactly one third of the 19683 units are cubes), so level3.gp's mod-9 precision is exactly right. **§4 CORRECTS `ec-density-bm.typ` §6**, which called the index-11 case "blocked by an unimplemented wild symbol" -- too pessimistic, since this repo already has two ways round: (a) level3.gp's product-formula evasion needs only a single prime above `ell` in `Q(zeta_ell)` (automatic: `ell` is totally ramified) plus a depth bound for global representatives, and both hold at 11 (`h = 1`, one prime, `e = 10`, depth 11, so `U^(12)` are 11th powers and `v(11^2) = 20 >= 12` pins a class mod 121); (b) the survey's `11a1` at `p = 11` never evaluates the symbol -- the proof needs `beta_11` NON-DEGENERATE, not computed, and split multiplicative reduction settles it structurally (the Tate `mu_5` is a THIRD line distinct from both `C_1` and `C_2`, so `phi` restricts to an isomorphism and the Weil pairing of two distinct lines is perfect). So "no explicit class" and "no proof" are different things; what `wild-symbols.typ` establishes is the COST of an explicit class, not a barrier to the theorem. **§5 answers whether the failure of the survey's search for `ell > 5` is a K3 Brauer-group fact.** The literal claim -- "the geometric Brauer group of a K3 has no `ell`-torsion for `ell > 5`" -- is FALSE: `Br(Xbar) = (Q/Z)^(22-rho)` with `rho <= 20`, so it has `ell`-torsion for every `ell`. But there is an EXACT true statement next to it. Kunneth gives `Br(Abar)[ell] = Hom(E_1[ell],E_2[ell]) / (Hom(E_1,E_2) tensor F_ell)` for `A = E_1 x E_2`, so for `E_1 = E_2 = E` without CM the quotient is by the SCALARS, `Br(Abar)[ell] = End(E[ell])/F_ell`; and `Br(Kum(A)bar) = Br(Abar)` away from 2 (Skorobogatov-Zarhin). Taking Galois invariants, **`Br(Xbar)^{G_Q}[ell] = End_G(E[ell])/F_ell`** -- so the survey's criterion `End_G(E[ell]) != F_ell` is LITERALLY "the Kummer surface has a Galois-invariant transcendental Brauer class of order `ell`", and the search for the twisting `phi` and the search for `ell`-torsion are the same search (the scalars divided out being exactly the algebraic part, which is why `phi` must be non-scalar). What bounds `ell` is then a modular-curve theorem, not K3 geometry: over Q for ODD `ell` only the split-Cartan case survives, since (i) one stable line with equal characters forces `chi_1^2 = chi_cyc`, hence `ell = 2` given a rational `ell`-torsion point, and (ii) image inside a NONSPLIT Cartan is impossible over Q -- complex conjugation has `det = -1` and `c^2 = 1`, so eigenvalues `{1,-1}`, distinct for odd `ell`, while a nonsplit Cartan element has eigenvalues `alpha, alpha^ell` which are either equal or conjugate outside `F_ell`, never `{1,-1}` (so the fourth row of the triage table is EMPTY over Q). Split Cartan means a non-cuspidal rational point of `X_split(ell) = X_0(ell^2)`, and by Mazur-Kenku the cyclic isogeny degrees over Q are `N <= 19` plus `{21,25,27,37,43,67,163}`, forcing `ell^2 in {4,9,25}`, i.e. **`ell in {2,3,5}` exactly** -- `49` is not on the list. Brute force over 44741 curves finds decomposable `E[ell]` for `ell in {2,3,5}` and nothing else, and `ell = 7` fails even on the CM curve of conductor 49 (`j = -3375`, CM by `sqrt(-7)`), whose isogeny degrees `{1,2,7,14}` show a SINGLE 7-isogeny, so `E[7]` is reducible but not decomposable. Verdict: the connection is exact rather than analogical, but the bound is the vanishing of the GALOIS-INVARIANT part and that vanishing is Mazur-Kenku -- the mechanism runs out at `ell = 5` for the same reason there are no rational `ell^2`-isogenies past 25. **§6 answers whether `beta_p` varies with the twist: NO -- it depends on `d` only through its SQUARE CLASS at `p`.** If `d/d'` is a square in `Q_p` then `psi : E_d -> E_d'`, `(x,y) -> (c^2 x, c^3 y)` with `c^2 = d/d'`, is an isomorphism DEFINED OVER `Q_p`; it carries `E_d[ell]` to `E_d'[ell]`, each stable line to the corresponding one (so it commutes with `phi`), and preserves the Weil pairing -- hence identifies `W_p` with `W_p'` and `beta_p` with `beta_p'`, on the nose. So ONE CHECK PER SQUARE CLASS suffices (4 at odd `p`, 8 at 2), not one per twist. Verified on 14a1 (`E[3]` decomposable: `psi_3` has two rational roots), `ell = 3`, critical place `p = 7`: every `d` in class `[1]` (7-adic units `= 1,2,4 mod 7`, tested on 13 values including `d = 247`) gives Kodaira `I_3`, `c_7 = 3`, `a_7 = +1`, `M = 18`, while a non-square class gives `c_7 = 1`, `a_7 = -1`, `M = 8`. **What DOES move with `d`** is the vanishing away from `p`: `beta_v` can be non-zero only where `dim W_v = 2`, needing `mu_ell` in `Q_v`, so the places at risk are `{p} u {ell} u {q | d : q = 1 mod ell}` -- among the same twists `d = 37` brings in 37 and `d = 247` brings in 13 and 19, each a fresh place that could balance the reciprocity sum away from the critical place. That is the family-uniform obligation, and why the CM table lists "few bad primes necessary". **But the shortcut still fails**, for a logical reason: `beta_p != 0` is a LOCAL fact about a form on `W_p`, while "`E_d(Q)` fails to surject" is a GLOBAL fact about one twist; the implication `beta_p != 0 and beta_v = 0 (v != p) => image isotropic => dim <= 1 => no surjection` cannot be inverted, since a twist can fail to surject merely for having small rank. So testing one twist -- or the CM search's 708 -- is evidence, not proof, exactly as `kummer-padic-density.typ` presents it. The valid converse use is REFUTATION: a single twist that DOES span `W_p` (with `beta_v = 0` away from `p`) proves `beta_p` vanishes and kills the mechanism for that class. Net effect: symbol work is needed at most once per square class at the critical place -- bounded and local, not recurring across the family -- and often not even once (11a1 gets non-degeneracy structurally from the Tate curve); the genuinely `d`-dependent labour is the vanishing elsewhere, done by the dimension count `dim W_v <= 1`, which needs no symbol. Ends with a single-curve/family dictionary. |
| `density-bridge.gp` | **the six checks of `density-bridge.typ`**. (1) The CM Tamagawa row at `p = 3` recomputed with `elllocalred` for `d = 6` and `d = 3` (`IV*`, `c_3 = 3, M = 9` against `c_3 = 1, M = 3`). (2) The Lagrangian dimension count on conductor 37: for `v = 3,5,7,11,23,37`, `dim H^1(Q_v,E[2]) = 2 dim H^0` (local Euler characteristic plus self-duality of `E[2]`) while `dim W_v = dim E[2](Q_v) = dim H^0`, so `W_v` is exactly half-dimensional and self-annihilating. (3) The `U^(m)` cube filtration in `Q_3(zeta_3)` re-run for `m = 2..5` to exhibit the sharpness of level3.gp's bound. (4) `bnfinit(polcyclo(l))` for `l = 3, 11`: class number, number of primes above `l`, `e`, and the depth `pe/(p-1)`, establishing that the product-formula evasion has both its inputs at 11. (5) The `ell`-census: `ellisomat` over 44741 curves detecting two independent rational `ell`-isogenies (answer `{2,3,5}`), plus the conductor-49 CM curve showing `ell = 7` has only one 7-isogeny -- with the caveat, now recorded in the file, that `ellisomat` identifies isogeny targets up to isomorphism and so UNDERCOUNTS stable lines (14a1 has degrees {1,2,3,6} yet two stable 3-lines), so the census is a lower bound and the exact statement rests on Mazur-Kenku. (6) The twist-invariance table at `p = 7` for 14a1 across one square class and its complement, and the `d`-varying list of bad places `= 1 mod 3`. Output: `results/density-bridge.txt`. |
| `local-duality.typ` | **how local Tate duality is proved** -- the purely local fact, written after the question came up in `ec-density-bm.typ`. Statement: for `K/Q_p` finite and `M` a finite `G_K`-module with Cartier dual `M^D = Hom(M, Kbar^*)`, cup product followed by `inv` makes `H^i(K,M) x H^{2-i}(K,M^D) -> Br(K) = Q/Z` a perfect pairing of finite groups for `i = 0,1,2`; the abelian-variety companion `A(K)^ x H^1(K, Ahat) -> Q/Z` follows by a limit argument (§8). **§2 is the engine `inv : Br(K) = Q/Z`, worked out in five steps.** §2.1 explains what the unique extension of the valuation is FOR, which is easy to state and easy to skate over: a division algebra has no direct construction of a valuation, but every single element lies in a commutative subfield `K(x)`, so one defines `w(x)` as the extension of `v` to `K(x)` evaluated at `x` -- and uniqueness is used TWICE, first so that `w` is well defined at all (a given `x` lies in many subfields, and without uniqueness the value would depend on which one, and on which extension), second so that the ultrametric axiom can be checked: `w(x+y) >= min` has no non-commutative proof but reduces to `w(1+z) >= min(0,w(z))` with `1+z` inside the FIELD `K(z)`. Only then do `O_D`, `m_D` and the residue algebra `Dbar` exist. §2.2: **Wedderburn's little theorem** makes `Dbar` a FIELD, and with the degree formula `dim_K D = ef` plus `e <= n` (as `w` takes values in `(1/n)Z` via the reduced norm) and `f <= n`, the identity `ef = n^2` forces **`e = f = n`** -- the residue algebra is as big as it could be. §2.3: `Dbar/k` is generated by one element, Hensel lifts it to `alpha` with `K(alpha)/K` unramified of degree `n`, hence a maximal subfield; and a maximal subfield SPLITS, by letting `D` act on itself on the left and `L` on the right to get `D (x) L -> End_L(D) = M_n(L)`, injective by simplicity with both sides of `L`-dimension `n^2`. §2.4 computes `Br(L/K)` for `L` unramified cyclic: the crossed-product theorem (already in `csa-brauer.typ` §6) gives `H^2(G,L*)`, cyclic `G` gives `K*/N(L*)`, and then unramifiedness enters twice -- `pi` stays a uniformiser so `N(pi) = pi^n`, and units are norms because on residue fields `N` is `x -> x^{(q^f-1)/(q-1)}`, surjective onto a cyclic `F_q*`, after which one climbs the unit filtration where `N` becomes the TRACE, surjective by separability. Hence `N(L*) = {x : n | v(x)}` and `inv(x) = v(x)/n`, with a compatibility check as `n` grows. §2.5 checks the whole thing at `n = 2`, where the Hilbert symbol computes it. Then `H^2(K,mu_n) = (1/n)Z/Z` by Kummer plus Hilbert 90. **§3 is the key honest point: the theorem CONTAINS local class field theory.** Taking `M = Z/n` trivial and `i = 1` gives `Hom(G_K,Z/n) x K*/(K*)^n -> (1/n)Z/Z`, `(chi,a) -> chi(rec a)`, whose perfectness IS the norm residue isomorphism -- so there is no proof of the local fact that bypasses LCFT, they sit at the same depth. For `n = 2` this is checkable by hand and is verified exhaustively: `mu_2` is already in `Q_p`, both sides are `Q_p*/(Q_p*)^2`, the pairing is the HILBERT SYMBOL, and the Gram matrix over `F_2` has full rank with trivial radical at `p = 2,3,5,7,11,13`. §4 gives the proof architecture -- finiteness of `H^i`, the five lemma to induct on `#M`, Shapiro plus induced modules to reduce to a trivial module over a splitting field, i.e. back to §3 (Serre GC II.5, Milne ADT I.2, NSW VII.2), with the conceptual repackaging that `G_K` is a duality group of cohomological dimension 2 with dualizing module the roots of unity. **§5 is Tate's local Euler characteristic formula** `#H^0 #H^2 / #H^1 = ||#M||_K`, which upgrades isotropic to MAXIMAL isotropic: with `M = E[n]` self-dual under the Weil pairing, `#H^1 = #E[n](K)^2 |n|_K^{-2}` while `#(E(K)/n) = #E[n](K) |n|_K^{-1}`, so `#W^2 = #H^1` and `W` is LAGRANGIAN (isotropy itself from the Kummer sequence: two Kummer classes pair through the image in `H^1(K,E)`, zero on `E(K)/n`). Verified for `n = 2` at the wild place `v = 2` on five curves, where `dim W_2 = dim E[2](Q_2) + 1` against `dim H^1 = 2 dim E[2](Q_2) + 2` -- the `+1`/`+2` being exactly the `|2|_2^{-1}` correction absent at odd places. **§6 separates symmetric from alternating self-duality**, which is easy to conflate and explains a difference between two other documents: `Z/2` is self-dual SYMMETRICALLY, and over `Q_2` the Hilbert form on `F_2^3` is non-degenerate on an ODD-dimensional space so it has no Lagrangian at all (largest totally isotropic subspace has dimension 1, e.g. spanned by 2; the diagonal `(a,a)_2` is `-1` exactly on `{-1,-2,-5,-10}`), whereas `E[n]` is self-dual ALTERNATINGLY on an even-dimensional space where Lagrangians exist and `W_v` is one -- which is precisely why the untwisted pairing collapses to `0 = 0` on the Kummer surface (`density-bridge.typ` §2) while the quaternion argument of `integral-bm.typ`, pairing a point against a CLASS, has no such degeneration. §7: for `ell != p` the module is unramified and `H^1_ur(K,M)`, `H^1_ur(K,M^D)` are exact annihilators each of order `#H^0`, giving duality in the tame range with no fundamental class -- this is the "unramified isotropy" the survey uses at good places -- while at `ell = p` the module is no longer unramified (the formal group is in the way), the count fails, and one is thrown back on `inv` and LCFT in full: the same tame/wild line as `wild-symbols.typ`. |
| `local-duality.gp` | **the six checks of `local-duality.typ`**. (2) Local duality for `M = Z/2` as non-degeneracy of the Hilbert symbol: exhaustive Gram matrices over `Q_p*/(Q_p*)^2` at `p = 2,3,5,7,11,13` (8 classes at 2, 4 elsewhere), rank over `F_2` full in every case, radical trivial in every case. (3) Unramified annihilation: at odd `p` the unramified classes `{1,u}` form an isotropic subgroup of order 2 = half of 4, while at `p = 2` the unramified classes `{1,5}` are isotropic (`(5,5)_2 = 1`) but of order 2 in a group of order 8 -- NOT half, which is exactly where the soft tame proof of duality breaks. (4) The Lagrangian count for `E[2]` over `Q_2` on five curves, `dim E[2](Q_2)` read off by `factorpadic` of the 2-division polynomial, confirming `dim W_2` is half of `dim H^1` with the `|2|_2^{-1}` correction included. (5) The largest totally isotropic subspace of `Q_2*/(Q_2*)^2` under the Hilbert form, found by enumerating all subspaces of `F_2^3`: dimension 1, so no Lagrangian, as an odd-dimensional non-degenerate symmetric form requires. (6) The `n = 2` case of §2's engine: for `u` a non-residue, `L = Q_p(sqrt u)` is the unramified quadratic extension, and the Hilbert symbol shows every UNIT is a norm (`(u,w)_p = +1` for all units `w`, every odd `p < 40`) while a uniformiser is not (`(u,p)_p = -1`), so `N(L*) = {v even}`, the index is 2 and `inv(x) = v(x)/2`; plus surjectivity of `N : F_{q^f}* -> F_q*` for all primes `q <= 19`, `f <= 4`, which is what starts the unit-norm climb. Output: `results/local-duality.txt`. |
| `cert-extended.gp` | **the extended density certificate for `kummer-padic-density.typ` §3.4** -- recomputes the 45x4 witness table of §3.3 and adds, for each `(p, class, d)`, the isomorphism type of `E^d(Q_p)`, the generators of `E^d(Q)` actually needed, and their images. Conventions: `E_d : Y^2 = X^3 + d^2 X + d^3`; for `p >= 3` the formal group is `E_1 = Z_p` with `E_n <-> p^{n-1} Z_p` and `M = c_p * #Etilde^ns(F_p)`; writing `E(Q_p) = Z_p x T` with `T` the torsion, **good reduction with `p` not dividing `M`** gives `T = E(F_p)` from `ellgroup`, so the `T`-coordinate of a point is literally its reduction mod `p`, recorded as a discrete log `[e]` or `[e,f]` against PARI's generators; the **classes `[p]` and `[up]`** have `v_p(d)` odd, hence `v_p(c_4) = 2`, `v_p(Delta) = 6`, type `I_0^*`, `a_p = 0`, `M = c_p p`, with prime-to-`p` part of `T` equal to `Phi(F_p)` of order `c_p` and `p`-part ASSERTED trivial rather than computed -- a gap closed later by `cert-ptors.gp`, which see. The `Z_p`-coordinate is recorded by VALUATION only, since choosing a topological generator of `Z_p` is a choice; a point generates that factor iff the coordinate is a unit iff the formal depth is 1. Results: 158 of the 180 lines need ONE generator, 22 need two (e.g. `p = 47`: `Z_p x C_30 x C_2` and `Z_p x C_18 x C_2`; `p = 67`: `Z_p x C_28 x C_2`, `Z_p x C_40 x C_2`). **This row used to add "and those 22 are exactly the twists where `E^d(Q_p)` is not procyclic". That is false, and `cert-lprimary.gp` found it: only 21 of them are.** The exception is `p = 83`, `[u]`, `d = -11`, where `Etilde(F_83) = C_78` is CYCLIC, so the group is procyclic and a single point does generate -- just neither of the two `ellsaturation` returned. "No single generator in our basis is dense" and "no single point of the group is dense" are different statements, and the flat index cannot tell them apart. 34 lines have `E^d(Q_p) = Z_p` outright (additive classes with `c_p = 1`). Cross-check: the four lines at `p = 5` reproduce §3.3 and §3.1 exactly -- twists `1, 3, 5, -35`, generators `(0,1)`, `(3,9)`, `(4,17)`, `(59004/1369, ...)`, `M = 9,3,5,5`. **A correctness note, found later and fixed:** the class column is a discrete logarithm and means nothing without saying against which generator, but PARI's `ellgroup(E,p,1)` returns a generator chosen AT RANDOM on each call (five successive calls at `p = 23`, `d = 1` return `(18,20), (9,7), (9,7), (1,7), (19,5)`). Computing it inside the per-generator loop made the class column irreproducible, and on the 22 two-generator lines it dlogged them against DIFFERENT bases, so the pair was not even mutually consistent. Fixed by a canonical base, stated once and printed per line so each is checkable on its own: order the affine points of `E^d(F_p)` lexicographically by `(x,y)` and take the first of maximal order; in the non-cyclic case the first `g_2` of order `n_2` independent of `g_1`. Verified deterministic across repeated calls. The class values therefore changed; the group structures, orders, depths and the choice of which generators are needed are unaffected, since none of them uses a discrete logarithm. **A notation fix.** The rendered table dropped the word "ord" from the bad-reduction entries, so a cell read `2 in C_2` where it meant *an element of order 2* in `C_2` -- naturally misread as the element `2 = 0`, which generates nothing. No values were wrong (every displayed number is an order and divides its group order, checked), but two different kinds of entry share the column: where a base is printed `t` is an ELEMENT, as a discrete log against that base; where none is, `t` is an ORDER. The word is restored and a box now states the distinction. For the certificate the order is the relevant datum -- an image generates `T` exactly when its order is `|T|`, which the asterisk marks -- and it pins the element down completely whenever `|T| <= 2`, i.e. on all but three lines (`p = 31` with `C_32` and `C_30`, and `p = 31`, `d = -62` with `C_4`, all of multiplicative or `I_1^*` reduction, 31 being a bad prime of `E` itself), where it determines the element only up to `Aut(C_n)`. **A second correctness fix, prompted by asking why the base column was empty on the bad-reduction lines.** It was empty because `ellgroup` needs good reduction and `Etilde(F_p)` is singular at additive reduction. Chasing that exposed a real error: for the classes `[p]` and `[up]` the script assumed `T = Phi(F_p)` is CYCLIC of order `c_p`, and reported `C_4` on ten lines where the truth is `C_2 x C_2`. Which it is can be settled with no Kodaira table: `E_0(Q_p)` is torsion-free for `p > 2` (as `E_1 = Z_p` is and `E_0/E_1 = Z/p` is odd), so `E[2](Q_p)` injects into `Phi` and `T = (Z/2)^2` exactly when the cubic splits completely over `Q_p` -- verified by `factorpadic` on all eleven `c_p = 4` lines: ten split (so `C_2 x C_2`) and one does not (`p = 31`, `d = -62`, which is `I_1^*` rather than `I_0^*` because 31 is the bad prime of `E` itself, and is genuinely `C_4`). That also corrects the convention text, which claimed type `I_0^*` throughout. Those ten lines now carry a base as well: **the 2-torsion points**, which map isomorphically onto `Phi`. Their `x`-coordinates lie in `Z_p` and not `Z`, so the class must be computed p-adically (`P` sits on the component of the unique `T_i` with `P - T_i` in `E_0(Q_p)`) -- an earlier attempt treated the roots as exact rational points, so `ellisoncurve` rejected them and every class came back as the failure sentinel. The roots are listed as `u p` ordered by the LEADING digit `u`: ordering by the integer lift instead depends on the working precision (at `p = 47`, `d = 94` it gives `[21,3,23]` at precision 20 and `[3,21,23]` at precision 60), which is the same irreproducibility in another guise. For `|T| <= 2` no base is needed, since the order already determines the element. Two performance notes also recorded in the file: computing the closure index by looping over `[0,M-1]^r` with exact rational point arithmetic is hopeless (the multiples have height `~M^2 h`), and the order of `Pbar` in `E(Q_p)/E_1` should be found by testing only the DIVISORS of `M` via `ellmul` rather than by `M` repeated additions -- that is the difference between minutes and milliseconds once generators have large height. **Table format (§3.4.3), later revision:** two columns added for the Kodaira type and `c_p`, and the group column reduced to `E^d(Q_p)_tors` -- every one of the 180 lines had the same `Z_p` in front of it, so printing it 180 times said nothing and the heading now carries it. That also retired an earlier fix which had written the prime out in each cell (`Z_11 x C_14` for a generic `Z_p x C_14`): with the `Z_p` gone there is no subscript left to disambiguate. Net effect is a wider table that is also narrower. Output: `results/cert-extended.txt`. |
| `cert-extended.sage` | **`cert-extended.gp` rewritten in Sage**, run against it rather than instead of it (`cert-extended.gp` is untouched). Sage is now installed natively, so no Docker: `sage cert-extended.sage > results/cert-extended-sage.txt`, ~80s, then `diff results/cert-extended.txt results/cert-extended-sage.txt`. The output format is deliberately PARI's, down to the `Mod(a, p)` spelling of a residue, so the diff IS the verification. What moves to a different layer: `ellrank`+`ellsaturation` -> `E.gens(algorithm='pari')`, `elltors` -> `E.torsion_subgroup()`, `ellgroup` -> `E.change_ring(GF(p)).abelian_group()`, `elllocalred` -> `E.kodaira_symbol(p)` + `E.tamagawa_number(p)`, `factorpadic` -> roots over `Qp(p,prec)`, `ellsub` on p-adic coordinates -> arithmetic on `E` over `Qp(p,prec)`; and every convention the GP file states by hand (the canonical base, the ordering of the p-adic roots by LEADING digit, the divisors-only search for the order in `E/E_1`) is restated in another language, so a transcription error in either shows up as a diff rather than as agreement. **Result: all 180 blocks agree on every computed invariant** -- reduction type and code, `c_p`, `a_p`, `M`, rank, torsion, the type of `E^d(Q_p)`, the canonical base, and as a multiset the valuation of `alpha`, the order in `E/E_1` and the formal depth. The diff is not empty and every line of it is the Mordell-Weil BASIS rather than a result: Sage sorts the points it returns and PARI does not, so two-generator pairs are listed the other way round; six images come back as `-P`, and since a class is a discrete log it negates with the point (checked: all six have `class_gp + class_sage = 0 mod n_i` in every coordinate); and ONE line, `p = 17`, `[up]`, `d = 51`, exhibits a different lone sufficient generator, `[-714/25, 23409/125]` against `[-17, 289]`, both passing `gensalone` with `(ord, depth) = (34, 1) = (M, 1)` -- checked in both directions -- the claim being that one generator suffices, not which one. So the basis is a choice the certificate does not depend on, the same point the GP file makes about the canonical base one level up. **Two things had to be got right that PARI hides.** Sage lists the invariants of `E(F_p)` in the opposite divisibility order from `ellgroup`, so reading `(2, 30)` off directly prints `Z_p x C2 x C30` for a group the table calls `Z_p x C30 x C2`; `ellgroup` here is a wrapper that reverses them. And Sage's DEFAULT `gens()` is mwrank, not `ellrank`: over two minutes on `d = 13559` against half a second, for no extra certainty, since the pari path returns only when `#gens` meets the upper bound and so proves the rank outright. **Two O(p^2) loops of the GP original are done in O(p) here**, with the enumeration order preserved so the canonical base is unchanged: the affine points of `E(F_p)` come from one square root per `x` rather than a scan over all `y`, and `singptp` uses the partial in `y` (`2y + a1 x + a3 = 0`, and `p` is odd) to determine `y` from `x` instead of scanning the plane. Verified to leave the output byte-identical. Output: `results/cert-extended-sage.txt`. |
| `cert-ledger.gp` | **the extended certificate for the LEDGER of section 2.3** -- and it had to be made one: an earlier version printed level-1 images and the reach index and called that a ledger certificate, but a level-1 image with no granularity attached is a TALLY entry, which over-approximates the reach and certifies nothing. Every twist is now put through `gran1`/`hitsE1` from `ledger.gp` -- literally the admission test `sweepgraded` and `rungradedk` run, so the certificate and the sweeps agree by construction rather than by inspection -- and the verdict `n_p` is printed per twist and per place. All 33 twists are admitted with `n_p = 1` at all three places, so every line is a genuine ledger entry and the termination theorem applies at `N = 1`. Admission is a property of the TWIST, not of a line: `hitsE1` asks whether the reach meets `E_1 \ E_2` at `p`, which may need a COMBINATION of generators that no single one achieves, so the verdict is printed once per twist though the images are one per (twist, generator). Regenerating changed nothing else -- diffing old against new output gives 0 lines outside the new granularity rows. The rest: the S-adic arena sweep with `S = {11,13,17}`, `f = x^3+x+1` -- the analogue of `cert-extended.gp` one level up. Two things differ from the single-place case: a tuple of square classes is covered by SEVERAL twists (seven index-2 reaches in three of the five covered tuples), so a tuple gets several lines; and the image of a generator is a TRIPLE `(alpha_p ; t_p)`, one coordinate per place of `S`, since `E^d(Q_S) = prod_p (Z_p x T_p)`. Hence **one line per (twist, generator)**: a rank-2 twist occupies two lines, and `d = 335` (rank 3) three. The arena depends only on the tuple, so `E^d(Q_p)` is stated once per tuple. The extra column is the **index of the reach** in the arena `G(1)` of order `N = prod_p M_p`: index 1 is a full twist, index 2 one of the hyperplanes the ledger stacks. Arena orders reproduce section 2.3 exactly (4536, 4536, 3240, 3240, 3960), and the star test on each sign-closed ledger reproduces its outcomes line for line -- `(1,1,1)`, `(1,1,u)`, `(u,1,1)`, `(u,1,u)` cover; `(u,u,u)` and `(1,u,1)` do not. **A trap is recorded in the file because it produced a wrong answer first**: the natural index computation writes each generator's image as a discrete log in `E^d(F_p)` and takes the determinant of the Hermite form of the lattice, but `ellgroup(E,p,1)` returns a generator chosen AT RANDOM on each call (five successive calls at `p = 11`, `d = 3714`, return `(5,3), (2,8), (2,8), (5,3), (2,8)`), so computing the base inside the discrete-log routine expresses different generators of the same twist against DIFFERENT bases and the lattice is meaningless. It reported index 6 for `d = 3714`, which would have broken the covering at `(u,1,1)` and looked exactly like an error in section 2.3.9. The fix has two halves. For the INDEX: one base per (curve, prime), guarded by a second computation directly on points of `prod_p E^d(F_p)` with no discrete logarithms at all -- run on every line, **32 comparisons, 0 disagreements**. For the CLASS COLUMN that is not enough, since a base merely fixed per run is still whichever generator `ellgroup` returned, so classes are consistent within a run but irreproducible across runs and uncheckable by a reader; so the base is the CANONICAL one of `cert-extended.gp` (first affine point of maximal order in lexicographic order on `(x,y)`) and it is printed, one point per place, in the `bases g` column. Both certificates now use the same convention. Unlike the single-place table, the `Z_p`-coordinate is NOT always a unit here (`11^1 u`, `13^2 u`, `17^1 u` all occur): a partial reach may fail the formal-group condition at a place, so that column carries information. The index is omitted for `(11,u,u)`, where `11 | d_0` makes the reduction additive and `Etilde(F_11)` does not exist; the per-place data is still listed with the `T`-coordinate given by its order. Output: `results/cert-ledger.txt`. |
| `cert-p2.gp` | **the extended certificate at `p = 2`, for section 3.2** -- the eight witness twists, one per class of `Q_2*/(Q_2*)^2`, with the local half written out. The conventions of §3.4.1 do NOT apply verbatim and have to be SHIFTED: for `p >= 3` the formal group is `E_1(Q_p) = Z_p`, but at 2 that fails (`E_1(Q_2)` can carry 2-torsion), so one goes a step down to `E_2(Q_2) = Ehat(4Z_2) = Z_2` with `E_n <-> 2^{n-2} Z_2`. The finite quotient to surject onto is therefore `E(Q_2)/E_2` of order `Q_2 = 2 M_2`, `M_2 = c_2 (2 - a_2)`, the extra factor 2 being `#(E_1/E_2)`; a point generates iff its order `m` there is `Q_2` AND its depth `v = v_2(c(mP))` is exactly 2 -- the shifted form of "depth 1" -- with closure index `(Q_2/m) 2^{v-2}`. **The torsion vanishes on all eight lines**, proved two ways without using the certificate (so no circularity): the odd part of `T` injects into `E(Q_2)/E_1` of order `M_2`, which is a power of 2 in every line (`M_2` is 2 or 4); and `E_2` is torsion-free so 2-torsion injects into `E(Q_2)/E_2`, but `E[2](Q_2) = 0` because `x^3+d^2x+d^3` is IRREDUCIBLE over `Q_2` -- its roots are `d` times those of `x^3+x+1`, one statement for all eight twists -- and a point of order 4 would give one of order 2. So `E^d(Q_2) = Z_2` throughout, the base and `T`-coordinate columns are empty, and the whole content of a line is the pair `(m,v)`. That is also exactly the hypothesis section 2.2 needs: `E^d(Q_2)` is PROCYCLIC, so one generator can suffice and the surface's supply of PAIRS of points is enough; had `f` split completely over `Q_2` the group would be `Z_2 x (Z/2)^2`, three generators would be needed and the argument would break. Also checks that `Y^2 = X^3+d^2X+d^3` is minimal at 2 for all eight (`v_2(c_4) = 4`, `v_2(Delta) = 4` for `d` odd; 6 and 10 for `d` even -- never `v_2(c_4) >= 4` with `v_2(Delta) >= 12`), so the denominator test is the right one. Result: all eight have rank 1, trivial torsion, `m = Q_2` and `v = 2`, hence **index 1** -- every class covered by a single FULL twist, with no hyperplane mechanism and nothing to stack. All eight reductions are additive (`a_2 = 0`, types II, III, `I_0^*`), since 2 divides `Delta = -2^4 * 31 * d^6` for every twist. Output: `results/cert-p2.txt`. |
| `cert-ptors.gp` | **the `p`-part of `E^d(Q_p)_tors`, which `cert-extended.gp` and `cert-ledger.gp` assert rather than compute** -- both read `T` off the prime-to-`p` part of `M = #(E^d(Q_p)/E_1)` (`T = if (Mp == 1, [], ...)`) and nothing in either computes the `p`-part. It is not always trivial. Whenever `p | M` the group can be `Z_p x Z/p^k` with `k > 0`, and that is not a cosmetic error in the fourth column: `Z_p x Z/p` needs TWO topological generators, so a rank-1 twist cannot be a density witness there at all, however unit its `Z_p`-coordinate looks. `p | M` happens at every ADDITIVE place and, at a good place, exactly when `E^d` is ANOMALOUS at `p` (`a_p = 1`, so `#Etilde(F_p) = p`) -- which does occur in the table, twice: `p = 19, d = -1` and `p = 97, d = 1`. **The criterion is exact.** Write `E(Q_p) = Z_p x C_n x C_{p^k}` with `p` not dividing `n`. Since `E_1` is torsion-free the finite part injects into `E(Q_p)/E_1`, so `n p^k | M` and `MQ` lies in `E_1` for every `Q`. Realising `E_1` as the closure of `Z_p (b,c,e)` -- it need NOT be `bZ_p x 0 x 0`, the complement can be skew, which is why `E_1` has to be handled and not assumed split off -- comparing indices gives `v_p(b) = v_p(M) - k`, and `MQ = (M alpha, 0, 0)` has parameter `lambda = M alpha / b`, so `depth(MQ) = v_p(lambda) + 1 = k + v_p(alpha) + 1`. Hence `depth(MQ) >= k+1` always, with equality iff `alpha` is a unit, i.e. **`k = min_Q depth(MQ) - 1`**. Two consequences: a SINGLE point of depth 1 PROVES `k = 0`, so no search has to be exhausted and the certificate is checkable; and the condition the tables already record for a lone generator, "ord in `E/E_1 = M` and depth 1", IS the `k = 0` criterion (equivalently that `P` generates topologically and `Z_p x Z/p` is not procyclic) -- so on every line carried by one generator the assertion was a theorem all along, and only the 22 two-generator lines of §3.4 were genuinely unbacked. Sampling is DETERMINISTIC (`x_0 = 0, 1, 2, ...`, keeping those with `x_0^3 + d^2 x_0 + d^3` a square in `Q_p`), so there is no `setrand` and no random generator to reproduce -- the same reproducibility standard the canonical-base fix imposed on the class columns. Results: of the 180 lines of §3.4, **92 have `p | M`** (90 additive, 2 anomalous) and **all 92 return `k = 0`**; of the 99 (twist, place) pairs of the §2.5 ledger, one is exposed (`d = 4279` at `p = 11`, additive, `M = 22`) and it too returns `k = 0`. `p = 2` is not exposed at all, since `Delta(E_d) = -16 * 31 * d^6` gives `v_2(Delta) >= 4` for every `d`, so 2 is never a place of good reduction and never anomalous. **The fourth column of both tables stands exactly as printed; what changed is that it is now computed.** **But `k = 0` is a fact about `496a`, not a general fact, and the counterexample is one section later in the same document.** The CM curve of §5.1, `f = x^3-2`, `E_d : Y^2 = X^3 - 2d^3`, is anomalous at `p = 61`, and there `E_d(Q_61) = Z_61 x Z/61` for EVERY twist: of the 196 twists `d <= 400` anomalous at 61, all 196 have `k = 1`, the rank-2 witness `d = 2931` among them. So §5.1's remark that 61 "likewise needs a rank-2 twist" was right but for the wrong reason -- `Etilde(F_61) = Z/61` is perfectly cyclic, and what fails is one level up, non-procyclicity of `E_d(Q_61)`, which makes rank 2 MANDATORY rather than incidental. CM is the mechanism: `61 = 1 mod 3` splits in `Z[zeta_3]`, so the CM is defined over `Q_61` and `E[61] = E[p] + E[pbar]` Galois-compatibly; one summand is the connected part `Ehat[61]`, the other is etale with Frobenius the unit root `alpha = a_61 = 1 mod 61`, hence rational precisely because `p` is anomalous. The splitting does not see `d`, which is why it is uniform across the family. `496a` has `j = 6912/31`, not an algebraic integer, so no CM and no such splitting -- which is why its two anomalous places are clean. The §5.1 text is corrected accordingly. Output: `results/cert-ptors.txt`. |
| `cert-lprimary.gp` | **the `ell`-primary certificate for the single-place sweep of §3.3-3.4** (document §3.5), running beside `cert-extended.gp`, which is untouched. **The theoretical basis, which the first version of this file left implicit.** `G := E^d(Q_p)` is profinite abelian and topologically finitely generated, so a CLOSED subgroup `R` equals `G` as soon as `R.Phi(G) = G`; for abelian `G` a maximal open subgroup has prime index, so `Phi(G)` is the closure of the subgroup generated by all `l`-th powers and `G/Phi(G) = prod_l G/lG`. With `G = Z_p x T` only `l = p` and the primes dividing `#T` contribute (`l` is invertible in `Z_p` otherwise), and those factors have coprime orders. Hence **`E^d(Q)` is dense in `E^d(Q_p)` iff `E^d(Q) -> E^d(Q_p)/l` is onto for each such `l`**, checked one `l` at a time. This is the Frattini argument -- topological Nakayama proper is the pro-`p` case; the profinite abelian statement needs no pro-`p` hypothesis, but does need `R` closed, which `R = closure(E^d(Q))` is. "Primes dividing the order of `G`" must be read as the supernatural `p^oo * #T`, which is why `l = p` is always in play even when `T` is trivial. The conditions are INDEPENDENT here, unlike the ledger's coverage test, which asks that a pair lie in one and the SAME entry and so couples the layers. **What the certificate shows**, per triple `(p,d,l)`: a BASIS of `E^d(Q_p)/l` (an `F_l`-space of dim <= 2), the IMAGES in it of explicit generators of `E^d(Q)`, and the RANK -- onto iff rank = dim. Generators are listed ONCE per pair `(p,d)` on a banner row, so the `l`-lines refer back to `P_1, P_2` instead of repeating them. The first version reported only `|R_l|` against `|G_l|`: the same verdict with the reasoning deleted, and nothing on the page to verify against. **Bases by reduction type:** `l = p` -- `T[p^oo] = 0` (`cert-ptors.gp`) so `G/pG = Z_p/p` is 1-dimensional with NO canonical basis (the identification with `F_p` is only up to `F_p^*`), so only vanishing is invariant, and it fails to vanish iff `v_p(alpha) = 0` iff `depth(MP) = 1`; `l != p` good -- `T = Etilde(F_p)`, basis the canonical `g_i` with `l | n_i`, coordinates by discrete log (so the base MUST be canonical: `ellgroup(E,p,1)` returns a random generator per call, though its STRUCTURE output is deterministic); `l != p` additive -- `T = Phi`, basis the 2-torsion points named by `x = u p`; `l != p` multiplicative -- `T` cyclic, image = class mod `l`. **A bug that classification fixed:** an earlier draft treated every non-good line as `T = Phi(F_p)`. True for ADDITIVE reduction (`E_0/E_1 = G_a` is a `p`-group, contributing nothing to `T`) but NOT for multiplicative, where `E_0/E_1` is the non-split torus, cyclic of order `p - a_p` prime to `p`, and is the whole of `T`; both `p = 31` lines with `31 ∤ d` came back "not onto" (31 is the bad prime of `E` itself, so these are the only multiplicative lines). The order of `Pbar` in `E/E_1` is the right invariant and needs no case analysis. **Results:** all 180 blocks onto at every `l`, 0 disagreements with §3.3, over 419 layer lines plus 180 banner rows. **It also sharpens the rank dichotomy:** `E^d(Q_p)` is procyclic iff every `G/lG` is 1-dimensional, readable off the dim column. Of the 22 lines where no single generator is dense, only **21** carry a 2-dimensional layer (at `p = 47, 67, 131, 139, 149, 173`, always `l = 2`); the 22nd, `p = 83, [u], d = -11`, has every layer 1-dimensional -- the group IS procyclic, and `P_1` vanishes at `l = 3` while `P_2` vanishes at `l = 13`, so `-P_1 - 2P_2` is a single dense point. §3.4 called all 22 non-procyclic; corrected. **Bug worth recording:** closing a layer subgroup by repeated pairwise addition under a fixed guard of 40 passes under-counts a cyclic layer of order 139, which is exactly what `p = 163` has; replaced by the standard incremental method. **Table format (§3.5):** the banner row per `(p,d)` carries the Kodaira type and `c_p` alongside the generators, since reduction type is a property of `(p,d)` and not of `l` -- putting it in a column would repeat it on every layer line. `kodsym` maps PARI's `elllocalred` code to the usual symbol, calibrated against `v_p(Delta)`: `1 = I_0`, `4+n = I_n`, `-1 = I_0^*`, `-4-n = I_n^*`, `2,3,4 = II,III,IV`, `-2,-3,-4 = II^*,III^*,IV^*`. Output: `results/cert-lprimary.txt`. |
| `lprimary.gp` | **what the `ell`-primary rewrite buys, measured rather than asserted** (document §2.3.4). Answers two questions for `f = x^3+x+1`, `S = {11,13,17}`, the `d_0 = 1` tuple. **(1) What admission discards.** `gran1` is an admission TEST: a twist that fails is skipped outright, not stored at a coarser grade. §2.3.2 distinguishes two kinds of refusal -- finite granularity `n_p >= 2`, usable in principle and lost only for want of level-2 machinery, versus infinite index in the `p`-direction, which has no granularity at all and is inadmissible in any ledger. Measured: of 119 twists with points, 102 admitted and **17 refused, every one of finite granularity exactly 2** (at one place, except `d = 287` at two). None is inadmissible; all 17 would enter a level-2 ledger with the termination theorem at `N = 2`. Granularity is read off cheaply: with `v_p(M_p) = 0` and no `p`-torsion (`cert-ptors.gp`) the arena's `p`-layer IS `E_1(Q_p) = Z_p`, the `Z_p`-coordinate map is a homomorphism so the closed span of the `alpha_i` is `p^(min_i v_p(alpha_i)) Z_p`, and with the depth formula `n_p = min_i depth_p(P_i)` -- the minimum of a column the certificates already print. Computed both ways (this formula and `hitsE1`'s combination search) on all 119: **0 disagreements**. **(2) What the rewrite costs.** A subgroup of a finite abelian group is the direct sum of its `ell`-primary parts, so layer decomposition is EXACT, not an approximation -- unlike level truncation, which is lossy and needs granularity to repair. Off-diagonal cells (`ell != p`) are finite and level-independent; the diagonal `G_{p,p} = Z_p` is the only thing that grows with `n`, and a closed subgroup of it is `p^{k_p} Z_p`: ONE INTEGER, exact, no truncation. The star test becomes: mask every `<=2`-generated subgroup of each layer against the ledger, coverage iff no choice of one mask per layer has empty intersection (the choice must be constant across layers -- coverage does NOT decompose into independent per-layer conditions). Run against the known answer: `G(1) = 4536 = 2^3*3^4*7` splits as `G_2` (order 8, 15 subgroups, 15 masks), `G_3` (81, 23, **1**), `G_7` (7, 2, **1**); 15 mask triples, none empty, **COVERED** -- reproducing the bitmap verdict. Only `ell = 2` carries information, so the whole covering question is 15 subgroup tests in a group of order 8, against the 20575296 ordered pairs the flat bitmap walks; the binding layer being `ell = 2` is exactly the `(Z/2)^3` and its seven hyperplanes, now visible in the representation instead of inferred. **Level 2**: `ker_1/ker_2` has order `11*13*17 = 2431`, COPRIME to 4536, so `G(2) = G(1) + prod_p Z/p` splits and the layers 2, 3, 7 are UNCHANGED -- level 2 adds three NEW layers, each cyclic of prime order, where a subgroup is 0 or everything. One bit per entry per place, not a factor 2431 on a bitmap; and that bit IS "granularity 1 at `p`", so admission is not a bolted-on filter but the reach's own coordinate in the `ell = p` layer. **Caveat recorded in the file:** `k_p = min_i depth_i` uses `T_p[p^oo] = 0` AND `v_p(M_p) = 0`. The first fails for the CM curve of §5.1 at `p = 61` (`Z_61 x Z/61`); the second fails at every additive place, and 56 of the 64 tuples for this `S` are ramified somewhere. A rewrite meant to sweep all 64 needs a two-invariant diagonal cell, not a single `k_p`. Output: `results/lprimary.txt`. |
| `coupling.gp` | **does the mod-`l` reduction of §3.5.1 survive the S-adic case, where a square class may need several twists?** (document §2.3.5) **No**, for two independent reasons, both exhibited on groups small enough to check by hand. **What still transfers:** for a SINGLE twist, "is `R(d)` full?" is exactly the §3.5.1 question, and Frattini applies verbatim over `S` -- `dim G/lG = [l in S] + sum_p dim E^delta_p(Q_p)[l]`, which is precisely §2.3's formula for `g(delta)`. So the rank dichotomy WAS the `l`-primary statement all along: `g` is the largest layer of the Frattini quotient. **What is still exact:** a closed subgroup of a profinite abelian group is the product of its `l`-primary parts, so each individual containment `<a,b> subset R` does decompose layerwise. **Failure 1 -- membership is not a mod-`l` condition.** Frattini decides whether a subgroup is EVERYTHING; coverage asks whether particular ELEMENTS lie in it, and mod `l` one sees only `R + lG`, in general strictly bigger. `G = Z/4`, `R = 0`, `a = 2`: the mod-`l` test passes and `a` is not in `R`. **Failure 2 -- the existential does not commute with the layers.** Coverage is `forall (a,b) EXISTS R forall l`; layerwise checking proves only `forall (a,b) forall l EXISTS R`, and the entry that works may differ between layers. Smallest instance `G = Z/6` with entries `<3>` and `<2>`: layer 2 covered by `<3>`, layer 3 covered by `<2>`, and 24 of the 36 ordered pairs lie in neither. **The repair** is to keep the decomposition but resolve the existential once at the end: mask each `<=2`-generated `H <= G_l` by `{R : H subset R_l}`, and coverage iff no choice of one mask per layer has empty intersection -- the star test of §2.3.4, which returns the correct negative on the `Z/6` example. **Why the coupling has never bitten:** every reach in every ledger here has index 1 or 2 (66 of index 2, 14 of index 1, none other), and an index-2 reach is full at every layer but `l = 2`, so the other layers give the full mask and the question collapses onto `l = 2`. That is data, not a theorem -- an index-2 reach alongside an index-3 one in one tuple is exactly Failure 2. Output: `results/coupling.txt`. |
| `kummer-ledger.typ` | **documentation of the ledger / arena sweep** (companion to `ledger.gp`): what the algorithm computes, the data structures (packed arena, bitmap reaches, membership masks), the salient code, the grading and certificates of exactness, and a section on TERMINATION -- the ledger need not stabilise, non-termination forces unbounded granularity, and neither can be ruled out. |
| `ledger.gp` | the arena, tally, ledger and star test at level 1 (document §2.3.1). **Vocabulary, and the file now states it at the top:** a TALLY is a record of level-`n` images with no granularity attached, so its entries over-approximate the reaches and closure is a NECESSARY condition only -- a sanity check; a LEDGER attaches to each entry a granularity `n_d` certifying `R(d) >= ker_{n_d}`, which pins the finite datum to `R(d)` exactly and lets the termination theorem apply, so a closed ledger is a PROOF. `runtally` (renamed from `runledger`, which was a misnomer) builds a tally; `rungraded`, `sweepgraded` and `rungradedk` build ledgers, admitting an entry only when `gran1`/`hitsE1` verifies it. Every sweep reported in the document is graded. Core: `arenainit`, `reachmap`, `signact`, `ledgeradd`, `maskvec`, `startest`, `runtally`, `rungraded`. Reduction-agnostic arena (§2.3.4): `shortmodel`, `shortdata`, `cosetreps1`, `cosetclose`, `cosetidx`. Sweeps over all tuples (§2.3.6): `sweepgraded`, and the per-tuple search `rungradedk`, `sweeptuples` (on `tuplepart`/`tuplename` from `sadic.gp`); `showcert` for the certificate. NOTE: `runtally` used to call `ledgeradd` with three arguments, so PARI supplied `d = 0` and the tally's certificate named no twists; fixed 2026-08-21. |
| `depends.gp` | **the obstructed set S read off (E, ell, phi) directly** (survey document §10). No local points, no descent images, no symbols: only reduction data. Five steps -- (1) bad primes only, since at good `v != ell` the local condition is `H^1_ur` and at good `v = ell` it is `H^1_f`, both functorial hence phi-stable for EVERY phi; (2) discard `v != 1 mod ell`, since a non-zero alternating `beta_v` needs all of `E_d[ell]` rational over `Q_v`; (3) keep multiplicative `v` with `ell | v(Delta_min)`; (4) the live square class is the unique unramified one making `E_d` SPLIT; (5) phi decides -- live iff the canonical line `C_can = mu_ell` is not phi-stable, detected by which `ell`-isogeny MULTIPLIES `v(Delta)` by `ell`. `sqreps`, `tw`, `ratroots`, `ntorsx`, `redtype`, `canline`, `splitclass`, `run`. Output: `results/survey-depends.txt`. |
| `corollary6.gp` | **search for surfaces with exactly one critical prime** (survey document §10.9). Uses Corollary 6 plus Theorem 8: `ell` odd, `E[ell]` decomposable, `ell` NOT dividing `N_E` (so Lemma 1(b) kills the wild place), and exactly one bad prime passing (a) split multiplicative, (b) full local `ell`-torsion, (c) no rational line canonical. Scans the universal 3-torsion curve `y^2+axy+by=x^3` at `ell=3` and the Tate 5-torsion form at `ell=5`, since decomposable `E[ell]` is rare in a naive box. `monicF`, `nisog`, `canrational`, `splitclass`, `fulltors`, `sigma1`, `scan`, `scan5`. Output: `results/survey-corollary6.txt`. |
| `corollary6-additive.gp` | **obstructed surfaces whose curve is ADDITIVE at the critical prime** (survey document §10.9.2). Theorem 8's hypothesis (a) is about `E_d`, not `E`, and by §10.8.1 the split class is RAMIFIED when `E` is type `I_n*`. Twisting the defining cubic by its own critical prime, `f_p(x) = p^3 f(x/p)` -- the SAME Kummer surface -- gives a model additive at `p`, and the critical class must move to `[p]` or `[u*p]`. Four cases, all confirmed by `runsurface`. Output: `results/survey-corollary6-additive.txt`. |
| `corollary6-check.gp` | **the §3 density check on those predictions** (survey document §10.9.1). Runs `runsurface` with the survey's own parameters over all 45 odd primes `<= 200` plus the eight classes at 2. Output: `results/survey-corollary6-check.txt`. |
| `level2.gp` | **the `ell = 2` split case** (survey document §10.10). (1) The norm lemma is FALSE for split `f`: it controls `(c(P),c(P)) = (c(P),-1)`, which is the diagonal only when both slots carry the same descent map, i.e. in the indecomposable case; for split `f` the diagonal is `(c_a(P),c_b(P))` and is non-trivial at the 2-torsion. (2) So beta is SYMMETRIC with a quadratic refinement `q_v(P) = beta_v(P,P)`; "alternating" is the case `q_v = 0`. Lemma 3's dimension bound fails, so a 1-dimensional `W_v` can be live -- as at infinity for 15a1's second phi. (3) **Lemma 11**: `beta_q = 0` for every odd `q | d` prime to `2 disc f` iff `f'(e_a)` and `f'(e_b)` are both perfect squares (Lemma C is 15a1: 400 and 225). The family is Pythagorean again -- root gaps `g a^2`, `g b^2` with `a^2+b^2` square, 15a1 being (3,4,5). (4) An additive live place is a `q | d` place in another model, so it is never isolated. `fp`, `ci`, `diagonal`, `betaq`, `qdtest`, `live`, `analyse`. Output: `results/survey-level2.txt`. |
| `wild-tamagawa.gp` | **tests, and REFUTES, chapter 11's Tamagawa conjecture** (survey document §11.5). The test is local -- whether alpha collapses involves no phi -- so every curve with additive potentially good reduction at 3 and `dim W_3 = 2` is a data point. Two measurement traps cleared first: the image of alpha is a subgroup but the set of values over a SAMPLE is not (the sample is not closed under addition -- this is why `alpha3.gp` reports sizes like 2 and 5 and flags them), so one must take the SPAN; and the tangent evaluation was first checked to be multiplicative mod cubes, 400/400 pairs on each of three curves. Calibrated on `x^3-2`: dim 0 at `d=1`, dim 2 at `d=6`. Result: fails 8 of 14, and structurally -- type II with `c_3 = 1` occurs both with `alpha = 0` and `alpha =/= 0`. Output: `results/survey-wild-tamagawa.txt`. |
| `wild.gp` | **the wild place `v = ell` with potentially good additive reduction** (survey document chapter 11). Inlines alpha3.gp's tangent/descent machinery so that reading it does not run alpha3's driver. Runs all four square classes of `x^3-2` at `v = 3` and shows that each is accounted for by a different clause: two die by dimension (`dim W_3 = 1`), one by COLLAPSE (`alpha = 0`), and the survivor is the class where §5.1.5 proves the obstruction sits. Output: `results/survey-wild.txt`. |
| `selmer-local2.gp` | **the local condition at `v = 2`** for the Selmer involution (survey document `selmer-involution.typ` §7.3). Computes `L_2(E_a)` and `L_2(E_{-1/a})` inside ONE algebra `Q_2 x K` using the canonical identification `theta' = -1/theta`, and finds them EQUAL. The check is exhaustive: over `Q_2` every unit is a cube, so `E_a` depends only on `a mod 8`, and all four classes are checked. Records two bugs caught by parity -- the naive "cube root <-> cube root" identification, and a wrong 2-torsion entry that inflated the group to order 8. Output: `results/selmer-local2.txt`. |
| `sha-zhao.gp` | **what the Zhao-method literature can and cannot say** about `y^2 = x^3+px` (companion to `sha-zhao.typ`). Nomoto (arXiv:2207.10380) treats exactly this family and proves `v_2 >= (r(D)-2)/2` with `r(D)` the number of primes of `Z[i]` dividing `D`; for `D = p` rational the prime SPLITS, so `r = 2` and the bound is VACUOUS. Checks that Nomoto's local invariant `(i/p)_4 = i^((p-1)/2)` is constant (= 1) on the family, and that `v_2(p-1)` does not determine the answer either (193 and 577). Output: `results/sha-zhao.txt`. |
| `sha-tunnell.gp` | **the square root `m` of `L(E_p,1)/Omega = 2m^2`** and what governs its parity (companion to `sha-tunnell.typ`). Observation 1 (82/82): `m` is even iff 2 is a quartic residue mod `p` iff `8 | b` in `p = a^2+b^2` -- Gauss's criterion, equivalently `p = x^2+64y^2`. Observation 2 (FAILS, 48 of 101): the octic analogue for `4 | m`; counterexample `4177` and `4937`, both `b = 64` and both `x^2+256y^2` with `y = 4`, yet `m = 2` and `4`. Output: `results/sha-tunnell.txt`. |
| `sha-bsdsum.gp` | **the Birch-Swinnerton-Dyer finite sum, evaluated exactly** (companion to `sha-bsdsum.typ`). Implements Nomoto Thm 2.6 (first branch, which is the branch for every `p = 1 mod 8`) with `D = Delta = p`, summing a rational function of the `x`-coordinates of `p`-torsion points of `y^2 = x^3-x` weighted by the quartic residue symbol. Normalisation validated against the paper's own `wp` special values to 60 digits. Result: `S(17)^4 = 2^6*17^3` and `S(257)^4 = 2^14*257^3` exactly, and `S = 2 sqrt2 m^2 p^(3/4)` on six primes to 40 digits -- so the sum computes `m^2` directly. Output: `results/sha-bsdsum.txt`. |
| `sha-fermat.gp` | **Sha of `y^2 = x^3 + px` at the Fermat primes** (companion to `sha-fermat.typ`). For `p = 1 mod 8` with `L(1) =/= 0` the Tamagawa product and torsion are both 2, so BSD collapses to `#Sha = 2 L(1)/Omega`; verified for all 64 such `p < 3000`, along with the Waldspurger square shape `L(1)/Omega = 2m^2`, giving `#Sha = (2m)^2`. Computes the `k = 4` case directly: `L(E_65537,1)/Omega = 32`, so `#Sha = 64`. Output: `results/sha-fermat.txt`. |
| `selmer.gp` | **the 2-Selmer involution `a -> -1/a`** (companion to `selmer-involution.typ`; MathOverflow 227987). Verifies the conjecture that `y^2 = x^3+a` and `y^2 = x^3-a^5` have equal 2-Selmer ranks for odd `a`, and checks Theorem A -- that the two curves share their mod-2 Galois module -- by comparing factorisation types at every good prime and the 2-division fields. NB `cmp` is a PARI builtin, so the driver is `compare2`. Output: `results/selmer-involution.txt`. |
| `additive.gp` | **the additive places** (survey document §10.8). Three computations: (1) the split-multiplicative square class can be RAMIFIED -- 11a1 twisted by 11 is `I5*` at 11, and it is `d = 11` that returns split multiplicative reduction, so Step 4 must scan all four classes and potentially multiplicative additive places are covered by Theorem 5 after all; (2) Lemma 7, `W_v = Phi_v/ell` at additive `v` not dividing `ell`, verified by measuring `|W_v|` against `|Phi/2|` (note `Phi = (Z/2)^2` for `I_n*` with `n` EVEN, not `Z/4`); (3) at `I0*` with `c_v = 4` the pairing is a Hilbert symbol of ROOT DIFFERENCES, checked against a direct search for all three phi on four curves -- exact agreement, and these places are LIVE. `kodname`, `splitscan`, `phimod2`, `wsize`, `lemma7`, `ci`, `imgs`, `predict`, `direct`, `census`, `run`. Output: `results/survey-additive.txt`. |
| `depends-15a1-sigma.gp` | **the full `Sigma(d)` for both phi on 15a1**, `d = -1` (survey document §10.5.1). Prompted by an apparent conflict: §3 witnesses every class of 15a1 at `p = 3`, while §10's recipe reports 3 as critical for the phi pairing `c_2, c_3`. No conflict: `phi_A = (c_1,c_3)` has `Sigma = {5}` (the §7.1 theorem), while `phi_B = (c_2,c_3)` has `Sigma = {infinity, 2, 3}` -- a three-place correlation with no constraint at 3 alone. Also shows `beta_oo =/= 0` on the ONE-dimensional `W_oo`, so beta is not alternating for `phi_B`: the norm lemma is about a pair of descent maps, not about the curve. `realchk`, `fin`. Output: `results/survey-15a1-sigma.txt`. |
| `depends-check.gp` | **out-of-sample test of the sufficiency theorem** (survey document §10.7.1). For curves appearing nowhere else, and each of the three rank-one phi on a curve with full rational 2-torsion, predicts the verdict at every odd bad place from reduction data alone and then computes the symbol table. Theorem 5 predicts non-degeneracy at a live place -- both images all four classes, 6 of 16 ordered pairs with a non-trivial symbol -- and a COLLAPSE of the canonical line's descent map at a dead one. 13 predictions, 13 agreements. CAVEAT: the sampler uses `x = +- m p^k` with `m <= 40`, so an image can be reported as 3 (impossible for a subgroup) -- a live verdict is still a proof, a dead one is evidence. `symtab`, `canroot`, `splitclass`, `oos`. Output: `results/survey-depends-check.txt`. |
| `twoplace.gp` | **the two-place screen** (survey document §9). Restricts to the family where *both* structural lemmas hold — `q(x) = (x+al)^2 + mu^2` a sum of two squares (so beta is alternating) and `b = al^2 + mu^2 = k^2` a perfect square (so §6.2.2 kills the places `q | d`) — i.e. `f = x q(x)` indexed by **Pythagorean triples**, where the odd live places lie among the prime factors of the hypotenuse `k`. For each triple, each candidate place (`2` and the primes of `b`) and each square class of `d` (4 classes for odd `v`, 8 for `v=2`) it reports whether the image of `c` is *non-isotropic* for the Hilbert symbol, and checks that the sampled points generate `E_1` (odd `v`) or `E_3` (`v=2`). NOTE `sqreps`: for `v = 1 mod 4` the list `1,-1,v,-v` covers only two of the four classes, so a primitive root is used. `sqreps`, `probe`, `run`, `detail`. Output: `results/survey-twoplace.txt`. |
| `template3.gp` | **the 14a1 template applied to 14a2 and 19a1** (survey document §7.5). For each: the base curve `E_0` with rational 3-torsion and the twist relating it to `y^2 = f(x)` (trivial for 14a2, `m=2` for 19a1); the critical place, where both lines are `Q_p`-rational, the image of `(c_1,c_2)` on `W_p` has 9 elements, and the tame cubic symbol is symplectic; and the wild place `v=3` over all four square classes. Images are the SUBGROUP GENERATED by the values, not the value set. `tw`, `lines3`, `ycoord`, `slope`, `cubp`, `cub3`, `span`, `genq`, `tamec`, `symtable`, `atp`, `at3`. Output: `results/survey-template3.txt`. |
| `localimg.gp` | **the local statements of §7, made exhaustive** (technique: §6). Each is about `E_d` over one `Q_v`, and `E_d/Q_v` depends on `d` only through its class in `Q_v^x` mod squares (4 classes for odd `v`, 8 for `v=2`), so a bounded number of local runs settles every squarefree `d`. Each run is a proof, not a sample: the sampled points are shown to *generate* the relevant quotient, and value classes are read off from the valuation and finitely many digits. Covers 14a1 at `v=7`; 15a4 at `v=5` and `v=2`; 15a1 at `v=5`, `v=3` (4 classes) and `v=2` (8 classes), plus a direct confirmation of Lemma C's prediction at fifteen pairs `(q,d)` with `q | d`. Also 17a1 at `v=17` and `v=2`, and `dimtors`, checking the ramified-twist lemma (§6.4.1) on both sides: no `Q_q`-rational 3-torsion for 14a1 at `q | d`, all three 2-torsion points rational for 15a1. `cubcls`, `sqcls`, `gen1`, `genq`, `run7`, `runc`, `run1`, `qcheck`, `dimtors`. Output: `results/survey-localimg.txt`. |
| `alpha3.gp` | **the image of the level-3 descent map at `v = 3`** (survey document §7.2.3; technique §6.4). Measures `dim ker alpha` — which `vell.gp` cannot, since it only tests non-vanishing — by evaluating the tangent at a `Q_3`-rational 3-torsion point and classifying the values in `Q_3^x` mod cubes, using only `v_3` and the unit part mod 9, so no precision is lost. Also checks that the sampled points generate `E(Q_3)/E_2`. Organised by the four square classes of `d` in `Q_3^x`, so it settles every squarefree `d` at once. `gval`, `dgval`, `cub3`, `generates`, `run`. Output: `results/survey-alpha3.txt`. |
| `vell.gp` | **`beta` at the wild place `v = ell`** (survey document §7.4.3; the lemma it uses is §6.4.1). When `dim W_ell = 2` the question is whether `L_ell` is `phi`-stable, i.e. whether BOTH `ker alpha_i` are non-zero, i.e. whether each dual isogeny image escapes `ell E(Q_ell)` — decidable because `ell E` contains `ell E_1 = E_2`. Confirms `beta_5 = 0` for `11a1` and, for `14a1`, `beta_3 = 0` at good reduction but `beta_3 != 0` at the additive twists. `grp`, `inLL`, `torsdim`, `dualof`, `run`. Output: `results/survey-vell.txt`. |
| `endo.gp` | **from an endomorphism of `E[ell]` to an algebra** (survey document §8.1). A rank-one `phi` in `End_G(E[ell])` is exactly an ordered pair of Galois-stable lines `(ker phi, im phi)`, and the algebra is `cor_L ( f_I(P), f_K(Q) )_ell` with `div(f_C) = ell(T_C) - ell(O)`. Enumerates the rank-one `phi` for each `f` and reports which give a class in `Br(X)`: 2 of 9 for `15a1` (the two the §7.1 local search kept), 1 of 1 when `E[2]` is indecomposable. Output: `results/survey-endo.txt`. |
| `ramification.gp` | **is `A` unramified on the Kummer surface?** (survey document §8.4). Computes the residue of `A_ij` along each of the 16 exceptional curves of `Kum(E x E)`. The residue is a *constant*: `-(e_a-e_i)(e_b-e_j)` on four curves, `f'(e_i)` or `f'(e_j)` on four more, trivial on the other eight — so `A` is unramified iff those are squares in `Q(e_a,e_b)`. `fp`, `resid`, `splitcase`, `quadcase`, `issq`. Output: `results/survey-ramification.txt`. |
| `level3.gp` | **the level-3 Brauer class and a direct evaluation of `beta_3`** (survey document §8.3). (A) the twist-free descent functions: `w = v sqrt(6d)/6` and `z = v sqrt(-2d)/2` clear `d` and land on the two *fixed* conductor-27 curves `6w^2 = x^3-2` (with `T=(2,1)`) and `-2z^2 = t^3-2` (with `S=(0,1)`), whose tangents give `ghat = w-x+1`, `hhat = z-1`; normalised `G = 36 ghat`, `H = hhat/2`. (B) `tameF` — the tame cubic Hilbert symbol over a number field containing `zeta_3`; checks `beta_v = 0` at every `v != 3` on rational pairs. (C) `wildcub` — the **wild** cubic symbol on `Q_3(zeta_3)`, got by the product formula (one prime above 3) plus global representatives mod 9; validated non-degenerate, skew, Steinberg. (D) `beta3` — evaluates `beta_3` on `E_d(Q_3)`: alternating, zero on rational pairs, symplectic on `W_3`. Output: `results/survey-level3.txt`. |
| `azumaya.gp` | **the Brauer class at level 2** (survey document §8.2). Checks that the classical descent map `c_i(P) = U(P) - d e_i` on `E_d` equals `prod_{j!=i} (x - e_j)` modulo squares on `X`, so that `beta_v(P,Q)` is the local invariant of the twist-free quaternion algebra `A = (prod_{k!=i}(x-e_k), prod_{l!=j}(t-e_l))` over `Q(X)`. Compares `inv_v A` with `beta_v` place by place and checks reciprocity. `runsplit`, `runmonic`, `allplaces`, `hil`. Output: `results/survey-azumaya.txt`. |
| `beta3.gp` | **the local input of §5.1.5**, with the isogenous curves. For `E_d : y^2 = x^3 - 2d^3` and `d` in the class `[u*3]` at 3: minimal models, Kodaira type, `c_3` and `M` for `E_d` and for both 3-isogeny quotients `B_i = E_d/C_i`; the census of sampled `Q_3`-points by layer `E_1 < E_0 < E(Q_3)`; and `v_3(x(psihat_i P_i))` for a point of `B_i(Q_3)` outside the kernel of reduction. Also whether `ker(psihat_i) = psi_i(C_j)` is pointwise `Q_3`-rational (it is for `i=2`, not for `i=1`) and whether `psi_i(E(Q_3))` escapes `N_i` — the two inputs half two uses. Helpers `kodname`, `singpt`, `ratpt`, `layer`, `inE0`, `firstinlayer`, `layercensus`. Output: `results/survey-beta3-curves.txt`. |
| `control.gp` | the control experiment for the `p = 3` open case (document §5.2.2): `armA`, `find3`, `armB`. |
| `cm-torsion.gp` | the CM mechanism at `p = 3` for `f = x^3-2` (document §5.2.1): `torsionQ3`, `row`, `correlate`, `structure`. |
| `cover-p2.gp` | the same check at `p = 2`, with exact rational arithmetic and the corrected target set (see below). |
| `results/` | raw output of the tuple sweeps, kept verbatim because it carries the certificates -- the witnessing twist per tuple for `S = {5,7}` and `S = {3,5,7}`, and the twists and signs behind every maximal reach for `S = {11,13,17}`. All of it is now tabulated in the document (§2.2.1, §2.3.6); these files are the machine-generated source. Also `survey-*.txt`, the raw output behind `kummer-survey.typ`, and `surfaces-cremona.txt`, the curve list. |
| `survey.gp` | **the survey** (`kummer-survey.typ`), both batches. The same test for a *general monic cubic* `f = x^3+ax^2+bx+c`, passed as `F=[a,b,c]`: `twistdata3`, `sweep3`, `sweptdata3`, `procyclic3`, `witnessodd`, `witness2`, `runsurface`. Deeper passes `deephunt`, `hunt2`; diagnostics `gloc`, `gloc2`, `classaudit`, `rankaudit`, `rankaudit2`. Family scans `sexticscan`, `quarticscan`. The §5.1.5 machinery at `l = 2`: `torsionmodule`, `halvable`, `dimW`, `cosetsE2`, `mod2A`, `imagelines`, and the Hilbert-symbol pairing `cmap`, `betav`, `alttest`, `normtest`, `cimagep`, `nzbeta`, `lemmaA`, `cyctest`. The single-place ledger: `ppoints`, `arena1`, `modlA`, `lbasis`, `ledgerp`. Constructing the pairing when `f` splits: `cvals`, `betan`, `badplaceset`, `nmats`, `cpairimage`, `nzlocal`; and at level 3: `cubeclass`, `cubeclassp`, `cubicsym`, `cubicsymp`, `tangent3`; and the triage `nisog`, `pairingpossible`, `triagepair`. |
| `cm-surfaces.sage` | the CM surface list: the eleven rigid CM surfaces plus the sextic (`j=0`) and quartic (`j=1728`) families, reduced to small monic cubics. Output in `results/surfaces-cm.txt`. |
| `surfaces.sage` | the surface list: every curve of conductor <= 40 from Sage's Cremona database, grouped into quadratic-twist classes and reduced to a small monic cubic. Output kept in `results/surfaces-cremona.txt`. |
| `survey-tables.py` | turns `results/survey-*.txt` into `survey-tables.typ` (generated; not edited by hand). |
| `kummer.gp` | the earlier single-generator version (`densecyclic`, `scan`) over the `t_0`-family. Kept only as the reference implementation for `validate.gp`. |
| `validate.gp` | validates the `p`-adic density test against an exact-rational reference implementation, and `densegroup` against `densecyclic`. |

## Reproducing the results

Main result, `f = x^3 + x + 1`, all odd primes up to 200 — one pass, ~5 s:

```
read("driver.gp");
SW = sweep(1, 1, 1500, 50);          /* 92k twists, each with a free point */
hybrid(1, 1, SW, 200, 60, 20000);    /* 45 / 45 primes */
```

The slow reference path, kept for cross-checking, is

```
D = build(1, 1, 3000);               /* ~11 s: ellrank on every twist */
report(1, 1, D, 200);                /* 43 / 45; 131 and 149 then need hunt() */
```

`p = 2` (all eight square classes):

```
read("p2.gp");
report2(1, 1, 400);
```

Independent coverage check on the surface:

```
read("cover2.gp");
coverage(1, 1, 5, 3, [1, 3, 5, -35], 400);      /* complete mod 5^3 */
read("cover-p2.gp");
cov2(1, 1, 4, [1,-1,3,5,6,-6,30,-30], 90);      /* complete mod 2^4 */
```

The CM case discussed in §5.2 of the document:

```
read("driver.gp");
SW2 = sweep(0, -2, 1500, 50);
hybrid(0, -2, SW2, 100, 60, 15000);   /* every odd 5 <= p <= 97; p=3 is 3/4 */
hunt(0, -2, 3, 3, 12000);             /* class [u*3] at p=3: no witness found */
```

and `p = 2` for the same surface, which is fine (8/8 classes, all rank 1):

```
read("p2.gp");     report2(0, -2, 600);
read("cover-p2.gp");
cov2(0, -2, 5, [1,3,-3,-1,-30,-10,10,30], 140);   /* 256/256 both regions */
```

Why that class and no other (§5.2.1) — the extra `Q_3`-rational 3-torsion point
`T_d = (2d, sqrt(6d^3))` exists exactly when `d` is in the class `[u*3]`:

```
read("cm-torsion.gp");
correlate(1500);      /* 458 twists, 0 mismatches with the prediction */
structure(-3);        /* (Z/3)^2, read off from Q_3-points  */
structure(3);         /* cyclic Z/3                          */
```

The control experiment (§5.2.2) — is the failure local, or about the curve?

```
read("control.gp");
armA(0,-2,7,1,400);        /* p=7, same CM curve, (Z/3)^2 : 20 of 25 dense  */
find3(21,26);              /* which classes have M=9 at p=3                 */
armB(21,26,2,3000);        /* non-CM, E[3] decomposable : 0 of 36 dense     */
armB(-24,26,2,3000);       /* non-CM, E[3] decomposable : 15 of 43 dense    */
placeaudit(1500);          /* which places can carry a non-zero beta_v      */
phistable(-3);             /* W_3 not phi-stable, i.e. beta_3 is nonzero    */
```

Which families are obstructed (§5.2.3) — 19 of 19 match the criterion
"obstructed iff the denominator of `j` is a power of 2":

```
read("families.gp");
sweepfamilies(45);         /* 17 families, 9 testable  (~7 s)   */
sweepfamilies(130);        /* 39 families, 19 testable (~2 min) */
```

Decomposable families are parametrised by a genus-0 curve, so there are
infinitely many; searching it directly shows only two have `j` with a
2-power denominator, and those are exactly the two obstructed ones:

```
read("families.gp");
scan2pow(1000);            /* 2433532 parameter values -> j = 0 and 9261/8 */
```

The quantitative consequence of the pairing mechanism (§5.2.4): unobstructed
families should still span *below* the unconstrained rate.

```
read("families.gp");
poolstats(130);            /* 195/421 = 0.463 against 48/81 = 0.593 (~2 min) */
```

Sanity checks:

```
gp -q validate.gp < /dev/null
```

## Two things to be aware of

**A fixed bug.** An earlier `densegroup` short-circuited with
`if(rem == 1, break())` once the running index reached `M`. That skipped the
remaining generators, so they never contributed to the kernel lattice `L` — and
condition (ii) is tested only on a basis of `L`. A twist with generators
`P_1 not in E_1` and `P_2 in E_1 \ E_2` (which *is* dense) was reported as not
dense. The bug is one-sided: it can only turn a true "dense" into a reported
"not dense", never the reverse, so no positive result was ever affected. It is
fixed here (`rem == 1` gives `divisors(1) = [1]`, which the generic branch
handles correctly).

**The target set at `p = 2`.** For odd `p`, if `y` is a unit and
`y^2 = s mod p^k` then `±sqrt(s) = y mod p^k`, so "all congruence solutions" is
the right target set. At `p = 2` that congruence has *four* solutions but only
two are genuine reductions, so the naive target set is a factor 2 too large and
the check reports a spurious failure. `cover-p2.gp` uses the corrected
enumeration: lift `x, t` mod `2^(k+4)`, require `s = 1 mod 8`, record
`±sqrt(s)`.

## Search strategy

There are two ways to get rational points on the twists, and they have very
different costs.

**The `t_0` sweep.** A twist `E_d` has an affine rational point iff `d` is the
squarefree part of `f(t_0)` for some `t_0`, and then `(t_0, 1)` *is* such a
point. So sweeping `t_0 = a/b` and bucketing by squarefree part hands out one
generator per twist for free, and the family is complete — no twist with a
rational point is missed.

**Per-twist descent.** `ellrank` on each `E_d` in turn. This finds generators
of any rank, but you pay for a 2-descent every time.

Measured on `f = x^3 + x + 1`:

| strategy | throughput | rank-2 twists produced |
|---|---|---|
| `t_0` sweep | 91963 twists in 663 ms = **0.007 ms/twist** | 54 of 91963 (0.06%) |
| per-twist `ellrank` | 366 twists in 833 ms = **2.28 ms/twist** | all, on demand |

The sweep is ~300x cheaper per twist but almost never yields **two independent
points on one twist**: that needs a collision of squarefree parts, and
`|a|,b <= H` gives ~`H^2` values of `f(t_0)` spread over a range ~`H^4`, so
collisions stay negligible (7.5k sweep -> 18, 92k sweep -> 54). Rank 1 is
insufficient exactly when `E_delta(Qp)/E_1` is not cyclic.

**The triage.** That condition depends only on `(p, delta)`, not on which twist
in the class you take, since all `d` in one class give `Qp`-isomorphic curves.
So one local computation per `(p, delta)` — four per prime, no point search —
decides the path. `procyclic()` implements it (exact at good reduction via
`ellgroup`; conservative at additive primes, so the cheap set is never
overstated).

| `f` | `(p,delta)` pairs | procyclic, cheap path | rank >= 2 mandatory |
|---|---|---|---|
| `x^3+x+1`, `p < 200` | 180 | 158 (88%) | 22, at `p = 31,47,67,131,139,149,173` |
| `x^3-2`, `p < 100`   | 96  | 78 (81%)  | 18, at `p = 3,7,13,19,31,37,43,61,67,73,79,97` |

That second list is exactly `{3}` together with `p = 1 mod 3`. No prime
`2 mod 3` appears. This is why a rank-1-only search on the CM curve looks like
it "fails for `p = 1 mod 3`": CM by `Z[zeta_3]` is what makes those classes
non-procyclic. It is a blind spot of the method, not a fact about the surface —
see §5.2 of the document.

**Result.** `hybrid()` settles all 45 odd primes below 200 in one pass in ~5 s,
including `p = 131, 149`, which the pure-descent path only reached via separate
targeted `hunt()` calls. Of the 180 pairs, 134 are resolved straight from the
sweep with no descent at all.

## S-adic density

`X(Q_S)` is the product of the `X(Q_p)` for `p` in `S`. Density of `E_d(Q)` in
each factor separately does **not** imply density in the product; the two
agree when the factors have coprime supernatural order, i.e. when the integers
`p*M_p(d)` are pairwise coprime.

```
read("sadic.gp");
E = ellinit([1,1]);
densegroup(E, [[0,1]], 5);        /* 1: dense at 5            */
densegroup(E, [[0,1]], 7);        /* 1: dense at 7            */
coprimeS(E, [5,7]);               /* 0: M_5 = 9, M_7 = 5      */
denseS(E, [[0,1]], [5,7]);        /* -1: factorwise only      */
reportS(1, 1, [5,7], 4000);       /* 12 of 16 tuples witnessed */
```

The counterexample is `f = x^3+x+1`, `d = 1`, `S = {5,7}`: both quotients
`E(Q_5)/5` and `E(Q_7)/5` are `F_5`, so the product has `F_5 x F_5` as a
quotient, onto which the cyclic group `E(Q)` cannot surject.

### Deciding density in the product directly

`E_2(Q_p)` sits inside the Frattini subgroup of `E_d(Q_p)` for odd `p`, and
Frattini subgroups multiply over products, so `E_d(Q)` is dense in the product
iff it surjects onto the **finite** group `B = prod_p E_d(Q_p)/E_2(Q_p)` of
order `prod_p p*M_p`. That needs no coprimality, and makes density a finite
check:

```
read("sadic.gp");
Border(ellinit([1,1]), [5,7]);          /* 1575                            */
denseprod(ellinit([1,1]), [[0,1]], [5,7]);   /* 0: rank 1 cannot generate  */
reportSprod(1, 1, [5,7],  4000);        /* 16/16 tuples: X(Q) dense in X(Q_S) */
reportSprod(1, 1, [3,5,7], 6000);       /* 46/64 -- a starved search, see below */
```

Finding one witness per tuple proves density outright, with no hypothesis.
Only the converse -- reading a failed search as genuine failure -- needs
`E_delta(Q_S)` to be topologically 2-generated, which is a local check and is
strictly weaker than the coprimality condition above.

### Enumerating by tuple, not by size

The 18 tuples the uniform sweep misses at `S = {3,5,7}` are starved, not
obstructed. A tuple fixes the parity of `v_p(d)` at each place, so the
odd-valuation places force a divisor `P = prod{p : v_p(d) odd}` of `d`; the
eight all-odd tuples need `105 | d`, and `|d| <= 6000` offers those eight a
total of 38 candidates -- while the tuple `(1,1,1)` gets the whole squarefree
range. All 18 missed tuples have `P > 1`.

`reportSprodtuples` walks the same squarefree `d` ordered by the cofactor
instead, `d = +-P*m` with `m` squarefree and coprime to `S`, so every tuple
gets a comparable supply:

```
read("sadic.gp");
reportSprodtuples(1, 1, [5,7],   20000, 1500);   /* 16/16 tuples, 0.3 s */
reportSprodtuples(1, 1, [3,5,7], 20000, 1500);   /* 64/64 tuples, ~17 s */
```

**All 64 tuples are witnessed**, the hardest at `d = 31290`, so `X(Q)` is dense
in `X(Q_S)` for `S = {3,5,7}` -- again with no hypothesis, since every tuple is
settled by a single full twist. Raw output, with the witness for each tuple, is
in `results/sadic-pertuple-S3-5-7.txt` and `results/sadic-pertuple-S5-7.txt`;
both witness lists are tabulated in the document, §2.2.1.

Raw `|d|` is the wrong scale to judge that list by. The median witness is
`|d| = 1335`, but the median *cofactor* `|d|/P` is 131 over the eight
unramified tuples and 166 over the other 56 -- indistinguishable. The spread in
`|d|` is the forced divisor and nothing else.

Two local filters do the pruning, in increasing order of price: a twist with
fewer than `g` independent points cannot surject onto `B_d`, and density in the
product implies density in each factor, so three `densegroup` calls screen for
each expensive `denseprod`. Re-verifying every witness through the unfiltered
`twistdata` path reproduces all 64, and 24 of them are confirmed independently
by the coprimality criterion `(*)`.

### Triage before searching

`g`, the minimal number of topological generators of the arena
`prod_p E^delta(Q_p)`, is local data and needs no point search. When `g <= 2`
partial twists are useless and `denseprod` settles the class outright; only
when `g >= 3` can a union of partial reaches do something a single twist
cannot.

```
read("sadic.gp");
gexactS(1, 1, 1, [5,7]);        /* 2 -- exact                          */
gexactS(1, 1, 1, [11,13,17]);   /* 3                                   */
gtop(1, 1, 1, [11,13,17]);      /* 3 -- the upper bound, here tight    */
triage(1, 1, [11,13,17], 600);  /* all 48 realised tuples have g = 3   */
```

**Use `gexactS`, not `gtop`.** `gtop` bounds `dim E(Q_p)[l]` by `v_l(M_p)`,
which reads `l | M_p` as `l`-torsion -- but `M_p = #E(Q_p)/E_1` counts a
*quotient*, and `E(Q_p) = Z_p` with no torsion at all already has `M_p = 9`.
The bound is loose in exactly the direction that misleads: `triage` reports
`g = 3` for 2 of the 16 tuples at `S = {5,7}` and 24 of the 64 at
`S = {3,5,7}`, all of which in truth have `g <= 2` -- consistent with every
one of them being settled by a full twist above. It is tight at
`S = {11,13,17}`, where all 64 tuples really do have `g = 3`. `gexactS`
computes the dimension properly and is still purely local: at good reduction
with `l != p` reduction is an isomorphism on `l`-torsion, so `ellgroup`
settles it; only `l = p`, or `l` dividing the component group at additive
reduction, needs a division polynomial.

Beware also that `g >= 3` does **not** say no full twist exists, only that
none is forced.

## The ledger

At level 1 and good reduction the arena is just `prod_p Etilde(F_p)`, so a
reach is a bitmap and the star test is bitwise. For `f = x^3+x+1`,
`S = {11,13,17}` the arena has order `14*18*18 = 4536` with `g = 3`:

```
read("ledger.gp");
L = runtally(1, 1, 1, [11,13,17], 2000, 0);
```

No twist found is full, yet the ledger closes: it stabilises at exactly seven
maximal reaches, each of index 2, which is the number of index-2 subgroups of
`(Z/2)^3`. Every pair generates a subgroup of 2-rank at most 2, so lies in
some hyperplane -- partial patches doing together what no single twist can.
Deficiency falls 74.8% -> 28.1% -> 0 at 115 twists (`|d|` about 1900).

### Why that is not a proof: it is a tally, not a ledger

The point is worth stating plainly. A tally stores each reach as its
image at one fixed level `n`, and the preimage of that image is an
**over**-approximation of the reach. Coverage computed from
over-approximations is only ever a *necessary* condition, and level `n+1` can
always destroy what level `n` certified. So no amount of computing at a fixed
level, or at every level in turn, yields a proof.

The fix is what turns a tally into a ledger: make the level part of each entry. A ledger entry is
`(d, n_d, Rbar)` subject to `R(d)` containing `ker_{n_d}`, which is a
*certificate of exactness* -- the reach then equals the preimage of `Rbar`, so
the finite datum is exact rather than approximate. Granularity is detected by
computing the index at successive levels and stopping when it repeats.

Then (§2.3.2) if a **finite** graded ledger covers at `N = max n_d`, the
covering pulls back on the nose and density follows: a real proof from finite
data. Pruning still works across grades, and in the useful direction --
coarser entries are *larger* subgroups, so they absorb finer ones.

### Places versus layers

Two families of primes, kept apart deliberately. The **places** `p in S` are
where we localise; the **layer primes** `l` are those dividing `#G`. Picture
the arena as a grid, places as columns and layer primes as rows, cell `(p,l)`
being the `l`-primary part of `E^delta(Q_p)`.

Off-diagonal cells (`l != p`) are finite; only the diagonal ones are infinite;
and `ker_n` is pro-`p` at each place, so it lies entirely on the diagonal.
**Granularity is therefore diagonal**: a vector `(n_p)`, one per place, with
`n_p = 1` exactly when the reach's `p`-layer is all of `G_{p,p}`.

When no place of `S` divides `M_q` for another place `q` (true for
`S = {11,13,17}`, where `M = (14,18,18)`), the `p`-layer is just `E_1(Q_p)`,
and `n_p = 1` is the old condition (ii). So it is directly testable:

```
read("ledger.gp");
gran1(twistdata(1,1,53)[1], twistdata(1,1,53)[2], [11,13,17]);   /* 1 */
L = rungraded(1, 1, 1, [11,13,17], 2000, 0);
```

Admitting only granularity-1 twists, the ledger still closes: 102 of 119
twists admitted, seven maximal reaches, deficiency 0. Every reach then
contains `ker_1`, so the termination theorem applies with `N = 1` and
**density in that component of `X(Q_S)` is proved** -- with no full twist
anywhere.

## The sweep over all 64 tuples

```
read("ledger.gp");
sweepgraded(1, 1, [11,13,17], 5000);     /* uniform in |d|   */
sweeptuples(1, 1, [11,13,17], 2000, 250); /* per tuple        */
```

`sweepgraded` dispatches each twist to the ledger of its own square-class
tuple. Sweeping squarefree `|d| <= 5000` realises 54 of the 64 tuples and
**proves five dense**, each with a certificate naming its twists:

| tuple | `d0` | `N` | twists | ledger | outcome |
|---|---|---|---|---|---|
| `(1,1,1)`  | 1   | 4536 | 222 | 7 | covered -- seven hyperplanes |
| `(1,1,u)`  | 3   | 4536 | 206 | 1 | covered -- full twist `d = 335` |
| `(u,1,1)`  | -1  | 3240 | 177 | 7 | covered -- seven hyperplanes |
| `(u,1,u)`  | 74  | 3240 | 185 | 7 | covered -- seven hyperplanes |
| `(11,u,u)` | 11  | 3960 | 19  | 1 | covered -- full twist `d = 4279` |

`(11,u,u)` is the one to watch: `11 | d0`, so `E^d0` is additive at 11 and the
old `F_p` arena did not exist there at all.

The 49 failures are supply, not obstruction. A tuple fixes the parity of
`v_p(d)` at each place, so the odd-valuation places force a divisor
`P = prod{p : v_p(d) odd}` of `d`; the all-odd tuple needs `2431 | d`, and a
uniform sweep of `|d| <= 5000` gives it two candidates. `sweeptuples`
enumerates `d = +-P*m` with `m` squarefree and coprime to `S` instead, so
every tuple gets a comparable supply, and stops a tuple once its ledger
closes. Four tuples were well supplied (116-149 twists) and still failed;
probing them shows they hold **five of the seven** index-2 hyperplanes, so
they are short of supply too, not obstructed -- `(1,1,1)` itself needed 222
twists to collect all seven.

Run with `m <= 2000` and a cap of 250 candidates per tuple, `sweeptuples`
proves **13 of the 64** tuples dense -- eleven on a single full twist, and
`(1,1,1)` and `(1,13,u*17)` on seven hyperplanes. Document §2.3.6 lists the
witness `d` for all thirteen. All eleven full twists have Mordell-Weil rank
*exactly* 3, the least `g = 3` permits, so the mechanism is not that those
tuples sit under twists of unusually large rank -- only that the search met a
rank-3 twist there. The two enumerations are
complementary rather than nested: the uniform sweep still proves `(u,1,1)`
and `(u,1,u)`, which the capped per-tuple run misses (102 and 98 twists with
points, against 177 and 185). Together they settle **15 of the 64 tuples**,
and none of the other 49 looks obstructed -- their ledgers hold 3 to 8
maximal reaches, concentrated at 4 and 5.

Where the time goes: of 14134 candidates tried, only 2928 (**21%**) have a
rational point at all, and 88% of those are admitted. Four fifths of the run
is an `ellrank` returning "rank 0, no torsion". Neither the ledger nor the
star test is the bottleneck -- finding points is. The fix is the `t0`
bucketing described above: enumerate `t0` and group by the squarefree part of
`f(t0)`, so every candidate carries a point by construction, with the tuple
imposed as a congruence `P | f(t0)` rather than discovered by a rank
computation.

Not yet implemented for deeper granularity. The obstacle is the index
computation, not the bookkeeping: the `O(N)` triangular method is already impractical at level 2
for three places (`1.1e7`) and hopeless at level 3 (`2.7e10`). Granularities
need an `l`-primary rewrite, which is also the right representation since
closures in a profinite abelian group decompose over primes.

## The survey over thirty surfaces

`kummer-survey.typ` runs the same test over the thirty smallest surfaces and
reports p-adic density for every `p <= 200`.

**Which surfaces.** `X` sees `f` only through `f -> c^-3 f(cx+mu)`, so surfaces
are elliptic curves *up to quadratic twist* and the invariant to sort by is
`N_min`, the conductor of the minimal twist. Taken: all 28 surfaces with
`N_min <= 20` (complete, from Cremona via Sage), plus `27a1` and `496a` as
controls -- which are exactly the `x^3-2` and `x^3+x+1` of the main document,
so the two known answers are re-derived.

**Monic cubics, not depressed ones.** Depressing costs a shift `x -> x-a/3`,
cleared by `3^2, 3^3`, so coefficients blow up by `3^4` and `3^6`: curve 11a2
is `x^3-14x^2-31216x-1983614` but depresses to `x^3-281532x+57496282`. That is
an artefact of the model. `survey.gp` carries `F=[a,b,c]` throughout; the twist
is `Y^2 = U^3 + a d U^2 + b d^2 U + c d^3` with `(U,Y) = (du, d^2 v)`, and
everything downstream is unchanged.

**Enumerate by cofactor, not by |d|.** The classes `[p]`, `[u p]` force `p | d`,
so a bound on `|d|` starves them ~`p/2` to 1. Writing `d = +-P*m` with `P = p`
for those two classes and bounding the *cofactor* `m` fixes it -- the same
lesson as the S-adic tuple sweep, at a single place. Under the old bound 11a3
reports 44/45 and the shortfall looks like an obstruction; under the new one,
45/45.

```
gp -q -s 4000000000 survey.gp < /dev/null
runsurface("11a3", [1,-1,1], 1500, 50, 200, 60, 3000, 300);   /* ~6 s */
```

**Result.** 5392 of the 5400 (odd prime, square class) pairs carry a witness;
22 of the 30 surfaces are witnessed at every odd `p <= 200`; at `p = 2`, 239 of
240 classes, so 21 are witnessed at every `p <= 200`. Total 321 s for the
main run, about 21.5 min including the follow-ups.

**The eight that fail** sit on eight distinct surfaces, each at exactly one
prime, and that prime always divides the conductor. All have `g = 2` -- so a
rank->=2 twist is mandatory -- and between 96 and 167 rank->=2 twists were
tested in the failing class without a witness, over a range then extended
6.7-fold. Seven of the eight are *non-CM*, which says the CM mechanism of
§5.1 explains which class fails for `x^3-2` but is not the general reason a
class fails; the common signature is multiplicative reduction with
`gcd(c_p, p-a_p) > 1`, making `E_delta(Q_p)/E_1` have `(Z/l)^2` as a quotient.

**All eight are now proved non-dense**, not merely unwitnessed: seven by a
twisted pairing plus reciprocity (see *Twisted pairings at non-CM surfaces*
below, and document §7) and the eighth -- `x^3-2` at `p = 3` -- by the argument
of §5.1.5, which was what put it on the list. So the failure of the search to
find a witness was not a limit of the search.

```
deephunt([-11,-1,-83], 11, 1, 1, 20000);       /* 11a1, p=11, class [u] */
gloc([10,105,-116], 1, 7);                     /* 2 -- rank 1 is useless */
rankaudit("14a1", [10,105,-116], 7, 0, 3000);  /* 144 rank->=2 twists, none dense */
```

### The ledger at one odd place (document §3.3.1)

The ledger cannot help *prove* density at a single odd place, and it is worth
saying why: `G = E_delta(Q_p)` is topologically 2-generated for `p` odd, so
feeding the star test a pair of topological generators forces some `H_d` to
contain both, hence to be all of `G`. **The star test closes iff a full twist
exists.** That is the `g <= 2` regime; the ledger earns its keep at
`S = {11,13,17}` precisely because `g = 3` there, so no pair can generate.

What it does give is a *measurement*: are the reaches spread over all the lines
of `A/lA = (Z/l)^2` (a pairing) or confined to one (a linear functional)? Here
`A = E_delta(Q_p)/E_1` and `l` is the layer with `dim A/lA = 2`.

```
ledgerp("15a1", [-10,-127,136], 5, 0, 2, 2000, 30);
```

`27a1` is excluded (non-density is proved there). For the other seven:

| surface | p | class | l | twists | reach = (Z/l)^2 | lines seen | star deficiency |
|---|---|---|---|---|---|---|---|
| 11a1 | 11 | [u] | 5 | 584 | **0** | 6 of 6 | 480/625 |
| 14a1 | 7 | [1] | 3 | 1062 | **0** | 4 of 4 | 48/81 |
| 14a2 | 7 | [1] | 3 | 1062 | **0** | 4 of 4 | 48/81 |
| 15a1 | 5 | [1] | 2 | 1012 | **0** | 3 of 3 | 6/16 |
| 15a4 | 5 | [1] | 2 | 1012 | **0** | 3 of 3 | 6/16 |
| 17a1 | 17 | [1] | 2 | 1150 | **0** | 3 of 3 | 6/16 |
| 19a1 | 19 | [u] | 3 | 546 | **0** | 4 of 4 | 48/81 |

If the reaches are exactly the `l+1` lines and nothing more, the uncoverable
pairs are the spanning ones, `(l^2-1)(l^2-l)` out of `l^4` -- that is 6/16,
48/81, 480/625. **Every deficiency is exactly that**, so each ledger has
collected every line and is missing only the full reach, i.e. the full twist.

Every line occurs and roughly equally often (11a1: 92/97/97/103/77/95 over the
six lines of `(Z/5)^2`), so no linear functional vanishes on the rational
points. That is the *pairing* signature -- the same one §5.5.6 finds for
`x^3+x`, where §5.5.4 then proves the pairing is `(x(P),x(Q))_v`. So the seven
non-CM cases look like instances of the same mechanism; constructing it for
them is not done.

`14a2` is the informative one: `M = 36`, and at `l = 2` its ledger **closes**
(252 of 1062 twists reach all of `(Z/2)^2`, deficiency 0/16) while at `l = 3` it
behaves like the rest. The class is unwitnessed because of the 3-layer alone --
sharper than "no full twist found", and it says where a proof would have to work.

**`p = 2`.** When `f` splits completely over `Q_2` (ten of the thirty),
`E_delta(Q_2) = Z_2 x (Z/2)^2` for *every* delta, needs three generators, and
the Kummer surface only supplies pairs -- the criterion stops being an
equivalence. Seven of those ten fall short at `m <= 300`; deeper passes recover
all but `17a4`, class `[7]`, always on a rank-2 or rank-3 twist.

**Model-independence.** Rewriting the surface permutes the class labels by the
class of `c`. Checked on 14a1 in three models: the failing class moves from
`[1]` to `[u]` and back, exactly as `(3|7) = (-1|7) = -1` predicts -- which also
re-runs the case through the original depressed code path.

Rebuild the document with `typst compile kummer-survey.typ` after
`python3 survey-tables.py`.

## The CM batch

`kummer-survey.typ` §5. The first batch was chosen by size and came out non-CM
throughout, leaving `x^3-2` -- the one surface known to be defective -- with no
company.

**Which CM surfaces there are.** Thirteen CM `j`-invariants; for `j != 0, 1728`
each is a *single* quadratic-twist class, hence one surface -- eleven rigid
points, `D = -7,-8,-11,-12,-16,-19,-27,-28,-43,-67,-163`. For `j = 0` and
`j = 1728` the twisting is sextic and quartic, so one `j` spreads over infinitely
many surfaces:

    D = -3 :  f = x^3 + B,   surfaces <-> B > 0 cubefree
    D = -4 :  f = x^3 + A x, surfaces <-> A != 0 squarefree

`x^3-2` is the member `B = 2` of the first. That is the point: the family can be
*walked*, so "is the defect about the CM order or about that surface?" is
decidable.

```
docker run --rm --platform linux/amd64 -v "$PWD":/work -w /work \
       sagemath/sagemath:latest sage cm-surfaces.sage    /* the list */
gp -q -s 4000000000 survey.gp < /dev/null
sexticscan(100, 3, 3000);      /* 85 surfaces at p = 3, 72 s  */
quarticscan(60, 1500);         /* 74 surfaces at p = 2, 10 s  */
```

**Result.** 43 CM surfaces over all `p <= 200` (334 s): of 7740 (odd p, class)
pairs all but one are witnessed, of 344 classes at `p = 2` all but two. *Two*
surfaces are defective: `x^3+2` at `p = 3` (the quadratic twist of `x^3-2`, so
the known one) and **`x^3+x` at `p = 2`, which is new**. The deepest witness
anywhere in the project is `D = -163` at `p = 199`, `d = 1949802`, rank 3.

**Walking the families settles it.** Of the 85 sextic surfaces `B <= 100`,
exactly one is defective at 3: `B = 2`. Of the 74 quartic surfaces `|A| <= 60`,
exactly one is defective at 2: `A = 1`. So the defect does not spread -- it is a
fact about the individual surface, not about CM. Combined with the seven non-CM
defects of the first batch, CM is neither necessary nor sufficient; what the
defective cases share is only `g = 2` at the failing place.

**Why `x^3+x` at `p = 2` counts.** `f = x(x^2+1)` has exactly one root in `Q_2`,
so `dim E[2](Q_2) = 1`, `E_delta(Q_2)` is 2-generated and the necessity argument
applies -- the first `p = 2` shortfall in either batch that is evidence of
non-density rather than a limitation of the criterion. The local structure picks
the classes out on its own, exactly as `find3` does at `p = 3`: the defective
classes are the two with `M_2 = 16`, `c_2 = 4` and rational 4-torsion, against
`M_2 = 8`, `c_2 = 2`, 2-torsion for the two witnessed even classes. They are
`delta = 2` and `delta = -2`, one square class seen twice, since `E_d = E_{-d}`
for `f = x^3 + Ax`.

### The obstruction at the place 2, elementary form (the §5.1.5 question)

§5.1.5 of the main document identifies a *mechanism* -- a Galois-twisted Tate
pairing plus reciprocity. The same mechanism works for `x^3+x` with the descent
done by 2 instead of 3, and it can be written down with **no cohomology at
all**: the twisted pairing is the
quadratic Hilbert symbol of two x-coordinates, and the only imported theorem is
Hilbert reciprocity (equivalently quadratic reciprocity). Document §5.5.

For `E_d : y^2 = x(x^2+d^2)`, `T = (0,0)`, set `c(O) = 1`, `c(T) = d^2`,
`c(x,y) = x`, a map to `K^x` modulo squares.

**Lemma 1 (Vieta).** `c` is a homomorphism. Three collinear points have
x-coordinates the roots of `x^3 - lam^2 x^2 + (d^2 - 2 lam nu) x - nu^2`, whose
product is `nu^2`, a square; if `nu = 0` the line passes through `T` and the
other two x's multiply to `d^2`. *(7780 triples tested, 0 failures.)*

**Lemma 2 (norm lemma).** `x(x^2+d^2) = y^2` makes `x = x^2+d^2` mod squares, and
`x^2+d^2 = N_{K(i)/K}(x+di)`. So `c(P)` is a **norm from `Q(i)`**, i.e.
`(c(P),-1)_v = 1` everywhere. For general `y^2 = x(x^2+ax+b)` the field is
`Q(sqrt(a^2-4b))`; the twist of `x^3+Ax` has `a^2-4b = -4Ad^2 = -A`, so it is
`Q(i)` **exactly when A = 1** -- the defective member. That is the answer to
"why exactly A = 1", the analogue of §5.1.1.

**Notation: two different 2's.** The *level* of the descent is 2 (`E[2]`, square
classes) and the *place* that survives reciprocity is also `v = 2`. These are the
two families of primes §2.3 keeps apart -- a layer and a place -- and here they
coincide numerically, so they must be separated in the notation. Below the level
is never given a letter (always the literal 2); `v` is a place of `Q`, and `q` a
finite *odd* place. Nothing here is called `l`.

**The symbol vanishes away from 2.** At `v = inf`, `x >= 0` on real points. At
odd `q` not dividing `2d`, `v_q(c(P))` is even (from `b^2 = a(a^2+d^2e^4)` and
coprimality), and two units pair trivially. At odd `q | d`:

> **Lemma A.** If `k = v_q(x(P)) != e = v_q(d)` then `c(P) = 1`: for `k < e`,
> `x^2+d^2 = x^2(1+(d/x)^2)` with the second factor a 1-unit hence a square
> (`q` odd); for `k > e` symmetrically. Either way `x(x^2+d^2) = y^2` makes `x`
> a square.
>
> **Lemma B.** `S = c(E_d(Q_q))` has order at most 2. By Lemma 1 `S` is a
> subgroup of a group of order 4, and by Lemma A every non-identity element has
> valuation `= e mod 2`. If `e` is even `S` is inside the units; if `e` is odd
> and `S` held both `q` and `qu`, it would hold `u` -- even valuation,
> contradiction.

Then `(g^i,g^j)_q = (g,-1)_q^{ij} = 1` by Lemma 2. *(Lemma A: 3345 points, 0
exceptions. Lemma B: 1851 bad odd places, `#S` always 1 or 2.)*

**The image at 2 is a mod-8 computation.** For `d = 2m`, `m` odd, writing
`x = 2^k u`: `k < 0` or `k >= 3` give class 1; `k = 0` needs `x = 5 mod 8`;
`k = 2` needs `u = 5 mod 8`; and `k = 1` needs `uw = 1 mod 8` with
`w = (u^2+m^2)/2`. Only `k = 1` sees `m`:

| m mod 8 | k=1 solvable? | c(E_d(Q_2)) |
|---|---|---|
| ±1 | yes: u = 1 (class 2) and u = 5 (class 10) | **{1,5,2,10}** |
| ±3 | no | {1,5} |

(and `{1}` for `d` odd). A scan of 324 squarefree `d` reproduces this exactly.
The Hilbert symbol on `{1,5,2,10}` has `+` on the diagonal (Lemma 2 again) and
`(2,5)_2 = -1`: non-degenerate, so its isotropic subgroups have order <= 2.

> **Theorem.** Let `d = 2m` be squarefree with `m = ±1 mod 8` (square class `[2]`
> or `[14]`). Then `E_d(Q)` is not dense in `E_d(Q_2)`, hence `X(Q)` is not
> dense in `X(Q_2)`.
>
> *Proof.* By the above and Hilbert reciprocity, `(c(P),c(Q))_2 = +1` for all
> rational `P,Q`, so `H = c(E_d(Q))` is an isotropic subgroup of `{1,5,2,10}`
> and `#H <= 2`. But `c` is *locally constant* on `E_d(Q_2)` (squares are open)
> and surjects onto `{1,5,2,10}`. So `U = {P : c(P) not in H}` is open,
> non-empty, and contains no rational point nor any limit of them.

No cohomology, no Selmer groups, no Frattini subgroups, no Nakayama: Vieta, a
norm identity, three valuation arguments, a computation mod 8, and reciprocity.
The proof covers **all** twists in the two classes at once.

```
homtest([0,1,0], 130, 6);          /* Lemma 1  */
lemmaA([0,1,0], 130, 5, 30, 5, 60); cyctest([0,1,0], 2, 1200, 30);
img2table([0,1,0], 200);           /* the mod-8 table */
```

Checks: across 190 twists in the two defective classes, all 950 rational pairs
give `(c(P),c(Q))_2 = +1`. The control `x^3+2x` has `a^2-4b = -2`, so Lemma 2
gives only `(c(P),-2)_v = 1`, its diagonal is not `+`, reciprocity localises
nothing -- 132/395 and 214/464 rational pairs have a *non-trivial* symbol at 2,
and all four of its classes are witnessed.

Document §5.5.5 gives the same argument cohomologically, as completely as it
can be given: Kummer/Tate/reciprocity as the standard input, `E[2]`
indecomposable but `End_G` non-scalar, `N = iota o n o pi`, and then

> **Lemma C.** `C_1^perp = C_1` (the Weil pairing is alternating, so
> `C_1 <= C_1^perp`, and perfect, so dims force equality), whence `e_2` induces
> a *perfect* `ebar` on `(E[2]/C_1) x C_1`.
>
> **Lemma D.** Cup product is functorial in the coefficient pairing:
> `f_*(a) cup_phi g_*(b) = a cup_{phi o (f x g)} b`, proved on cochains -- the
> two cocycles are *equal*, not just cohomologous.
>
> **Lemma E (the adjunction).** `<a, iota_* z> = <pi_* a, z>_ebar`. The whole
> content is one identity *between pairings of modules*,
> `e_2 o (id x iota) = ebar o (pi x id)`, which holds because `ebar` is *defined*
> by `ebar(xbar,t) = e_2(x,t)` -- legitimate exactly because `C_1` is isotropic.
> Lemma D applied twice then does it.

So the isotropy of `C_1` is not a convenience: it is what makes `ebar` exist and
therefore what makes the adjunction true. The section also identifies
`pi_* o delta_v` with the `phi-hat`-descent map via the map of short exact
sequences (`phi-hat o phi = [2]`, and `phi` induces `E[2]/C_1 = ker phi-hat`),
notes that `N` is self-adjoint for `e_2` so `beta` is symmetric, and ends with
the dictionary table. Read that way the argument goes further than §5.1.5 could at
level 3 on both points it leaves open -- the alternating property, which there
needs decomposability of `E[3]`, is Lemma 2 here; and the local input
`beta_3 != 0`, which there needs a cubic norm-residue symbol at a wildly
ramified place, is here the mod-8 table. That is exactly because at level 2 the
norm-residue symbol is *quadratic*, while at level 3 it is cubic.

## Twisted pairings at non-CM surfaces (document §7; the local technique is collected in §6)

**All eight classes of §3.3 are now settled** -- seven by a twisted pairing plus
reciprocity, and the eighth by the direct non-density proof that put it on the
list in the first place. Each theorem covers its whole square class with no
extra condition, which is what the surface-level conclusion needs (document
§6.8), and no step rests on a sample: what is computed is computed over a
complete set of square classes, with the sampled points shown to generate the
relevant quotient.

**What the theorems actually establish** (corrected 2026-08-21): the conclusion
that carries to the surface is not "`E_d(Q)` is not dense in `E_d(Q_p)`" but the
*pair* statement `beta_p(P,Q) = 0` for all `P,Q` in `E_d(Q)` -- equivalently,
the image of `E_d(Q)` in `E_d(Q_p)/l` is a proper subspace, so any two of its
elements are `F_l`-multiples of one another. The group statement is true but too
weak: the preimage of a proper subspace is an *open* subgroup, hence not nowhere
dense, so no Baire argument applies over the countably many `d` in the class --
and since a 2-dimensional `W_p` is the union of its `l+1` lines, as few as `l+1`
twists can fill the local class. The pair statement is uniform in `d` and cuts
out a proper open subset of `X(Q_p)` directly. Document §6.8 now carries this as
a lemma; the earlier Baire argument there was wrong.

| case | l | critical p | E[l] | wild place v = l | status |
|---|---|---|---|---|---|
| `x^3-2` (§5.1.5) | 3 | 3 -- **wild** | decomposable | *is* the critical place | complete |
| `x^3+x` (§5.5.4) | 2 | 2 -- **wild** | **indecomposable** | *is* the critical place | complete |
| `15a1` (§7.1) | 2 | 5 | split over Q | 8 square classes at 2 | complete |
| `14a1` (§7.2) | 3 | 7 -- tame | decomposable | good ordinary, and collapse | complete |
| `15a4` (§7.3) | 2 | 5 | **indecomposable** | 8 square classes at 2 | complete |
| `11a1` (§7.4) | 5 | 11 | decomposable | good ordinary | complete |
| `14a2`, `19a1` (§7.5) | 3 | 7, 19 -- tame | decomposable | good ordinary, and collapse | complete |
| `17a1` (§7.6) | 2 | 17 -- tame | **indecomposable** | 8 square classes at 2 | complete |

The local technique they share is collected in document §6: the criterion at a
place and its indecomposable variant, which places cannot contribute, the two
mechanisms at the wild place `v = l`, and the reduction of "all twists" to a
bounded number of square classes.

### 15a1 at p = 5: level 2, f split (document §7.1)

§3.3.1 found all seven open classes carry the pairing signature; this constructs
the pairing in one of them, at a **non-CM** surface -- so the mechanism is not
about complex multiplication.

`15a1` has `f = (x-17)(x-1)(x+8)`, so **f splits over Q**: `E[2]` is the trivial
Galois module and `End_G(E[2]) = M_2(F_2)`. In contrast with `x^3+x`, where the
twisting endomorphism was essentially unique, here it is not scarce at all --
it has to be **chosen**. Every candidate has the shape

    beta_n(P,Q) = prod_{i,j in {1,2}} ( c_i(P), c_j(Q) )_v ^ n_ij,
    c_i(P) = x(P) - d e_i,   c1 c2 c3 = y^2,   n in M_2(F_2)

-- 16 of them, the untwisted Tate pairing being the antidiagonal, which dies on
the Lagrangian `W_v`. Two conditions pin `n` down:

* **beta_5 must not vanish on W_5.** Sampling `E_d(Q_5)` gives `c2` always a
  square, so `c3 = c1` there, while `c1` is *onto* `Q_5^x` mod squares. Every
  `beta_n` collapses on `W_5` to `(c1(P),c1(Q))_5 ^ n_11`, so `n_11 = 1`.
* **beta_v must vanish at every v != 5.** Testing all 16 on 7318 rational pairs
  from 150 twists leaves four, two of them with `n_11 = 1`.

The survivors are transposes of each other, and their product is the untwisted
pairing (so beta is symmetric on `W_v`):

    beta_v(P,Q) = ( c1(P), c3(Q) )_v = ( x(P) - 17d, x(Q) + 8d )_v

```
loc5(1);            /* c2 is a square, c1 is onto */
search();           /* the 16 candidates */
sweepv(400);        /* local vanishing at 2, 3 and q | d */
```

**Local statements.** `beta_5` is alternating (`(c1,c3) = (c2,d)(c3,-1)` for one
point, and at 5 both die: `c2` is a square there and `-1` is a square in `Q_5`
since `5 = 1 mod 4`) and non-zero (`c1` onto, `(5,u)_5 = -1`) -- so symplectic on
the 2-dimensional `W_5`. `beta_inf = 1`: for `d > 0` real points have `x >= -8d`
so `c3 >= 0`; for `d < 0`, `x >= 17d` so `c1 >= 0`. For `q` outside `{2,3,5}` and
prime to `d`, the `c_i` differ by `16d, 25d, 9d`, so at most one is a non-unit
and its valuation is even -- both arguments are units and the symbol is 1. *That
the root differences 16, 25, 9 are perfect squares is what confines the rest to
2, 3, 5 and `q | d`; 5 survives because `e1 - e3 = 25`.*

The places 2, 3 and `q | d` were long **verified rather than proved** -- 640 of
640 such places over 202 twists. They are now proved, and split into a finite
part and a lemma. `E_d/Q_v` depends on `d` only through its class in `Q_v^x`
modulo squares, and the class at 5 constrains neither the class at 2 (eight) nor
at 3 (four), so `localimg.gp` runs all twelve, checks in each that the sampled
points **generate** the relevant quotient, and evaluates every Hilbert symbol
between the image of `c_1` and that of `c_3`. All trivial. The same runs give the
critical place: at `v = 5` the image of `c_1` is all four classes.

For `q | d` with `q` odd, infinitely many primes occur and no finite check
reaches them. **Lemma C**: writing `d = q d'`,

    c_1(E_d(Q_q)) in {1, -q d'},   c_3(E_d(Q_q)) in {1, q d'}   mod squares,

so the four resulting symbols are trivial -- the last by Steinberg, `(a,-a)_q =
1`. The proof runs on the valuation `k` of `x(P)`: `k <= 0` makes every `c_i` a
square; `k = 1` gives exactly one `lambda_i` of odd valuation and three cases,
where *the root differences 16, 25, 9 being squares* is what makes the classes
come out as `d'` up to sign; `k >= 2` is empty because `prod c_i = 136 d` has odd
valuation when `q` does not divide 136. `q = 17` is left over -- `17 = e_1` makes
that last case non-vacuous -- but `17 | d` forces `v_17(d)` odd, so only two
classes occur and both are run.

That is the third job the squareness of the root differences does, after
confining the bad places and making `A` unramified on the Kummer surface
(document §8.4.2).

> *Theorem.* For every squarefree `d` in the class `[1]` of `Q_5`, `E_d(Q)` is
> not dense in `E_d(Q_5)`; hence `X(Q)` is not dense in `X(Q_5)`.

It also explains the §3.3.1 measurement -- the reaches spread over all three
lines 180/129/170 -- since an isotropic line of a symplectic form is exactly a
line forced to exist without being preferred. The selection step is what would
have to be redone for each of the other six classes; that is not done.

### 14a1 at p = 7: the same at level 3 (document §7.2)

The second construction runs at a different **level**. §3.3.1 puts the
obstruction for 14a1 at `l = 3`, so this needs 3-descent and cubic norm-residue
symbols -- the machinery §5.1.5 records as missing. It works anyway, because
**the critical place is 7, not 3**, and `7 = 1 mod 3`, so the symbol there is
*tame*.

**E[3] is decomposable.** `psi_3 = (x-2)(3x+1)(x^2+2x+13)`: two rational
subgroups, confirmed by `ellisomat` (two independent 3-isogenies). So §5.1.5's
hypothesis holds on the nose -- unlike `x^3+x`, where `E[2]` was indecomposable.
`C1 = <(2,2)> = Z/3`; the other subgroup has `x = -7/3` with `y` in `Q(sqrt(-3))`,
so `C2 = mu_3`. In 3-torsion normal form

    E : y^2 + 5xy + 7y = x^3,  T1 = (0,0);   E_d : Y^2 = 4X^3 + d(5X+7d)^2

and since the tangent at a point of order 3 meets E only there,
`div(tangent) = 3(T) - 3(O)`: the descent map is `c_T(P) = tangent_T(P)` modulo
**cubes**, and for `T1` the tangent is `y = 0`, so `c1(P) = y(P)`. With `phi` the
projection onto `C1`, `beta_v(P,Q) = -<c2(P), c1(Q)>_v`.

**The critical place is tame.** `7 = 1 mod 3` so `zeta_3` is in `Q_7`, both
kernels are rational there, `E[3](Q_7) = (Z/3)^2` and `dim W_7 = 2`. Sampling
`E(Q_7)` shows the image of `(c1,c2)` is the **diagonal**, so
`beta_7(P,Q) = -(c1(P),c1(Q))_7`, the tame cubic symbol on `Q_7^x/cubes`:
48 of 81 entries non-zero, diagonal all 0 (since `-1 = (-1)^3` is a cube). So
non-degenerate and alternating -- symplectic on `(Z/3)^2`, whose isotropic
subspaces are the four lines §3.3.1 counted at 120/126/139/118.

**Every other place, structurally.** beta is alternating at *every* place for
free: `E[3]` decomposable gives §5.1.5's argument verbatim (`0 = <dP,dP> =
2<a1,a2>` and 2 is invertible mod 3). So `beta_v = 0` whenever `dim W_v <= 1`,
and the analysis reduces to computing `dim W_v`. With
`C1^(d) = Z/3 (x) chi_d` and `C2^(d) = mu_3 (x) chi_d`,

    C1^(d)(Q_v) != 0  iff  d square in Q_v,    C2^(d)(Q_v) != 0  iff  -3d square

so both need `-3` square in `Q_v` -- false at 2 (`-3 = 5 mod 8`) and 3 (odd
valuation), true at 7 (`-3 = 4`). Hence `W_inf = 0` (3-divisible), good `v != 3`
by unramified isotropy, `dim W_2 <= 1`, `W_q = 0` for `q | d` (both `d` and
`-3d` have odd valuation), and only `v = 3` imposes a condition.

> *Theorem.* For **every** squarefree `d` in the class `[1]` of `Q_7`, `E_d(Q)`
> is not dense in `E_d(Q_7)`, hence `X(Q)` is not dense in `X(Q_7)`.

The hypothesis is vacuous on the class, and getting there took two corrections.
An earlier version carried the condition `E_d[3](Q_3) = 0`, covering 533 of the
1062 squarefree `d` with `|d| <= 2000`, and then drew a conclusion about `X`
anyway -- which document §6.8 explains is invalid. The condition then went in two
stages. `d` a square in `Q_3` forces `3` not to divide `d`, so `E_d = E_0` over
`Q_3` with good reduction, and `a_3 = -2` is prime to 3, so the reduction is
**ordinary** and document §6.5.1 applies. The remaining class, `3 | d` with
`d/3 = 2 mod 3`, is additive at 3, where instead the descent image *collapses*:
`alpha` is identically trivial, so `ker alpha` is all of `W_3`, `L_3` lies inside
a single `H_i`, and stability is trivial. Since `E_d/Q_3` depends only on the
class of `d` modulo squares, four local computations settle every twist.

### 15a4 at p = 5: the x^3+x template, verbatim (document §7.3)

The triage predicted this would follow §5.5.4, and it does -- shifting by the
rational root makes it literally the same shape.
`f = (x-1)(x^2+12x+612)` and `f(x+1) = x^3+14x^2+625x`, a shift with `c = 1`, so
the same surface:

    f = x(x^2 + 14x + 625),   E_d : y^2 = x(x^2 + 14dx + 625d^2),  T = (0,0)

with `c(P) = x(P)` and `c(T) = 625d^2`, a square. The point is

    x^2 + 14dx + 625d^2 = (x+7d)^2 + (24d)^2

-- a **sum of two squares**. So `a^2-4b = -2304d^2 = -1` mod squares, the
2-torsion field is `Q(i)`, Lemma 2 applies unchanged, and **beta is alternating
at every place**. Over the 202 twists in the class: Lemma 1 on 2444 triples,
Lemma 2 on 10036 evaluations, `beta_v(P,P) = 0` on 10036 -- zero failures each.

**The places.** `v = inf`: the quadratic is a sum of two squares hence positive,
so `x >= 0` on real points -- cleaner than `x^3+x`, no sign case-split.
`q` outside `{2,5}` and prime to `d`: a common factor of `a` and
`a^2+14dae^2+625d^2e^4` divides `625d^2`, so `v_q(c)` is even and both arguments
are units. *The bad places are only 2, 5 and `q | d` -- no analogue of the 3
that `x^3+x` had.* Odd `q | d`: Lemmas A and B verbatim (their proof only uses
that a 1-unit is a square at an odd place). `q = 2`: the 1-unit argument fails,
so the image is computed -- cyclic (`{1}`, `{1,5}` or `{1,10}`) in **all 202**
twists. `q = 5`: the image is **all four** classes, so the symbol is
non-degenerate, and alternating since `5 = 1 mod 4` makes `-1` a square.

| place | checks | beta_v != 0 | |
|---|---|---|---|
| q = 5 (critical) | 202 twists | **202** | symplectic on W_5 |
| q = 2 | 202 twists | 0 | image cyclic |
| q odd, q \| d | 288 places | 0 | Lemmas A, B |

```
homtest([14,625,0], 11, 6);  normtest([14,625,0], 11, 100);
nzbeta(cimagep([14,625,0], 11, 5, 40, 6, 50), 5);   /* 1 */
nzbeta(cimagep([14,625,0], 11, 2, 40, 8, 50), 2);   /* 0 */
```

> *Theorem.* For `f = x(x^2+14x+625)` and every squarefree `d` in the class `[1]`
> of `Q_5`, `E_d(Q)` is not dense in `E_d(Q_5)`; hence `X(Q)` is not dense in
> `X(Q_5)`.

Across 231 twists, all **1079** rational pairs give `(c(P),c(Q))_5 = +1`, as the
theorem requires; and §3.3.1's reaches for 15a4, spread 82/138/104 over the three
lines, are the isotropic lines of this symplectic form.

The theorem once said "modulo the local vanishing at 2". That step is now
exhaustive: the class of `d` at 2 is unconstrained by its class at 5, so all
**eight** square classes of `Q_2^x` are needed, and all eight give an image of
`c` of order at most 2 -- enough, beta being alternating. Generation is checked
modulo `E_3`, which works because `2E` contains `2E_2 = E_3`. One small
correction fell out: the earlier pass over 202 twists listed the possible images
as `{1}, {1,5}, {1,10}`; there is a fourth, `{1,2}`, occurring for `d` in the
class of 2 -- e.g. `d = 34`, which is a square in `Q_5` and so lies in the class
the theorem is about.

Indecomposable `E[2]` makes phi essentially unique (document §6.2), so no search
was needed at all -- against nine candidates for 15a1.

### 11a1 at p = 11, level 5 -- and no quintic symbol needed (document §7.4)

The triage flagged this as needing the **quintic** residue symbol at 11. It does
not: the proof needs `beta_11` to be *non-degenerate*, not to be *evaluated*, and
multiplicative reduction settles that for free.

Caveat first: the reduced monic cubic `x^3-11x^2-x-83` has `N = 704` and trivial
torsion -- it is a twist. Take `E_0 = 11a1 = [0,-1,1,-10,-20]`, `N = 11`, torsion
`Z/5 = <(5,5)>`, two 5-isogenies, so `E[5] = C_1 + C_2` with `C_1 = <T> = Z/5`
and `C_2 = mu_5`. The failing class is `[1]` at 11 in this parametrisation.

**W_11 is forced.** `11a1` has *split multiplicative* reduction at 11
(`a_11 = 1`, `I_5`, `c_11 = 5`), so over `Q_11` it is a Tate curve
`Q_11^x / q^Z` with `v(q) = 5`. For a Tate curve the Kummer cocycle of `[x]` is
`sigma -> sigma(y)/y` with `y^5` in `x q^Z`, which lies in `mu_5` for *every*
point -- so `W_11` sits inside `H^1(Q_11, mu_5)`. Both have dimension 2
(`11 = 1 mod 5`, so `zeta_5` is in `Q_11`), hence **`W_11 = H^1(Tate mu_5)`**.

The Tate `mu_5` is intrinsic: `[x] -> v(x) mod 5` is the map to the component
group, so `mu_5 = E[5] cap E^0`. If it were `C_2`, `phi` would kill `W_11`; if it
were `C_1`, `beta` would be the untwisted pairing, zero on the Lagrangian. So
everything turns on whether either global subgroup meets `E^0` -- and neither
does:

| subgroup | x-coords mod 11 | in E^0? |
|---|---|---|
| singular point | (5,5) -- **the 5-torsion point itself** | -- |
| `C_1 = <(5,5)>` | 5 and 16 = 5 | **no** |
| `C_2 : 5x^2+5x-29` | disc `605 = 5*11^2`, double root `-1/2 = 5` | **no** |

So the Tate `mu_5` is a *third* line, `phi` restricts to an isomorphism
`mu_5 -> C_1`, and the Weil pairing of two distinct lines in a symplectic plane
is perfect: **`beta_11` is non-degenerate on `W_11`**.

**The other places.** beta is alternating everywhere (§5.1.5's argument:
decomposable, 2 invertible mod 5), so `beta_v = 0` wherever `dim W_v <= 1`.
`W_inf = 0` (5-divisible); good `v != 5` by unramified isotropy; at `q | d` both
`C_i^(d)(Q_q) = 0` since `chi_d` is ramified there and the cyclotomic character
is not, so `W_q = 0`; at `v = 5` the cyclotomic character has order 4 on `G_5`
while `chi_d` has order <= 2, so `C_2^(d)(Q_5) = 0` always and
`dim W_5 = 1 + [d square in Q_5]`.

> *Theorem.* Let `d` be squarefree, a square in `Q_11` (class `[1]`), and **not**
> a square in `Q_5`. Then `E_0^(d)(Q)` is not dense in `E_0^(d)(Q_11)`; hence
> `X(Q)` is not dense in `X(Q_11)`.

An earlier version of this theorem also asked `d` not to be a square in `Q_5`,
covering 652 of 1115 twists; that condition is gone. At `v = 5` -- the wild place
here, since `l = 5` -- either `dim W_5 <= 1` and beta is alternating, or `d` is a
square in `Q_5`, which forces `5` not to divide `d`, so `E_d = E_0` over `Q_5`
with good reduction, and `a_5 = 1` is prime to 5: **ordinary**, so document
§6.5.1 gives `beta_5 = 0`. §3.3.1 agrees: none of 584 twists reached the full `(Z/5)^2`, and the six lines came up
92/97/97/103/77/95 -- which is all an alternating form on `(Z/5)^2` can say,
since *every* line there is isotropic.

**Lesson for the triage.** A non-scalar `phi` is necessary but not sufficient:
one also needs `L_p` not to be `phi`-stable, a further *local* condition -- and
it is exactly the input §5.1.5 long left open (now proved, and independently
evaluated: see below and document §8.3.4). At a place of multiplicative
reduction it is free, since `W_p` is then forced to be `H^1` of the Tate `mu_l`:
the mechanism fires iff the Tate `mu_l` is neither `C_1` nor `C_2`, i.e. iff
neither global subgroup meets `E^0`.

### 14a2 and 19a1: the 14a1 template, twice more (document §7.5)

The two remaining odd-level classes, done together because they are the same
argument. Both are level 3 with `E[3]` decomposable and `p = 1 mod 3`, so the
critical symbol is tame.

`14a2` needs no reindexing: `y^2 = f(x)` with `f = x^3-11x^2-528x-2240` **is**
14a2, conductor 14, torsion `Z/6`. `19a1` does: `f = x^3-10x^2-4x-6` gives
conductor `2^6 * 19`, so `y^2 = f(x)` is `E_0^(2)` for `E_0 = 19a1`, and since
**2 is a non-residue mod 19** the surface's failing class `[u]` at 19 becomes the
class `[1]` for the reindexed parameter. That is the caveat of §7.8.1 biting: the
reduced cubic is a model of the *surface* and can twist the curve.

At the critical place both lines of `E[3]` are `Q_p`-rational, so `dim W_p = 2`
(`M_p = 36` and `54`), and the image of `(c_1,c_2)` on `W_p` has **9 elements**,
so `c_1` alone identifies `W_p` with `Q_p^x` modulo cubes. Written additively on
classes `[al, j]` -- valuation mod 3, cubic character of the unit -- the tame
symbol is

    (a,b)_p = j_a be - j_b al   in Z/3

(the sign `(-1)^(al be)` drops because `p = 1 mod 6` makes `(p-1)/3` even), which
is the standard symplectic form: 48 of 81 non-zero, diagonal zero, at 7 and at 19
alike.

At the wild place `v = 3`, four square classes settle every twist and both curves
reproduce the 14a1 table exactly, exhibiting **both** mechanisms of §6.5: classes
`[u]` and `[3]` have `dim W_3 = 1`; class `[1]` is good ordinary (`a_3 = -2` for
both) and falls to §6.5.1; class `[3u]` is additive `I*0` with the descent image
trivial, so `L_3` collapses into one `H_i`. Everything else is structural --
§6.4.1 at `q | d`, unramified isotropy at good `v != 3`, and for `14a2` the one
extra bad prime `v = 2`, where `mu_3` is not in `Q_2` so `dim W_2 <= 1`. For
`19a1` the conductor is 19, so there is no such place at all.

> *Theorem.* (a) For `f = x^3-11x^2-528x-2240` and every squarefree `d` in the
> class `[1]` of `Q_7`, `X(Q)` is not dense in `X(Q_7)`.
> (b) For `f = x^3-10x^2-4x-6` and every squarefree `d` in the class `[u]` of
> `Q_19`, `X(Q)` is not dense in `X(Q_19)`.

`template3.gp` does the computations. Two things it forced: the image must be the
**subgroup generated** by the values on a generating set, not the set of values
-- taking the latter gave 19a1 sizes 5, 6, 7, which are not subgroup orders and
differed between twists that are `Q_19`-isomorphic; and the tame symbol is much
cleaner in the additive encoding than as a product in `F_p`.

### 17a1 at p = 17: the last class (document §7.6)

The eighth class, and the only one at `l = 2` with `E[2]` **indecomposable** --
so the toolkit's decomposable framing does not apply and document §6.2 is what is
needed. It is the shortest of the six, because it has exactly the 15a4 shape.

`f = x^3-6x^2+x-876 = (x-12)(x^2+6x+73)` **is** 17a1 (conductor 17, torsion
`Z/4`), and shifting by the rational root -- with `c = 1`, so the same surface --
gives

    f = x(x^2 + 30x + 289),   289 = 17^2,   x^2+30x+289 = (x+15)^2 + 8^2

Two features of that quadratic do all the work. It is a **sum of two squares**,
so the 2-torsion field is `Q(i)`, `c(P)` is a norm from `Q(i)`, and beta is
alternating at every place (Lemma 2). And `b = 289` is a **perfect square**,
which is the hypothesis under which Lemmas A and B transfer (document §6.2.2):
at every odd `q | d` the image of `c` has order at most 2, and an alternating
form vanishes there.

`E[2]` has the unique stable line `C = <T>`, `T = (0,0)`, so by §6.2 the twisting
endomorphism is the nilpotent `N` and is essentially unique, `c(P) = x(P)` modulo
squares, and `beta_v(P,Q) = (c(P), c(Q))_v` -- the **same** map in both slots, so
triviality at `v` means the image of `c` is *isotropic* rather than that `L_v`
splits. The remaining places: `v = infinity` because the quadratic is positive so
`x >= 0` on real points; `q` not dividing `2*17*d` by coprimality, the bad places
being only 2, 17 and the divisors of `d`; `q = 2` over all **eight** square
classes, image of order at most 2 every time; and `q = 17`, where the image is
**all four** classes so the symbol is non-degenerate, and `-1` is a square there
since `17 = 1 mod 4`, so `beta_17` is symplectic on the 2-dimensional `W_17`.

> *Theorem.* For `f = x(x^2+30x+289)` -- that is, 17a1 -- and every squarefree
> `d` in the class `[1]` of `Q_17`, `E_d(Q)` is not dense in `E_d(Q_17)`, hence
> `X(Q)` is not dense in `X(Q_17)`.

The four surfaces at `l = 2` split two ways: `15a1`, where `f` splits over `Q`
and three stable lines force a search among nine candidate `phi`; against
`x^3+x`, `15a4`, `17a1`, all indecomposable, all with `q` a sum of two squares
and `b` a perfect square, all following §5.5.4 with nothing to choose.

### §5.1.5's input, checked in Magma and then proved (document §7.7)

`beta_3 != 0` for `x^3-2` -- i.e. that both dual 3-isogeny images lie in
`E_1(Q_3)` -- confirmed a third time, in Magma, by a different route: the PARI
check built the duals by hand, `verify-dual.sage` constructed `Q_3`-points and
tested membership, and this evaluates the dual isogeny's *rational maps*
(`IsogenyMapPhi` / `IsogenyMapPsi`) at 3-adic x-coordinates, constructing no
points at all.

```
Qx<x> := PolynomialRing(Rationals());
K := pAdicField(3, 200);
for d in [-3, 6, -21, 87] do
  E := EllipticCurve([0,0,0,0,-2*d^3]);
  printf "\nd = %o   Kodaira at 3 = %o   c_3 = %o\n",
         d, KodairaSymbol(E,3), TamagawaNumber(E,3);
  for ker in [x, x - 2*d] do
    Ep, phi := IsogenyFromKernel(E, ker);
    ph  := DualIsogeny(phi);
    num := IsogenyMapPhi(ph);  den := IsogenyMapPsi(ph);
    a := aInvariants(Ep);  A := K!a[4];  B := K!a[5];
    tot := 0; bad := 0;
    for n in [-8..30] do
      for u in [1,2,4,5,7,8,10,11] do
        x0 := K!u * K!3^n;
        v  := x0^3 + A*x0 + B;
        if v ne 0 and IsSquare(v) then
          d0 := Evaluate(ChangeRing(den, K), x0);
          if d0 ne 0 then
            X := Evaluate(ChangeRing(num, K), x0) / d0^2;
            tot +:= 1;
            if X ne 0 and Valuation(X) ge 0 then bad +:= 1; end if;
          end if;
        end if;
      end for;
    end for;
    printf "  kernel %o : %o points of E'(Q_3), %o outside E_1\n", ker, tot, bad;
  end for;
end for;
```

| d | Kodaira at 3 | c_3 | points of E'(Q_3) | outside E_1 |
|---|---|---|---|---|
| -3 | IV* | 3 | 244 / 20 | **0 / 0** |
| 6 | IV* | 3 | 244 / 20 | **0 / 0** |
| -21 | IV* | 3 | 244 / 20 | **0 / 0** |
| 87 | IV* | 3 | 244 / 20 | **0 / 0** |

(The two counts are the two kernels `x` and `x-2d`.) Magma also independently
returns `IV*` and `c_3 = 3`, the labels Sage corrected.

**And then a proof.** `phihat(E'_1)` is inside `E_1` -- an isogeny extends to the
Neron models, so it respects the kernel of reduction -- hence phihat induces a
map of the *finite* groups `A' = E'(Q_3)/E'_1 -> A = E(Q_3)/E_1`. Checking that
is exhaustive, and it needs one point:

| step | value | | conclusion |
|---|---|---|---|
| `#E[3](Q_3)` | 3 | only `x = 2d` is rational | `#E(Q_3)/3E = 9` |
| `#A = M` | 9 | 4 classes found outside `E_1`, all killed by 3 | `#A[3] >= 5 > 3`, so `A` is not cyclic: `A = (Z/3)^2` and `3E = E_1` |
| `#A' = M'` | 3 | **prime** | one class generates `A'` |
| `v_3(x(phihat P'))` | -2 | for `P'` outside `E'_1` | `phihat P'` is in `E_1`, so `phihat(A') = 0` |

The last two lines are the content: since `#A' = 3` is **prime**, any single
class outside `E'_1` generates `A'`, so one evaluation settles the induced map.
Both kernels give `v_3 = -2`, and the same numbers come out for
`d = -3, 6, -21, 87, -30, 69`. One `d` suffices, since all `d` in a square class
give `Q_3`-isomorphic curves -- exactly the reduction §5.1.5 needs.

> So `phihat(E'(Q_3))` is inside `E_1(Q_3) = 3E(Q_3)` for **both** 3-isogenies:
> the two dual images are zero in `W_3`, so `W_3` meets neither `H^1(C_1)` nor
> `H^1(C_2)`, so `W_3` is not phi-stable, so **`beta_3 != 0`**. The input §5.1.5
> leaves open is closed.

```
gp -q -s 4000000000 survey.gp < /dev/null
firstout(...); isogapply(...)     /* see results/survey-beta3-exhaustive.txt */
```

The only thing still taken on trust is the standard fact that an isogeny carries
`E'_1` into `E_1`; everything else is a finite computation.

### The triage, in hindsight (document §7.8)

Before constructing anything, ask whether the mechanism is *available*: it is
not automatic. `beta_v(P,Q) = <delta_v P, phi delta_v Q>` is useful only when
`phi` is **non-scalar**, since a scalar collapses beta to the untwisted Tate
pairing, which vanishes on the Lagrangian `W_v`. So the first question is
whether `End_G(E[l]) != F_l`.

| structure of E[l] | End_G | non-scalar? |
|---|---|---|
| two stable lines (decomposable) | `F_l x F_l` | **yes** |
| one stable line, characters agree | `F_l[N]/(N^2)` | **yes** |
| one stable line, characters differ | `F_l` | no |
| irreducible, image in nonsplit Cartan | `F_{l^2}` | **yes** |
| irreducible, larger image | `F_l` | no |

The Weil pairing forces `chi_1 chi_2 = cyclo_l`, so with a rational `l`-torsion
point (`chi_1 = 1`) the characters agree **only for l = 2**. Hence: at `l = 2`,
reducible always works, irreducible works iff `disc f` is a square; at odd `l`,
need two stable lines or a nonsplit-Cartan image. Twisting changes none of it,
since `End_G(V (x) chi) = End_G(V)`.

```
pairingpossible("11a1", [-11,-1,-83], 5);
triagepair("11a1", [-11,-1,-83], 11, 1, 5);
```

> **All the remaining classes admit a non-scalar phi** -- the mechanism is
> available in every one of the eight cases of §3.3. What is *not* settled is
> whether it fires: selecting the right phi and proving the local vanishing
> still has to be done case by case.

**Caveat on the model.** The reduced monic cubic is a model of the *surface*,
and `reduce_cubic` can twist the curve, so the curve to analyse is `E_0`, the
minimal quadratic twist -- that is where the rational `l`-torsion lives.

| class | p | l | E_0: N | tors | stable lines | p mod l | E[l] |
|---|---|---|---|---|---|---|---|
| 11a1 [u] done | 11 | 5 | 11 | 5 | 2 | 1 | decomposable |
| 14a2 [1] | 7 | 3 | 14 | 6 | 2 | 1 | decomposable |
| 19a1 [u] | 19 | 3 | 19 | 3 | 2 | 1 | decomposable |
| 15a4 [1] done | 5 | 2 | 15 | 8 | 1 | 1 | indecomposable |
| 17a1 [1] | 17 | 2 | 17 | 4 | 1 | 1 | indecomposable |
| 14a1 [1] done | 7 | 3 | 14 | 6 | 2 | 1 | decomposable |
| 15a1 [1] done | 5 | 2 | 15 | 8 | 3 | 1 | split |

Two things hold across the board. **`p = 1 mod l` in every case, and it is
forced** (`dim W_p = 2` needs `mu_l` in `Q_p`), so at the *critical* place the
symbol is always the **tame** `l`-th power residue symbol, never the wild one --
the structural reason every case here is more tractable than §5.1.5's. And the
places that can carry beta are only three kinds: the critical `p`, the wild
`v = l`, and bad primes `= 1 mod l` where `d` is a square -- and there are **no**
such extra bad primes for the three odd-`l` cases.

**Two templates.**

* *Template C (§6.2), l odd and E[l] decomposable*: `14a2`, `19a1`, and `11a1`
  which §6.4 has since carried out. beta is alternating for free, so only the
  wild `v = l` is left; these went through exactly as 14a1 did -- see §7.5, where
  both mechanisms of §6.5 appear, one in each of two square classes. (The worry
  that `11a1` would need a quintic symbol was unfounded -- see §7.4.)
* *Template A (§5.5.4), l = 2 with E[2] indecomposable*: `15a4` and `17a1`,
  carried out in §7.3 and §7.6. Here
  the alternating step is not free (2 is not invertible mod 2). But the norm
  lemma applies: **both have 2-torsion field `Q(i)`**, so `(c(P),-1)_v = 1` and
  beta is alternating for the same reason as `x^3+x`.

Suggestive rather than proved: of the four defective classes at `l = 2`, the
three with `E[2]` indecomposable all have 2-torsion field `Q(i)` -- the condition
making beta alternating -- while `x^3+2x`, which has no obstruction, does not.

## Two live places (document §9)

Every theorem of §7 arranges for `beta_v` to vanish at all places but one, so
that reciprocity forces the survivor to zero. What if *two* places survive?
Reciprocity then gives `beta_p + beta_q = 0` on rational pairs, so the image
`R` of `E_d(Q)` in `W_p (+) W_q` is **isotropic** for `gamma = beta_p (+) beta_q`
rather than zero. If `gamma =/= 0` then `R` is proper, `W_p x W_q` is a finite
discrete quotient of `E_d(Q_p) x E_d(Q_q)`, and `E_d(Q)` is not dense in the
product. With both `beta` non-degenerate alternating, `dim R <= 2` out of 4.
The passage to `X` is NOT via the properness of `R` (see §6.8): it is the
isotropy itself that transfers, since `beta_p(P_p,Q_p) + beta_q(P_q,Q_q) = 0`
is a proper closed condition on a point of the *product* `X(Q_p) x X(Q_q)`.

The point is that **this is invisible one place at a time**: `R` can be the
graph of an isomorphism `W_p -> W_q`, so `X(Q)` may be dense in `X(Q_5)` and
dense in `X(Q_13)` and still fail in the product. The §3 survey tests one prime
at a time and would report a witness in every class.

Where to look: both structural lemmas must hold at once, which forces
`al^2 + mu^2 = k^2`, so the level-2 family is indexed by Pythagorean triples and
the odd live places divide the hypotenuse `k`. **That is why the phenomenon
never showed up in §7**: the three surveyed members of the family are `x^3+x`
(0,1,1), `15a4` (7,24,25) and `17a1` (15,8,17), and 1, 25, 17 are all prime
powers -- a prime-power hypotenuse allows only one live place.

`twoplace.gp` screens 24 triples: 11 have a single live place, 13 have two or
three. The cleanest hit is

    (33,56,65):  f = x(x^2 + 66x + 4225),  N = 7280,  live at 5 and 13,

both odd, each live in exactly one square class (the class of 1) with the image
of `c` all four classes, hence symplectic; dead at `v=2` in all eight classes,
at 7 and every other `q` not dividing `2*5*13*d` by coprimality, and at odd
`q | d` by §6.2.2 since `b = 65^2`. Runner-up: (13,84,85), live at 5 and 17.

**Theorem.** For `f = x(x^2 + 66x + 4225)` and every squarefree `d` that is a
square in both `Q_5^x` and `Q_13^x`, `E_d(Q)` is not dense in
`E_d(Q_5) x E_d(Q_13)`; hence `X(Q)` is not dense in `X(Q_5) x X(Q_13)`.

Not settled: whether `X(Q)` *is* dense at 5 and at 13 separately for this
surface (if so, the obstruction is genuinely two-place); whether the Azumaya
algebra of §8 is unramified here; the split and level-3 analogues; and the
three-live-place case, e.g. (16,63,65).

## What the obstruction depends on (document §10)

Chapters 7 and 9 build examples one at a time. §10 asks the structural
question: given `E`, `ell` and a non-scalar `phi`, which sets `S` are
obstructed, and in which square classes -- *without* analysing the `beta_v`?

The reformulation that makes it tractable: the local Kummer image `L_v` is
**maximal isotropic**, so `beta_v = 0` exactly when `phi_* L_v = L_v`. Hence
everything is `Sigma(d) = {v : L_v not phi-stable}`, and the obstructed sets are
the `S` containing it. Then:

1. **Only bad places.** At good `v != ell`, `L_v = H^1_ur`; at good `v = ell`,
   `L_v = H^1_f`. Both are functorial in the Galois module, hence phi-stable for
   EVERY phi. (This upgrades §6.5.1: good *ordinary* was never needed, good
   reduction suffices, supersingular included.) For `ell` odd, `infinity` and
   the `q | d` also die, so `Sigma(d)` sits inside the primes of `N_E`,
   independent of `d`.
2. **`v = 1 mod ell`.** A non-zero alternating `beta_v` needs `dim W_v = 2`,
   i.e. all of `E_d[ell]` rational over `Q_v`, so `zeta_ell` in `Q_v`. This is
   the sharpest filter, and it is **why the hypotenuse appeared in §9**: the
   2-torsion field there is `Q(i)`, so live places are `= 1 mod 4` -- exactly
   the primes dividing a hypotenuse.
3. **`ell | v(Delta_min)`,** at a multiplicative place. Additive types have
   `c_v <= 4`, so for `ell >= 5` only split multiplicative places can be live
   at all: the obstructed set is read off the discriminant. (`11a1`:
   `Delta = -11^5`, `ell = 5`.)
4. **The class is forced:** of the four, the two ramified ones give additive
   reduction and one unramified one gives non-split; only the split class
   survives. That is the "one class in four" of every §7 theorem.
5. **phi decides** by comparing the canonical line `C_can = mu_ell` with its
   stable lines, and `C_can` is visible with no local points: quotienting by it
   MULTIPLIES `v(Delta)` by `ell`, an etale line divides it by `ell`.

**Caveat at `ell = 2`** (found by Rene, 2026-08-21): step 1 leans on Lemma 2,
which needs `ell` odd. At `ell = 2` the recipe's output is `Sigma(d)` intersected
with the ODD MULTIPLICATIVE places -- it says which of those are critical and
nothing about `infinity`, `v = 2`, or the `q | d`, which need §6.2.2 and the
norm lemma and are phi-dependent. So a one-element output at `ell = 2` is **not**
a one-place obstruction. The worked example is 15a1 itself: for
`phi = (c_2,c_3)` the recipe correctly reports 3 as critical, but the full
`Sigma(-1) = {infinity, 2, 3}`, so reciprocity gives a three-place correlation
and nothing is obstructed at 3 alone -- which is exactly why the §3 search
witnesses every class of 15a1 at `p = 3` and fails only at `p = 5`.

`depends.gp` runs the recipe; the predicted sets agree with the computed ones in
all nine cases in scope. The sharpest test is **`15a1`, where phi changes the
answer**: the canonical line is `e = 17` at `v = 3` and `e = 1` at `v = 5`, so
the document's phi (pairing `c_1, c_3`, excluding `e = 1`) is obstructed at 5,
and the other phi (pairing `c_2, c_3`, excluding `e = 17`) should be obstructed
at **3, in class `[u]`**. That prediction was made from the discriminant data
and then confirmed directly: six non-trivial symbols at 3 for the second phi,
and `|im c_2| = 1` at 5. Same curve, same `ell`, disjoint obstructed sets.

**Sufficiency is now proved at a Tate place** (§10.7). The key structural fact:
at a split multiplicative `v` not dividing `ell` with full local `ell`-torsion,
the Kummer image is the cohomology of the canonical line,

    L_v = H^1(Q_v, C_can),

because the Kummer cocycle of a point represented by `u` in the Tate
parametrisation takes its values in `mu_ell`. Since `H^1(Q_v,-) = Hom(G_v,-)`
on trivial modules, `phi_* L_v = H^1(Q_v, phi(C_can))`, so phi-stability of
`L_v` IS phi-stability of `C_can` -- both directions at once. Writing
`phi(s) = a s + b t`, one gets `beta_v(P,Q) = b * (u,w)_ell`, the `ell`-th power
Hilbert symbol, which is **non-degenerate** when `b =/= 0`. That is why every
case in §7 came down to a symbol, and why live places always showed the *full*
image.

The global consequence (§10.7.2, Corollary 6): for `ell >= 3` put
`S = {infinity, ell} u {v | N_E}`. If ONE place of `S` satisfies the hypotheses,
then `X(Q)` is not dense in the product of `X(Q_v)` over `S`, so weak
approximation fails. This never computes `Sigma(d)`: a sum over a product is
non-constant as soon as one summand is, so one non-zero `beta_p` inside a finite
`S` is all an obstruction needs -- and `S` does not depend on `d`, which is what
makes the conclusion about the surface.

**The additive case is now done away from `ell`** (§10.8). Two observations:

* The split-multiplicative class need not be UNRAMIFIED. When `E` is additive of
  type `I_n*` -- potentially multiplicative -- it is a ramified class that makes
  `E_d` split, so those places were never a gap: Theorem 5 governs them via a
  twist with `v | d`. Step 4 and `splitclass` now scan all four classes.
* **Lemma 7**: at an additive `v` not dividing `ell`, the formal group is pro-`v`
  and `E_0/E_1` is the additive group of the residue field, so `E_0(Q_v)` is
  pro-`v` and `W_v = Phi_v/ell`, the component group mod `ell`. Since `c_v <= 4`:
  `ell >= 5` gives `W_v = 0`; `ell = 3` gives `dim W_v <= 1`, hence
  `beta_v = 0` since beta is alternating at odd `ell`; `ell = 2` gives
  `dim W_v <= 2`, with equality exactly for `Phi = (Z/2)^2`, i.e. `I_n*` with `n`
  EVEN.

**Theorem 8**: for `ell >= 3` and `v` not dividing `ell`, `v` is in `Sigma(d)`
if and only if `E_d` is split multiplicative at `v`, `E_d[ell]` is fully rational
over `Q_v`, and phi does not stabilise `C_can`. That is sufficiency answered
outright at every place away from the wild one.

At `ell = 2` additive places really can be live. **Proposition 9**: for type
`I0*` with `c_v = 4`, `W_v = Phi_v` is generated by the 2-torsion, so beta is a
Hilbert symbol of ROOT DIFFERENCES -- `c_i(T_j) = e_j - e_i` -- with no point
search. Verified exactly against a direct search on four curves and all three
phi, and those places are live.

Still open: the degenerate `ell = 2` additive case `dim W_v = 1`, where beta can
be non-zero only by failing to be alternating; and the wild place. Also not written out: the
identification of `C_can` by the `j`-valuations of the `ell`-isogenous curves.

**Why the wild place is genuinely different**, in one line: Lemma 7 needs
`v` not dividing `ell` so the formal group is prime to `ell`. At `v = ell` the
formal group is pro-`ell` and contributes EVERYTHING -- which is why `x^3-2` is
live at `v = 3` on a type II fibre with `c_v = 1`, where `Phi/3 = 0`. Its `W_3`
is entirely formal group. That is the one place no argument in §10 reaches.

## Six new obstructed surfaces, predicted then verified (document §10.9)

Theorem 8 makes the search mechanical. For a SINGLE critical prime, add
`ell` not dividing `N_E` to Corollary 6, so that Lemma 1(b) kills the wild place
-- the one place Theorem 8 does not reach. Then `Sigma(d) = {p}` for every `d`
in the class that (a) selects, reciprocity forces `beta_p = 0` on rational
pairs, and §6.8 gives non-density in `X(Q_p)`.

`corollary6.gp` scans the universal curve with a rational 3-torsion point,
`y^2 + axy + by = x^3`, since decomposable `E[3]` is rare in a naive box. Eight
curves in `|a|,|b| <= 40` satisfy the hypotheses, and ALL EIGHT have exactly one
critical prime. Two are already in the document (14a1, 19a1); six are new. At
`ell = 5` the Tate 5-torsion family returns exactly one curve -- conductor 11,
i.e. 11a1, the known case, rediscovered and alone in its range.

| `N_E` | `f` | critical `p` | class |
|---|---|---|---|
| 26 | `x^3 + x^2 - 72x - 496` | 13 | `[1]` |
| 35 | `x^3 + 4x^2 + 144x + 80` | 7 | `[1]` |
| 37 | `x^3 + 4x^2 - 368x - 3184` | 37 | `[1]` |
| 38 | `x^3 + x^2 + 152x + 5776` | 19 | `[1]` |
| 91 | `x^3 + 4x^2 + 208x + 2704` | 13 | `[1]` |
| 370 | `x^3 + x^2 - 296x + 21904` | 37 | `[1]` |

**Theorem 10**: for each of these, at `ell = 3` and for every squarefree `d` in
the class `[1]` of `Q_p`, `beta_p` vanishes on rational pairs; hence `X(Q)` is
not dense in `X(Q_p)`.

**Two more at `ell = 5`.** Quadratic twists give the SAME surface -- substituting
`x = du, t = dw` in `y^2 = f_d(x)f_d(t)` gives `y^2 = d^6 f(u)f(w)` -- so the
parameter is `j`, and the better search scans the genus-0 Hauptmodul of `X_0(ell)`
for `j` whose curves have decomposable `E[ell]`. At `ell = 5`, four `j` turn up in
2066 scanned; one is 11a1, one has `5 | N` (wild place uncovered), and two are new:

| `N_E` | `f` | critical `p` | class |
|---|---|---|---|
| 23808 | `x^3 - 4x^2 + 30608x - 5474624` | 31 | `[u]` |
| 18176 | `x^3 + 4x^2 - 69104x - 6427840` | 71 | `[u]` |

Note the live class is `[u]`, not `[1]`: the class prediction is doing real work.

**The experiment.** Nothing in the prediction used a rational point, so the §3
search is a genuine test. `corollary6-check.gp` runs it over the 45 odd primes
`<= 200` and the eight classes at 2:

    all eight:  44 of 45 odd primes full, and the prime that falls short is
                EXACTLY the predicted one, missing EXACTLY the predicted class.

Eight predictions, eight confirmations, with the critical prime and class read
off the conductor, the discriminant and the isogeny class beforehand.

**How many are there? (§10.9.1)** For odd `ell`, a non-scalar phi needs `E[ell]`
decomposable or a non-split Cartan image -- the indecomposable-equal-characters
case would need `alpha^2 = chi_cyc`, impossible since `chi_cyc` is surjective onto
`F_ell^x` while squares are not. And decomposable `E[ell]` forces `ell <= 5`:
with `E[ell] = C_1 + C_2` and `E_1 = E/C_1`, the composite
`E_1 -> E -> E/C_2` has cyclic kernel of order `ell^2` (it is not `E_1[ell]`,
since `hat-psi_1(E_1[ell]) = C_1 =/= C_2`), so `E_1` carries a rational cyclic
`ell^2`-isogeny, and Mazur-Kenku bounds those: `ell^2` in {4, 9, 25}.

* **`ell = 7` is EMPTY, necessarily.** Confirmed: 2066 `j` scanned on `X_0(7)`,
  none decomposable. The remaining loophole is a non-split Cartan image mod 7
  (giving `End = F_49`; nothing in §10.7 needs phi to have rank one) -- but a
  scan of 2596 curves testing whether `a_p^2-4p` is a non-square mod 7 at every
  good `p <= 200` finds none compatible. Not pursued further.
* **`ell = 3` and `ell = 5` are INFINITE.** `X_0(9)` and `X_0(25)` both have
  genus 0, so infinitely many `j` carry a rational cyclic `ell^2`-isogeny and the
  middle curve of each chain has `E[ell]` decomposable. Whether infinitely many
  have EXACTLY ONE critical prime is not proved, but the yield is high: 8 of 8 at
  `ell = 3`, and 3 of 4 at `ell = 5`. One row needed
a second look: at `p = 2` the surface `N_E = 38` first reported 6/8. Theorem 8
says 2 cannot be critical at `ell = 3` (full rationality of `E_d[3]` over `Q_2`
would need `zeta_3` in `Q_2`), so that had to be search depth -- raising the
bound from 300 to 4000 gives 8/8.

**Additive at the critical prime (§10.9.2).** None of the eight has its critical
class among the ramified ones. But hypothesis (a) is about `E_d`, not `E`, and by
§10.8.1 the split class is RAMIFIED when `E` is of type `I_n*`. Twisting the
defining cubic by its own critical prime, `f_p(x) = p^3 f(x/p)`, gives the SAME
surface in a model where `E` is additive at `p`, and the critical class must move:

| `f` after twisting | `ell` | type at `p` | predicted class | search |
|---|---|---|---|---|
| `x^3 + 28x^2 + 7056x + 27440` | 3 | `I3*` at 7 | `[7]` | 44/45, misses `[7]` |
| `x^3 + 13x^2 - 12168x - 1089712` | 3 | `I3*` at 13 | `[13]` | 44/45, misses `[13]` |
| `x^3 + 148x^2 - 503792x - 161279152` | 3 | `I3*` at 37 | `[37]` | 44/45, misses `[37]` |
| `x^3 + 284x^2 - 348353264x - 2300594642240` | 5 | `I5*` at 71 | `[u*71]` | 44/45, misses `[u*71]` |

Four for four, the last being sharpest: the recipe named the FOURTH of the four
classes and that is the one the search misses. These are the same surfaces in
different models, so what is tested is §10.8.1 plus the class-label transport --
change the model and the obstruction reappears where the recipe says.

**At odd `ell` that is the only way an additive critical place arises**: by
Lemma 7 a potentially GOOD additive place has `dim W_v <= 1` at `ell = 3` and
`W_v = 0` for `ell >= 5`, so it is dead. Every critical place at odd `ell` has
`v(j) < 0`, and multiplicative-versus-additive is a choice of twist. Genuinely
additive critical places exist only at `ell = 2`, as the `I0*` fibres with
`c_v = 4` of Proposition 9, which Corollary 6 does not reach.

## Level 2: the norm lemma is false, and additive places are never isolated (§10.10)

**The norm lemma cannot be repaired for split `f`.** It controls
`(c(P),c(P)) = (c(P),-1)`, which is the diagonal of beta only when BOTH SLOTS
carry the same descent map -- true in the indecomposable case, where
`ker N = im N`. For split `f` the rank-one phi has `ker = C_a`, `im = C_b` with
`a =/= b`, and the diagonal is `(c_a(P), c_b(P))`, which nothing controls: for
`x(x-5)(x+5)` at 5, two of the three 2-torsion points have `beta(P,P) =/= 0`.

**The right statement**: isotropy of `L_v` gives
`(c_a(P),c_b(Q)) = (c_b(P),c_a(Q))`, so beta is SYMMETRIC, with a quadratic
refinement `q_v(P) = beta_v(P,P)`; "alternating" is the case `q_v = 0`. So
Lemma 3's dimension bound FAILS for split `f` -- a 1-dimensional `W_v` can be
live through the diagonal alone, which is exactly what happened at infinity for
15a1's second phi.

**Lemma 11** replaces the norm lemma at the places `q | d`: for split `f`,
`beta_q = 0` for every odd `q | d` prime to `2 disc f` **iff `f'(e_a)` and
`f'(e_b)` are both perfect squares**. Lemma C of §7.1.2 is the case of 15a1
(`f'(e_1) = 400`, `f'(e_3) = 225`); its second phi fails it (`f'(e_2) = -144`),
which is why that `Sigma` was large. Ordering the roots and writing the gaps as
`p, q`, the condition is `p(p+q)` and `q(p+q)` square, hence `pq` square; with
`p = g a^2, q = g b^2` it becomes `a^2 + b^2 = square` -- **Pythagorean triples
again**, 15a1 being (3,4,5) with gaps 9 and 16. And `beta_infinity` dies for
every `d` exactly when the excluded root is the MIDDLE one.

**Why no isolated additive example FOR SPLIT `f` AT ODD `v`.** Two facts, not
one: (i) `disc f` is a perfect square for split `f`, so `v(Delta_min)` is EVEN
at odd `v`, which excludes `III` (3), `III*` (9) and `I_n*` with `n` odd; and
(ii) of the remaining potentially good types, `II`, `IV`, `IV*`, `II*` have
`Phi` trivial or `Z/3`, so `Phi[2] = 0` and Lemma 7 gives `W_v = 0`. Only `I0*`
survives -- and `I0*` IS a ramified quadratic twist of good reduction. Additive
live places are real:
for `x(x-3)(x-5)`, the place 13 is live in class `[13]` on an `I0*` fibre with
`c_v = 4`, potentially GOOD, so no twist makes it multiplicative. But a
potentially good additive fibre means the model is a ramified twist of a good
one -- rescaling `f` by a square, which does not change the surface, returns a
model where the place is good and the liveness reappears as a place `q | d`. So
an additive live place IS a `q | d` place in another model, and those come as an
infinite family indexed by the primes dividing `d`, all live or all dead
together by Lemma 11. Concretely `x(x-507)(x-845)` is `u(u-3)(u-5)` rescaled by
`169`: in the first model 13 carries a live `I0*` fibre, in the second 13 is not
even a bad prime. Both report conductor 480.

Since a surface-level statement needs the `q | d` places dead (§6.8), and that
condition kills the additive ones too, **an additive critical place of this kind
can never be the only live one**.

**Half the wild place is now closed (Lemma 12, §10.7.3).** For `ell` odd and
`E_d` potentially multiplicative at `v = ell`, `beta_ell = 0` whatever phi is.
The Kummer cocycle still lands in `mu_ell`, so `L_ell` sits inside
`H^1(Q_ell, C_can)`; and `dim Q_ell^x/(Q_ell^x)^ell = 1 + 1 + 0 = 2`, the middle
1 being the principal units -- the formal group, exactly what is absent when
`v` does not divide `ell`. Then either `q` is not an `ell`-th power, so
`dim W_ell = 1` and an alternating form on a line vanishes; or it is, and
`L_ell = H^1(Q_ell, C_can)` with `C_can` phi-stable for EVERY phi, since
`Hom_G(mu_ell, Z/ell) = 0` because `zeta_ell` is not in `Q_ell`. The non-split
and `I_n*` cases follow by restriction to the quadratic extension.

**So Corollary 6's hypothesis weakens** from "`ell` does not divide `N_E`" to
"`E` has good or potentially multiplicative reduction at `ell`" -- what must be
excluded is potentially GOOD ADDITIVE reduction at `ell`. Honestly, this buys
little supply: for a Tate curve the extension `0 -> mu_ell -> E[ell] -> Z/ell -> 0`
has class the Kummer class of `q`, so local decomposability at `ell` forces
`q` to be an `ell`-th power, and a scan of 3037 `j` on `X_0(3)` found NO curve
with `E[3]` decomposable and potentially multiplicative at 3. It closes a case
rather than opening one.

**And `x^3-2` IS an honest additive example** (spotted by Rene), and it escapes
all of the above. Its live class `[u*3]` at `v = 3` has type **IV\*** with
`c_3 = 3` -- potentially good, and NOT a quadratic twist of good reduction (no
quadratic twist of it is good at 3, checked). It escapes the parity argument
because `x^3-2` does not split; the `Phi[2]` argument because `ell = 3` there,
where `Phi = Z/3` is exactly what survives; and Lemma 7 itself, because
`v = ell`: `Phi_3[3]` is 1-dimensional while `dim W_3 = 2`, the missing
dimension being the formal group. So the strongest kind of additive critical
place does occur -- at the wild place, which is the case §10 never reaches. It belongs to obstructions whose set `S`
depends on `d` -- exactly what §6.8 cannot convert into a statement about `X`.
That, not the missing norm lemma, is the real obstacle.

## The wild place (document chapter 11)

The last gap: `v = ell` with potentially GOOD additive reduction, where the
formal group is pro-`ell` and survives into `W_v`. Chapter 11 does not close it,
but cuts it down to a narrow shape -- and the shape is what `x^3-2` has. For
`ell` ODD:

* **W1 (dimension).** `dim W_ell = 1 + dim E_d[ell](Q_ell)`, the leading 1 being
  the formal group; and `dim E_d[ell](Q_ell) <= 1`, since two independent
  rational `ell`-torsion points would put `zeta_ell` in `Q_ell`. So `dim W_ell`
  is 1 or 2, and it is 1 the pairing vanishes (alternating on a line). **A live
  wild place needs a `Q_ell`-rational `ell`-torsion point.**
* **W2 (module rigidity).** With `C_0` that rational line, the local module is
  `0 -> Z/ell -> E[ell] -> mu_ell -> 0` -- the OPPOSITE orientation to the Tate
  case of Lemma 12 -- and every phi preserves `C_0`, since the offending
  component lies in `Hom_G(Z/ell, mu_ell) = mu_ell(Q_ell) = 0`.
* **W3 (criterion).** `H^1(Q_ell, C_0)` is maximal isotropic of dimension 2 in
  the 4-dimensional `H^1(Q_ell, E[ell])`, so `L_ell = H^1(Q_ell, C_0)` forces
  `beta_ell = 0`; and that happens exactly when the `C_0`-descent map collapses.
  **So a live wild place needs `dim W_ell = 2` AND a non-collapsing alpha** --
  collapse (§6.5.2) is not just one route to vanishing, it is the only thing
  left to rule out.

`wild.gp` runs all four square classes of `x^3-2` at `v = 3`, and each is
accounted for by a different clause:

| `d` | type | `c_3` | `3 \| c_3` | rat. 3-tors | `dim W_3` | verdict |
|---|---|---|---|---|---|---|
| 1 | II | 1 | no | yes | 2 | `alpha = 0`: collapse, dead |
| 2 | II | 1 | no | no | 1 | dead by W1 |
| 3 | IV* | 1 | no | no | 1 | dead by W1 |
| 6 | IV* | 3 | **yes** | yes | 2 | `alpha =/= 0`: **live** |

and the survivor is exactly the class where §5.1.5 proves the obstruction sits.

**The Tamagawa reading is tempting and FALSE.** `d = 1` and `d = 6` both have
`dim W_3 = 2` and differ in `c_3`, i.e. in where the second dimension comes from,
so on `x^3-2` collapse happens exactly when `3` does not divide `c_3`. Tested
properly (`wild-tamagawa.gp`, §11.5) it fails on 8 of 14 curves -- and fails
STRUCTURALLY: **type II with `c_3 = 1` occurs both with `alpha = 0` and with
`alpha =/= 0`**. So reduction type and Tamagawa number together do not determine
whether alpha vanishes, and no recipe in the style of §10.6 can reach the wild
place. There is no invariant of that kind to find. What survives, as an
observation, is the useless direction: `3 | c_3` did always imply `alpha =/= 0`.

Two measurement traps had to be cleared to see this, the second for the third
time in this project: the image of alpha is a subgroup, but the set of values
over a SAMPLE is not (the sample is not closed under addition) -- which is why
`alpha3.gp` prints sizes like 2 and 5 and flags them as impossible. One must
take the SPAN. And the tangent evaluation was checked to be multiplicative mod
cubes first (400/400 pairs, three curves), so the map is a genuine homomorphism
and sampling is the whole explanation.

Not proved: sufficiency, and any criterion for non-collapse in terms of
reduction data. At `ell = 2` W1 already fails, since `zeta_2 = -1` is in `Q_2`,
so `dim W_2` can reach 3; `x^3+x` was settled by direct computation in §5.5 and
nothing here improves on it.

## beta_3 =/= 0 is now proved (document §5.1.5)

The one input the §5.1.5 theorem rested on -- that both dual 3-isogeny images
lie in `E_1(Q_3)`, equivalently that `W_3` is not phi-stable -- is settled, and
the theorem there is no longer conditional.

An isogeny extends to the Neron models, so `phihat(E'_1)` is inside `E_1` and
phihat induces a map of the **finite** groups `A' = E'(Q_3)/E'_1 -> A =
E(Q_3)/E_1`. Four facts settle that map:

* `#E[3](Q_3) = 3` -- of the two kernels only `x = 2d` is `Q_3`-rational, that
  being `T_d` -- so `#E(Q_3)/3E = 9`;
* `#A = M = 9`, and four distinct classes outside `E_1` are all killed by 3, so
  `#A[3] >= 5 > 3`: `A` is not cyclic, `A = (Z/3)^2`, and `3E(Q_3) = E_1`;
* `#A' = M' = 3`, **prime**, so any single class outside `E'_1` generates `A'`;
* for such a class, `v_3(x(phihat P')) = -2 < 0`, so `phihat P'` is in `E_1`.

A homomorphism killing a generator is zero. The primality of `#A'` is what makes
one evaluation enough -- it is the same order-3 observation the old status note
recorded as *blocking* a structural shortcut, which is true but is also exactly
what turns one point into an exhaustive check. Both kernels and
`d = -3, 6, -21, 87, -30, 69` agree; one `d` suffices, since all `d` in a class
give `Q_3`-isomorphic curves.

Raw output in `results/survey-beta3-exhaustive.txt`; the machinery (`ppointsE`,
`firstout`, `isogapply`) is in `survey.gp`. Before this the input had been
confirmed numerically three times -- PARI, Sage (`verify-dual.sage`, 3376
points) and Magma (rational maps of the dual, no point construction at all,
244 + 20 points) -- and all three are kept in §5.1.5, the last being what
prompted looking for a proof.

## The Sage cross-check

The theorem of §5.2.4 rests on one computation: that both dual-isogeny images
lie in `E_1`, equivalently that `W_3` is not stable under projection onto
`C_1`, equivalently that `beta_3` is not identically zero. The PARI version
(`control.gp`, `dualinE1`/`phistable`) builds the duals by hand and evaluates
them with its own substitution code, so it is worth re-running with
independent machinery.

`verify-dual.sage` does that with Sage's `EllipticCurveIsogeny.dual()` and
`rational_maps()`. The image is amd64 only, so it emulates on Apple silicon:

```sh
docker pull --platform linux/amd64 sagemath/sagemath:latest      # ~4.8 GB
docker run --rm --platform linux/amd64 -v "$PWD":/work -w /work \
       sagemath/sagemath:latest sage verify-dual.sage
```

It reproduces the same codomains, confirms `dual(phi) o phi = [3]`, and finds
3376 `Q_3`-points of the codomains with none leaving `E_1`. It also corrected
the Kodaira labels in the document: PARI's code `-4` is `IV*`, not `II*`.

### What Sage does not give us

Worth recording, since it is the obvious thing to reach for. Exhibiting the
Brauer class itself needs a cubic norm-residue symbol at the wild place
`v = 3`, and neither system here has one:

| tool | status |
|---|---|
| PARI `nfhilbert` | quadratic only, no exponent argument |
| Sage `hilbert_symbol` / `K.hilbert_symbol` | quadratic only, checked |
| Sage `E.three_selmer_rank()` | exists, but shells out to Magma |
| Brauer groups of surfaces | not in either system |

Away from 3 the symbol is tame and elementary, but those are exactly the
places the argument already handles structurally. So the gap is an
implementation task at one wild place, not a missing tool -- and the theorem
of §5.1.5 does not depend on closing it.
